# Roadmap

A status snapshot and prioritized next steps — not a promise or a schedule, just a living plan for a solo project. This repo stays private until the owner decides otherwise.

**This file is the durable source of truth for project state and decisions** — kept deliberately thorough so that pulling this repo fresh (a different machine, a new AI coding session with no memory of prior conversations, etc.) is enough to pick up where things left off without re-deriving context. If you're an AI assistant reading this cold: read this whole file plus `docs/protocol.md` before making architectural changes: several of the "obvious" simplifications (e.g. "just always call `update_character`", "just use full history") were deliberately rejected below, with reasons.

## Current state (done)

- Single-player skeleton: WebSocket server + Textual client, strict turn queue.
- Persistence: session state survives server restarts; the client remembers its own identity across restarts so it reconnects as the same character.
- Chat (`/chat <text>`) and dice rolls (`/roll <NdM[+/-K]> [reason]`) wired end-to-end, both exempt from turn order.
- DM narration behind a swappable `NarratorBackend` (`server/narrator.py`): `AnthropicNarrator` (hosted Claude) and `OllamaNarrator` (`server/narrator_ollama.py`, free local model). Both support tool use: a local SRD lookup tool (`server/rules/`, CC-BY-4.0 dataset) for consistent mechanics, and `update_character` for real HP/inventory/condition changes. Only `AnthropicNarrator` also has `web_search` — that's an Anthropic-hosted server tool with no local equivalent.
- **DM memory**: a rolling window of the last 6 turns (`Session.append_turn`, `MAX_HISTORY_MESSAGES` in `server/state.py`) is threaded through as real conversation history on every `narrate()` call, replacing the old single-last-turn-only context. Chosen over full history (unbounded cost growth) and a summary hybrid (more complexity than needed right now) explicitly because of the no-spend stance.
- **Real consequences**: an `update_character` tool (HP delta, add/remove inventory, add/remove a condition) the DM calls whenever narration should mechanically change the acting character. The engine applies it via `CharacterSheet.apply_update()` and pushes `character_update` to that player only when something actually changed (a no-op call, e.g. `hp_delta: 0`, doesn't spam an update). `CharacterSheet` gained a `conditions` field for this; the client sheet panel now renders it.
- CI, a 44-test suite, MIT license (with the SRD data separately CC-BY-4.0 attributed), issue/PR templates.
- **A real narration round-trip has been verified**, live, via Ollama — not just mocked, across five real turns total, two models compared head to head on the *same session* (same character "Torvin Ironheart", same rolling history):
  - **`llama3.1:8b`** — turn 1: correct, real `update_character` call (`hp_delta: -1`, the model's own judgment, no injury explicitly prompted). Turn 2, given "I approach the hooded figure and ask what they are drinking": did **not** call the tool at all — instead wrote its reasoning about calling the tool as plain narration text, including a literal fake `{"name": "update_character", "parameters": {...}}` JSON blob that leaked straight into the shared log, and broke character ("To answer this question, we need to update the character's sheet...") despite the system prompt explicitly saying never to. Confirmed via the server log that only one API request was made that turn (a real tool round-trip needs two) — Ollama's actual function-calling mechanism was never invoked; the model just role-played having a tool.
  - **`qwen2.5:7b`** — tried next, on the same session/character (now at HP 9/10 after the llama3.1 turns), specifically to see if a different model was more reliable at this. Turn 3, "I draw my sword and attack the hooded figure": correct, real tool call, `hp_delta: -4`, narration coherent (a magical flash, a shallow wound, the figure escaping), no leaked JSON, stayed in character. Turn 4, "I sheath my sword and look around" (deliberately non-mechanical, to test *precision* not just recall): correctly made **no** tool call — single API request, HP unchanged, clean narration. Turn 5, "I press a cloth against my wound to try to stop the bleeding" (a genuinely ambiguous case — first aid stops bleeding but doesn't typically restore HP without a proper check/spell): again correctly made **no** tool call. Three out of three clean, including one non-trivial judgment call, not just easy no-ops.
  - Minor style quirk noted on turn 5, not a correctness bug: narration ended with a numbered multiple-choice menu ("1. Ask the bartender... 2. Look around...") rather than open-ended prose. The system prompt asks for natural narration inviting the next action, not a menu — worth a prompt tweak if this recurs (e.g. explicitly say "no numbered option lists").
  - Sample size is still small (3 turns for qwen2.5:7b, 2 for llama3.1:8b) — not a rigorous benchmark, just enough signal to change the default. `qwen2.5:7b` is now `OllamaNarrator`'s default model (`server/narrator_ollama.py`, and `.env.example`'s `OLLAMA_MODEL`).
  - CPU-only inference (no GPU on the dev machine — 12 cores, 125GB RAM, integrated Intel graphics only) is slow either way: roughly 30-90s per turn.
  - The hosted Claude backend (`AnthropicNarrator`) shares the identical engine/tool-loop code path but hasn't been run live yet — still pending account credits.
- **Prompt tweak**: both `DM_SYSTEM_PROMPT` (`server/narrator.py`) and `OLLAMA_SYSTEM_PROMPT` (`server/narrator_ollama.py`) now explicitly say narration should end in open-ended prose, never a numbered/bulleted list of options — closes the turn-5 menu quirk noted above.
  - **Re-verified live** against `qwen2.5:7b`, two more turns (new session — no prior `sessions/` state existed on this machine — same character "Torvin Ironheart", reconstructed at HP 9/10 to continue the fiction): turn "swing my longsword at the bandit blocking the doorway" and turn "glance around the room to see what else is here." Neither ended in a numbered/bulleted list — the tweak holds so far, though the prior quirk was already rare (1 of 5 turns), so 2 more clean turns is weak-but-positive evidence, not proof.
  - Neither turn actually exercised `update_character`, and correctly so on inspection, not a miss: turn 1's damage landed on the bandit (an NPC), not on Torvin, and this engine only tracks the acting player's own sheet — no HP delta was owed. So this pair adds two more turns to the "no call needed, no call made" column, not a new positive-call data point; item below is still open.
  - Minor style oddity, not a correctness bug: turn 1's narration ended with a bare "Update?" rather than inviting the next action — worth watching for recurrence, not urgent enough to act on from a single instance.
- **NPC/monster state tracking.** Closes the exact gap the "Re-verified live" entry above flagged as still open: previously `update_character` only ever touched the acting player's own sheet, so a wounded NPC's HP existed only in that turn's prose — nothing stopped the DM from narrating a goblin at 3 HP one turn and full HP the next, since there was nothing to check against. `update_character`'s input gained an optional `target` field: omitted (or `"self"`) is the unchanged existing behavior; a name is an NPC — the first call for a given name creates a tracked sheet in the new `Session.npcs` dict (reusing `CharacterSheet` as-is, no new model), defaulting `max_hp` to 10 (the same fallback `join_session` already uses for a fresh player) if the DM doesn't pass a real value from `lookup_rule`; every later call with that same name updates the same tracked NPC. A new `npc_update` envelope (`{name, sheet_delta}`) broadcasts to *everyone* on a real change — deliberately not routed privately like `character_update`, since an NPC's wounds are shared observable fiction, not one player's private sheet. `state_sync` now includes `npcs` too, so a (re)joining player sees current NPC state immediately. The Textual client renders NPC status as a dim log line (`goblin: HP 3/7`) rather than a new dedicated panel — a deliberately small UI footprint for a first pass, revisit if it turns out to need more. 8 new engine-level tests (target dispatch, introduction vs. update, no-op-doesn't-rebroadcast, persistence across turns, `state_sync` inclusion); one real gap caught immediately by running them: `shared/protocol.py`'s `Envelope.type` is a strict pydantic `Literal` that didn't yet include `"npc_update"` — every construction raised a `ValidationError` until it was added alongside `character_update`.
- **DM-mediated dice rolls (`request_roll`), Anthropic-only for now (2026-08-07).** Previously `/roll` was entirely player-initiated and completely disconnected from the DM — the narrator never requested a roll, never set a DC, and never adjudicated success/failure from one; narration and mechanics were decided independently of anything actually rolled. A new `request_roll` tool (dice notation, optional `dc`, optional `reason`) lets the DM ask for a roll before narrating an uncertain outcome, get back a real result (plus a success/failure verdict if a `dc` was given), and narrate to match it rather than deciding the outcome first and rolling to match after the fact. `GameEngine._on_player_action` gained a `request_roll` closure (mirroring `apply_update`'s existing pattern) that rolls via the existing `dice.roll()`, tracks what was rolled, and broadcasts both a visible `dice`-kind `log_entry` and a `dice_result` envelope after narration completes — reusing and extending (not duplicating) the exact same broadcast shape `_on_dice_roll` already used for manual `/roll`, via two new shared helper methods (`_dice_log_text`, `_dice_result_envelope`). **Deliberately scoped to `AnthropicNarrator` only**: `REQUEST_ROLL_TOOL` is in its tool list, but *not* in `OllamaNarrator`'s `OLLAMA_TOOLS` — this session's investigation (see item 6) found `qwen2.5:7b`/`llama3.1:8b` already miss the one existing required tool call on most clearly-warranted turns, so adding a second required tool-call step before narration would only compound that, not help. `OllamaNarrator.narrate()` accepts the `request_roll` parameter for interface parity (`NarratorBackend` Protocol) but never wires it to a real tool, so local models cannot call it at all — not just "less reliable at it." 9 new tests across `test_narrator.py`, `test_narrator_ollama.py`, and `test_engine.py` (tool dispatch, the deliberate Ollama exclusion, dc/no-dc broadcast shape, invalid-notation handling). **Not yet verified live** — `AnthropicNarrator` as a whole is still only structurally tested against mocked responses, same gap as item 2 below; this adds to what that live verification needs to cover, it doesn't close it.
- **Immersion pass: persistent world state, quest log, NPC memory, opening hook (2026-08-07).** Motivated by open research into comparable AI-GM projects and papers this session (function-calling AI GMs, solo-RPG agentic-vs-static comparisons, the closest open-source sibling project `open-tabletop-gm`) — the converging finding was that structured, *persistent* world/plot state, not just character state, is what makes a session feel like a story worth following rather than a chatbot answering one prompt at a time. Before this, `WorldState` existed but was nearly inert (a free-text `summary`, unused `flags`), `CharacterSheet.notes` existed but nothing ever set it, and nothing happened on session start beyond silence until the player acted.
  - **`update_world` tool, Claude-only (same reasoning as `request_roll` above).** `WorldState` gained a real `apply_update()` (mirroring `CharacterSheet.apply_update()`'s exact pattern) and a new `objectives: list[Objective]` field (`Objective` = `{text, status: "active"|"completed"}`). The tool supports `location`, `summary` (a standing 1-3 sentence campaign summary, not per-turn detail), `add_objective`/`complete_objective`/`remove_objective` (matched by exact text), and `set_flag`/`clear_flag`. Broadcasts a new `world_update` envelope (broadcast, not private - shared fiction like `npc_update`) on a real change. Not in `OLLAMA_TOOLS`, for the same reliability reasoning as `request_roll`.
  - **NPC memory via `CharacterSheet.notes`, both backends.** The `notes` field already existed on the model but nothing ever set it. `UPDATE_CHARACTER_TOOL`'s schema (shared by both backends - not Claude-gated, since it's the *existing* tool, not a new one) gained a `notes` parameter, and `CharacterSheet.apply_update()` now handles it (overwrite-if-changed, same pattern as the other fields). Both system prompts now tell the DM to give a new NPC a brief note on introduction (personality/goal/relationship) and update it if the relationship changes, so a recurring character feels continuous instead of reset each time they reappear.
  - **An opening scene on genuine campaign start, both backends.** `_on_join_session` now detects a real campaign start (a brand-new character *and* an empty session log - not just a later player joining an in-progress one) and narrates a short opening scene before the first `turn_prompt`, instead of leaving the player facing silence until they act first. Reuses the exact same narration/tool-wiring path as a real turn (via a new shared `_narrate_and_apply(character, action_text)` helper factored out of `_on_player_action`), so the DM can use `update_world`/`lookup_rule` to set an initial location or objective right at the start, exactly like any other turn. Doesn't consume the turn queue (no `advance_turn()`); best-effort - a failure sends a warning `system_message` rather than blocking the join. Gated behind a new `GameEngine(..., enable_opening_scene=True)` constructor flag (default on) specifically so the reliability harness's fixed 8-turn scenario (which expects each turn to map to exactly one `narrate()` call) and the large majority of pre-existing engine tests, which relied on `join()` having no narration side effect, didn't all need rewriting - only the tests and harness scenario that predate this feature explicitly opt out.
  - **Client**: `CharacterSheetPanel` now also renders active objectives (below inventory/conditions, same list style), fed from `state_sync`'s already-present-but-previously-unused `world_state` payload and the new `world_update` envelope. No new panel/CSS - reuses the existing sheet panel, same "small footprint for a first pass" approach as the NPC status lines.
  - 15 new tests across `test_state.py`, `test_narrator.py`, `test_narrator_ollama.py`, and `test_engine.py` (`WorldState`/notes `apply_update()` behavior, tool dispatch, the deliberate Ollama exclusion, `world_update` broadcast shape, opening-scene firing-once/not-on-rejoin/failure-handling, the test helper's default-off regression guard). Client rendering reviewed by inspection only — no client-side test infrastructure exists in this repo to extend.
  - **Deliberately deferred**: background world-ticks (NPCs/factions advancing off-screen between sessions) and a relationship graph (who-knows-whom, surfaced by `open-tabletop-gm`'s design) — both real ideas from the same research, but bigger and more speculative; this pass builds the smallest slice that directly closes the "nothing persists, nothing happens on its own" gap first, matching this project's own "measure the smallest real slice" discipline throughout this session.
  - **Not yet verified live** — same `AnthropicNarrator` gap as `request_roll` above (no `ANTHROPIC_API_KEY` configured in the dev environment this was built in); structurally tested against mocks only. A live Ollama regression run confirmed the `_narrate_and_apply` refactor itself didn't change existing turn behavior.
- **Client crash on modern Textual, found while capturing README screenshots (2026-08-07).** `CharacterSheetPanel` (`client/app.py`) defined its own private helper method named `_render` — which silently shadows `Widget._render()`, an internal method Textual's layout engine calls on every widget to compute content height. The override returned `None` (it just called `self.update(...)` as a side effect) instead of a `Visual`, so any layout pass touching the sheet panel before its first `update()` call — which is every app startup, since the panel is empty until the first `state_sync` — crashed with `AttributeError: 'NoneType' object has no attribute 'get_height'`. Confirmed via a minimal repro (a bare `Static` subclass with only a same-named `_render` method reproduces it in isolation) that this is a real name collision, not a version-support gap: the currently-installed Textual (8.2.8, well past this project's `textual>=0.58` floor) calls `self._render()` from `get_content_height()`; older Textual apparently didn't hit this exact path the same way, which is presumably why it went unnoticed in day-to-day use. Fixed by renaming the helper to `_refresh_display`. All 88 tests still pass; verified live end-to-end afterward via a real headless session against `qwen2.5:7b` (see Screenshots in the README). Worth a `textual` upper/lower bound revisit given a patch-level-invisible framework change was able to crash the app outright.
- **Silent persistence failure discovered on a long-running dev server, startup writability check added (2026-08-07).** A `server.main` process that had been running continuously since 2026-08-06 (pre-dating this repo's move from `~/Documents/ai-dungeon-master` to its current `~/Projects/github/oracle` location) was still alive with its `cwd` pointed at the now-deleted old directory - Linux keeps a process's cwd handle valid even after the directory itself is unlinked, so the process kept running normally, but `SESSION_STORE_DIR`'s default relative path (`"sessions"`) resolves against that dead cwd. Every `_save()` since has been raising `FileNotFoundError: 'sessions/default.json'` with nothing surfaced to the player - a live, real character's entire session (multiple turns of narration, HP loss, an NPC encounter) existed only in that process's memory, one restart away from total loss, discovered only by directly reading the server's own log for a traceback. **Fixed the class of failure this represents, not just this one incident**: `JSONFileSessionStore.__init__` (`server/persistence.py`) now does a real write-and-delete probe against its directory, not just `mkdir(parents=True, exist_ok=True)` (which is a silent no-op against an already-existing-but-unwritable directory, and wouldn't itself have caught this incident's actual failure mode either, since `sessions/` had genuinely existed under the old path at some point). Raises a new `SessionStoreUnwritable` with a clear, actionable message; `server/main.py` catches it and exits with a one-line logged error instead of a raw traceback. 2 new tests (`tests/test_persistence.py`): a read-only existing directory, and a read-only parent blocking directory creation. **Known limitation, not solved by this fix**: a startup check only catches a *misconfigured* store directory - it does nothing for this incident's actual shape, where the directory was valid at startup and disappeared later, mid-process-life. That would need every `_save()` call site to handle a save failure as a visible, non-fatal `system_message` (matching the pattern `_on_player_action`'s narration-failure handling already uses) instead of letting it propagate into an unhandled exception that silently kills that connection - not yet done; flagged as a real follow-up below, not assumed closed by this entry.
- **A related, unexplained client crash observed live in that same session, cause not confirmed.** While driving a few manual turns against that same long-running (pre-fix) server to check for issues, the interactive Textual client process - and, since it was tmux's only remaining pane, the whole tmux server with it - exited within seconds of a `/roll 1d20` command being sent. `dmesg`/`journalctl` showed no OOM-kill or crash signature, just systemd logging the pane's scope ending normally; no core dump, and the pane's own stderr died with it, so there's no traceback to inspect. Timing is suspicious but not proof - noted here rather than either ignored or overclaimed as a confirmed `/roll`-handling bug. The game server process itself (holding the only in-memory copy of that session, given the persistence bug above) survived undisturbed.
- **`.env` was never actually being read - the entire README setup flow silently did nothing, found while confirming a clean session works end-to-end (2026-08-07).** Every config read in this codebase (`DM_BACKEND`, `ANTHROPIC_API_KEY`, `OLLAMA_MODEL`, `SESSION_ID`, ...) is a plain `os.environ.get(...)`; nothing anywhere called `load_dotenv()` or equivalent, despite the README's own setup instructions being "`cp .env.example .env`, then fill in the lines that matter." A freshly-created `.env` with `DM_BACKEND=ollama` had zero effect - `create_narrator()`'s `os.environ.get("DM_BACKEND", "anthropic")` fell through to the hardcoded `"anthropic"` default every time, and with no `ANTHROPIC_API_KEY` ever exported either, every single narration call (including the opening scene) failed immediately with `TypeError: Could not resolve authentication method`. This had been silently swallowed by the same missing-exception-logging gap noted just above (the very fix that let this be diagnosed at all, on the first real attempt after adding it) - without that log line, this would have looked identical to the qwen2.5:7b flakiness this project has spent an entire investigation on, and very nearly got misdiagnosed as exactly that before the traceback showed a completely unrelated Anthropic SDK auth path being hit instead of Ollama. **Fixed**: added `python-dotenv` (`pyproject.toml`), `server/main.py` now calls `load_dotenv()` as the first line of `main()`, before anything reads `os.environ` - additive only, a real `export`ed value still wins over `.env`. Verified live end-to-end afterward: fresh session, `DM_BACKEND=ollama` from `.env` alone (no exports), opening scene generated correctly via Ollama, `sessions/default.json` saved with real log/history content. All 94 tests still pass. Worth noting this bug is presumably not new - it's very plausibly been present since `.env.example` itself was first added, silently defeating that entire section of the README for however long, until this session's own exception-logging fix (immediately) and manual live testing (immediately after) happened to catch it together.
- **Live smoke-testing on the newly GPU-verified machine (see GPU migration readiness, below) reproduced a fresh instance of the known leaked-pseudo-tool-call failure and caught a genuine gap in the missed-change heuristic meant to guard against it (2026-08-07).** Driving a handful of manual turns against `qwen2.5:7b` on the GPU: a combat turn ("I draw my sword and attack the shadowy figure") narrated a `add_condition: "frozen"` outcome ("chilling your skin", "a numbing cold spreading") but printed it as a literal JSON blob in the narration text (`Update your sheet: {"add_condition": "frozen", ...}`) instead of making a real `update_character` call - confirmed via the server log showing only **one** `httpx` request for that turn, where a genuine tool-call round trip needs two, the same diagnostic this project has used throughout. The opening scene on this same run also printed a paraphrased instruction-like preamble ("Update your sheet to reflect your current state:") before otherwise-normal narration - not verbatim from any prompt text, a hallucinated stage direction rather than a copied string, matching the "leaked raw internal instruction text" category already noted elsewhere in this file as a known detection gap. **A real, fixable bug found alongside this**: `POSSIBLE_UNTRACKED_CHANGE_PATTERN`'s own comment states its intent as "damage/death/condition language," but the actual regex had zero condition-related keywords - so this exact turn's clearly-condition-changing narration never tripped the missed-change warning it exists to provide. **Fixed**: added `condition`, `poison(ed/ing)`, `stun(s/ned/ning)`, `paraly(zed/zing/sis)`, `frozen`, `freez(e/es/ing)`, `chill(s/ed/ing)`, `numb(s/ed/ing)`, `blind(ed/ing)`, `burn(s/ed/ing)`, `prone`, `restrained` to the pattern (`server/engine.py`). 1 new regression test (`tests/test_engine.py`) reconstructing this exact scenario's narration text. All 95 tests pass. The underlying leaked-JSON/tool-avoidance behavior itself is unchanged - this closes a real gap in the *detection* heuristic, not the model's own reliability, which remains item 6's open investigation. Separately confirmed clean during the same session: `/roll`, `/chat`, and a genuinely non-mechanical action (opening a door) all behaved correctly with no false-positive warning.

- **Real character creation instead of every fresh character starting blank (2026-08-07), prompted directly by owner feedback after playing the GPU-backed session above.** The gap: a new character was created with just `name`, `hp: 10`, `max_hp: 10` and nothing else - `stats`/`inventory` are real fields on `CharacterSheet` but stayed empty until the DM's `update_character` tool happened to populate them mid-narration, which this project's entire reliability investigation (above) has shown is unreliable. The owner's own words: *"i did not see character sheet for my character information"* - a real, first-hand confirmation of the gap, not a hypothetical one. **Fixed with a deliberately small, deterministic slice** - not a full 5e character builder: `join_session`'s payload gained an optional `character_class` (`fighter`/`wizard`/`rogue`/`cleric`, the four classes already in the SRD dataset, `server/rules/srd.json`). A new `build_starting_character()` (`server/engine.py`) reads that class's real `hit_die` via a new structured `RulesIndex.get_entry()` (`server/rules/__init__.py`, refactored out of the existing JSON-string `lookup()` so both share one code path instead of duplicating the slug/category logic) for starting HP, and a small `CLASS_STARTING_EQUIPMENT` mapping for a starting item or two drawn from the SRD's existing (limited, CC-BY-4.0) equipment list. Blank or unrecognized class falls back to the exact original hp=10/max_hp=10 empty sheet, so old clients/tests that never send `character_class` keep working unchanged - confirmed by all 95 pre-existing tests passing without modification. `CharacterSheet` gained a `character_class` field; the client prompt (`client/main.py`) only asks for a class on a genuinely new character (skipped on reconnect, since an existing character keeps what it already has), and `CharacterSheetPanel` (`client/app.py`) now shows it next to the name. 8 new tests (`tests/test_engine.py`): all four classes' HP/inventory, blank/unknown-class fallback, and one engine-level join integration test. Verified live end-to-end: a fresh "Thorne (Fighter)" joined with real HP 10/10 and `Longsword`/`Leather Armor` already in inventory, no LLM call involved in producing it, persisted correctly to `sessions/default.json`.
  - **Deliberately left out of this pass, not forgotten** - see the checklist under Later, below: ability scores/a real stat block (no CON modifier system exists yet, so HP is just the hit die's max value, not 5e's actual hit-die-plus-CON-mod formula), the other product-feedback items from the same conversation (a main menu, other players' sheets being visible, a "DM is thinking" status indicator).
- **"DM is thinking" status indicator (2026-08-07), second item off the same owner-feedback checklist.** The gap: the input cleared on submit and nothing visibly happened until narration started streaming in - on `qwen3:8b`'s slower runs or CPU-only inference (30-90s/turn throughout this file) that's a long silent wait with zero feedback that anything is happening at all. `DungeonMasterApp` (`client/app.py`) gained a small `Static(id="status")` line between the log/sheet row and the input bar, toggled via a new `_set_thinking()` - shown the moment a real `player_action` is sent (not `/roll`/`/chat`, which are handled instantly server-side with no LLM in the loop at all, so there's nothing to wait on), cleared the moment the first real narration chunk arrives *or* a `system_message` lands (covers the narration-failed path, which never reaches a `log_entry` at all). Verified live against local CPU-only Ollama, where the gap is longest and most visible: "The DM is thinking..." appeared immediately on submit and stayed up through the full generation delay, clearing cleanly the instant streamed text began. No new tests - this project has no client-side test infrastructure (a standing gap noted elsewhere in this file); verified by direct observation instead, same as every other client-rendering change so far.
- **Case-insensitive NPC name matching (2026-08-08), closing the "Remaining candidates" item flagged under item 6 below.** `Session.npcs` was keyed by the exact raw `target` string the DM's `update_character` call sent; a name that came back re-cased on a later turn (`"Bandit"` after an earlier `"bandit"` - exactly what a live `qwen2.5:7b` split-experiment run actually did, see item 6) would silently create a second, disconnected NPC entry rather than updating the one already tracked, rather than a hypothetical. **Fixed in `GameEngine._narrate_and_apply`'s `apply_update` closure (`server/engine.py`)**: NPC lookup/storage now keys off `target.casefold()` (`npc_key`), while the `CharacterSheet.name` set at introduction keeps the original first-seen casing - so a re-cased later call updates the existing tracked NPC instead of duplicating it, and the tool result / `npc_update` broadcasts stay consistent under whichever casing introduced the NPC, not whatever casing a later call happens to use. `_state_sync_envelope`'s `npcs` payload was keyed the same way (raw dict iteration) and got the same fix - now keyed by `npc.name` rather than the (now-casefolded) internal dict key, so a reconnecting client's status lines match what live `npc_update` broadcasts already show. 2 new tests (`tests/test_engine.py`): a re-cased target on a later turn updates the existing NPC rather than duplicating it, and `npc_update` broadcasts keep the first-seen name across a recase. Full suite green (105 tests; the 2 pre-existing `test_persistence.py` `chmod`-based unwritable-directory failures are a root-user sandbox artifact - `chmod 0o500` doesn't block root - confirmed unrelated by reproducing them against an unmodified checkout).
- **Other players' presence and sheets in multiplayer (2026-08-08), third item off the owner-feedback checklist.** The gap, in the owner's own words: *"i assume when others join they will be on the left column as well"* - not true before this. `CharacterSheetPanel` only ever rendered `characters.get(self._player_id)`, your own sheet; nobody else's presence showed up anywhere beyond the shared `"X joined the session"` log line. **Design question resolved as name+HP-only, not full sheets** - matches the "others shouldn't see your inventory" expectation the owner already confirmed correct for `character_update`'s private-only routing (see docs/protocol.md), and keeps the state actually synced small. New `_public_character_view()` (`server/engine.py`) is the one place defining what's public - `player_id`, `name`, `character_class`, `hp`/`max_hp`, `conditions` - never `inventory`, `stats`, or `notes`. Three places now use it: `state_sync`'s `characters[]` (the recipient's own entry is a full sheet, everyone else's is the redacted view - previously *every* entry was a full sheet, a real pre-existing leak this closes as a side effect, not just cosmetic client-side hiding), a new `player_joined` broadcast (fired on every join, including a reconnect, alongside the existing `"X joined the session"` system message), and a new `player_update` broadcast (fired alongside every private `character_update`, so another player's HP/conditions in your presence view stay live turn to turn, not just accurate at join time). `player_left` (protocol-defined but never emitted, per docs/protocol.md's own prior note) now actually fires too - a new `GameEngine.handle_disconnect(player_id)`, called from `Transport._handler`'s existing `finally` block (`server/transport.py`) when a socket closes, since a disconnect isn't a client-sent event the normal `handle()`/envelope dispatch can see. `player_update` is a new `EventType` (`shared/protocol.py`); `player_joined`/`player_left` already existed in the schema, just unused until now.
  - **Client** (`client/app.py`): `CharacterSheetPanel` gained `render_others()` and a "Party" section (see the design-pass entry just below for the naming/layout choice) appended after Objectives, in the same left-column panel the owner's own words asked for - not a new widget, matching this project's established "small footprint for a first pass" convention (the same reasoning NPCs' dim log line and the sheet's existing Objectives section both already followed). `DungeonMasterApp` tracks `self._others: dict[player_id, public_view]`, populated from `state_sync` and kept live via `player_joined`/`player_update`/`player_left` branches in `_handle()`.
  - **Verified for real, not just mocked engine tests** - this is the first genuinely multi-connection feature in the project, so a real 2-connection test was worth the extra step: `tests/test_transport_e2e.py` gained a second test that starts a real `Transport` on a real socket, connects two independent real `websockets` clients, and confirms end to end that the second player's real `state_sync` contains the first player's redacted view (no `inventory` key reaches the wire), that a live `player_joined` broadcast for the second player reaches the first player's still-open connection, and that closing the first player's real socket produces a real `player_left` broadcast on the second. 6 new engine-level tests (`tests/test_engine.py`) cover the same behavior at the unit level (public-view-only `player_joined` payload, redacted vs. full `state_sync` entries, `player_update` alongside `character_update`, `handle_disconnect` broadcasting `player_left`, including the defensive never-joined case). The client widget has no test infrastructure (a standing gap this project has noted before) - verified by direct inspection instead: instantiating `CharacterSheetPanel` and calling `render_sheet()`/`render_others()` directly confirmed the rendered text shows the owner's own full sheet plus a distinct "Other Players" section (`- Rowan (Rogue): HP 5/8 (prone)`) with no inventory field anywhere in it. Full suite green (111 tests; same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
  - **What this doesn't close**: a real turn-taking session between two real players (each submitting `player_action`s, seeing narration and each other's state changes) has still never been run - this slice verifies connection/presence/disconnect handling for real, not gameplay itself. See the "Multiplayer" item under Later, updated to reflect this narrower remaining gap.
- **Character sheet panel design pass, referencing how D&D Beyond and Roll20 actually lay theirs out (2026-08-08)** - the panel had never been designed against any real reference before this; it was just a flat list that grew one section at a time as features shipped. Researched both rather than guessing from memory: D&D Beyond's own support docs and independent write-ups confirm its sheet groups Armor Class/Initiative/Speed/HP into one core combat-stat block, visually set apart from ability scores/equipment further down; Roll20's D&D 5E sheet separates a "Core Page" (the everyday-use stats) from Bio/Spells/Settings pages. **Only the *shape* carried over, not fabricated fields** - at the time of this entry, Oracle had no AC, Initiative, Speed, or ability scores yet, so this didn't invent placeholder values for them; it applied the same "identity + vitals header, visually separated from everything else" structure to the fields Oracle actually had (name, class, HP). Ability scores have since been built (see the "Real ability scores" entry, below) and the sheet panel now shows them - AC/Initiative/Speed remain the still-missing fields.
  - `CharacterSheetPanel._refresh_display()` (`client/app.py`) now renders a header block (name/class + a new HP line) followed by a dim `─` divider before Inventory/Conditions/Objectives, and a second divider before the Party section - the same "your own vitals up top, set apart from the rest" grouping the research confirmed, adapted to a single narrow text column instead of D&D Beyond's boxed multi-column layout.
  - **HP gained a text bar** (`_hp_bar()`) - block characters (`█`/`░`) colored green/yellow/red by HP fraction, the same at-a-glance token-health-bar convention virtually every VTT uses (Roll20's colored token bars specifically), adapted to plain terminal text since there's no image/token rendering here. The main sheet gets a full-width (10-char) bar; each Party row gets a shorter (6-char) one - deliberately less detail for "everyone else, at a glance" than for your own character, matching the same "your own sheet is the deep view, the party list is the quick-glance view" split Roll20's turn-order tracker and D&D Beyond's Campaign dashboard both make relative to a full character sheet.
  - **Renamed "Other Players" → "Party"** - not a functional change, but real copy research: neither D&D Beyond nor Roll20 has a section called "Other Players"; the genre-standard term for "everyone else in this scene/session" is "Party" (D&D Beyond's Campaign dashboard, Roll20's turn-order tracker both use it), so this replaces an engineering-flavored label with genre-appropriate copy.
  - **Deliberately not done at the time of this entry**: an ability-score grid, saving throws, skills, AC/Initiative/Speed, or death saves - none of these existed in `CharacterSheet` yet, and adding placeholder UI for data the engine didn't track would have misrepresented what the character sheet actually knew. Ability scores were built the same day (see the "Real ability scores" entry, below) and did land in exactly this panel, as anticipated here; saving throws/skills/AC/Initiative/Speed/death saves remain not built.
  - **Verified by direct inspection**, same as every other client-rendering change in this project (no client-side test infrastructure exists): instantiated `CharacterSheetPanel` directly and confirmed the rendered output for a damaged character (`HP 7/10` green bar), a downed one (`HP 0/10` red, empty bar), and a party with a critically low member (`HP 2/8` red bar) all rendered correctly, including the divider placement and the renamed "Party" header. No server-side files touched, so the existing 109-passing/2-pre-existing-failure test count is unchanged.
- **A pre-game main menu (2026-08-08), closing the last item on the owner-feedback checklist - the one explicitly flagged as this project's broadest-scope open request.** Owner's own framing, kept close to verbatim since the exact shape mattered: *"should there be a main menu first where players can join, chat, create or load their character and or review their character... then when start dm begins narrating the scene... maybe begin with players introducing themselves."*
  - **Three design questions resolved rather than guessed at, each logged here since none had an obviously-right answer**: (1) who can trigger the actual start - resolved as *any* joined player, not a separate host/GM role, matching every other symmetric mechanic in this protocol (turn order itself has no privileged player). (2) whether solo play should skip the lobby - resolved as no, it always shows, for one consistent flow rather than two divergent ones; an extra click for a solo player is a small, deliberate cost. (3) what "load a character" means - resolved as the existing automatic single-identity-per-machine rejoin (`.player_id`), not a new multi-character picker - a materially bigger, un-asked-for feature the owner's phrasing didn't actually require once read literally (reviewing an existing character is a real lobby feature now; picking between several saved ones on one machine is not).
  - **Protocol** (`shared/protocol.py`, `docs/protocol.md`): two new envelope types, `start_session` (client→server, empty payload, idempotent) and `session_started` (server→broadcast, empty payload, the lobby→session transition signal). `state_sync` gained a `started` field (`GameEngine._has_started()`) so a (re)joining client can route itself correctly without inferring from `log_tail`.
  - **Engine** (`server/engine.py`): `_on_join_session` no longer triggers the opening scene or broadcasts `turn_prompt` unconditionally - narration and turn visibility now wait for an explicit `_on_start_session`, and `turn_prompt` on join is gated on `_has_started()` (so a genuine reconnect into an already-started game still sees whose turn it is, but a fresh join lands in the lobby). `Session` gained a `started: bool` field - but `_has_started()` treats `started or bool(log)` as the real signal, not the field alone, specifically so a session saved before this field existed (this project's own real `sessions/*.json`, among others) isn't incorrectly dropped back into a pre-game lobby it already left long ago. A multi-player `start_session` builds a roster-aware opening-scene prompt (everyone present, by name/class, with a nudge toward introductions if more than one) rather than a new multi-actor tool-routing mechanism - `character_summary`/`apply_update` still anchor on one character, exactly like any other turn; the owner's own "maybe" already signaled this should be a prompt nudge, not a hard mechanical requirement.
  - **A real ordering bug caught before it ever shipped, by tracing the actual broadcast sequence, not by running anything yet.** The first version broadcast `session_started` *after* the opening scene's narration completed - but `_narrate_and_apply` streams `log_entry`/`npc_update` broadcasts *during* narration, and a client still on the lobby screen has no session-view widget to render them into. Fixed by broadcasting `session_started` first, so every client transitions into the real session view before narration starts streaming - the same experience a normal turn's narration already gives, not a special case.
  - **Client** (`client/app.py`, `client/main.py`): the client's blocking `input()` prompts (name/session ID/class), which can't run inside a live Textual event loop, are gone - `client/main.py` now only resolves local player identity (the `.player_id` file) before launching the app. Three real `Screen`s replace the old single-view `App`: `WelcomeScreen` (the old prompts, as real widgets), `LobbyScreen` (your own sheet for review, a live "Party" list, lobby-only chat, "Start Adventure"), and `SessionScreen` (the pre-existing sheet+log+input view, moved unchanged). `DungeonMasterApp` holds session state as plain attributes and routes incoming envelopes to whichever screen is active - the same state-on-the-App, screens-read-`self.app.*` pattern this workspace's other TUI projects already establish.
  - **Two more real bugs, both found only by actually running the app against a real server, not by reading the code back:**
    1. **`App.query_one()` doesn't track screen transitions at all** - confirmed by reading Textual's own `App._on_compose`/`default_screen` source after the failure, not assumed: `_compose_screen` gets pinned to whatever screen existed the moment `App.compose()` first ran and never updates again, so `self.query_one(...)` called from the App instance silently searches a screen that's no longer the active one for the entire remaining life of the app. Every App-level widget lookup (`refresh_sheet_widgets()`, the lobby chat/status writes) had this bug. Fixed by querying `self.screen.query_one(...)` (the real top of the screen stack) instead - the correct, working pattern for anything but the very first screen.
    2. **A real mount race**, caught by the one true end-to-end test in this slice (see below), not by any mocked-transport test: `push_screen()`/`switch_screen()` schedule mounting the new screen's widgets asynchronously, and without awaiting that, the very next queued envelope - routinely `_on_join_session`'s own "X joined the session" `system_message`, broadcast immediately after `state_sync` - could already be dispatched and try to query a widget (`#lobby-status`) that didn't exist in the DOM yet, crashing with `NoMatches` inside the listener task. Fixed at the root, not papered over: `_handle()` is now `async` and genuinely `await`s `push_screen`/`switch_screen`, and `_listen()` awaits each `_handle()` call in turn rather than firing them off unawaited - by the time the next envelope is processed, the previous screen transition has actually finished mounting.
  - **Verified with a real client against a real server over an actual websocket, not just mocked-transport unit tests on either side** - the first test in this project to do so. `tests/test_transport_e2e.py` gained `test_real_client_lobby_to_session_flow_over_real_websocket`: a real `Transport`/`GameEngine` (a stub narrator standing in for a live LLM - this environment has no Ollama/Anthropic access, the same honest limitation this project's GPU-migration/Claude-verification entries already note) and a real `DungeonMasterApp` (real `ClientTransport`, no mocking) driven through the actual welcome→lobby→start→live-narration flow via Textual's `Pilot`. This is the test that caught both bugs above - neither would have shown up testing the client or server in isolation. 9 more tests in the new `tests/test_client_app.py` (screen routing for new-vs-returning/fresh-vs-reconnecting players, the Start button, lobby chat landing in the right log, party updates rendering without inventory) using a `FakeTransport` for faster, network-free coverage of the same logic. 8 new engine-level tests (`tests/test_engine.py`) replace the old join-time opening-scene tests, covering the new `start_session` trigger directly: fires once, is idempotent, mentions every present player when more than one has joined, still transitions the session even with narration disabled or failing, and a reconnect after start correctly sees `turn_prompt` again. Full suite: 123 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, confirmed unrelated as before).
  - **Not yet done, honestly scoped out rather than silently dropped**: real multiplayer turn-taking (two actual players trading `player_action`s through a live session) still hasn't been run - this slice verifies the lobby/start/presence machinery for real, not gameplay itself; see the "Multiplayer" item under Later. New screenshots for the welcome/lobby screens weren't captured - regenerating requires a real live narration session (no LLM backend available in this environment), same limitation noted throughout this file; the existing `docs/screenshots/*.svg` still accurately depict `SessionScreen`, which is functionally unchanged from before this slice.
- **Real multiplayer turn-taking, verified for the first time (2026-08-08), closing the exact gap the entry just above flagged as deliberately not yet done.** Two real `DungeonMasterApp`s (real `ClientTransport`s, no mocking) driven through one real `Transport`/`GameEngine` server over an actual websocket, each submitting real `player_action`s and watching the other's - the mechanics themselves (`turn_order`, `current_turn`, out-of-turn rejection) were already unit-tested in isolation, but never actually exercised end to end with two live clients trading turns.
  - **The core mechanics held up with zero engine changes needed** - out-of-turn rejection, action/narration broadcast reaching both clients, and turn advancement all worked correctly the first time this was run for real. Confirms `_on_player_action`'s existing `current_turn` check and the broadcast-vs-private-send split (already covered by unit tests) generalizes correctly to a real second connection, not just a mocked one.
  - **A real gap found by watching two real clients side by side, not by reading the code**: `turn_prompt` broadcasts to *everyone*, but the client only ever wrote something to the log for whoever's turn it now was - anyone else got no indication at all of whose turn it was, not even at session start. Concretely: Rowan's screen showed nothing when the session started on Thrain's turn, and after Thrain acted, Thrain's own screen gave no sign the turn had passed to Rowan - only Rowan's did. Not a crash, just silence, exactly the kind of gap that's invisible reading code turn-by-turn for a single player but immediately obvious watching two real screens side by side.
  - **Fixed in `client/app.py`'s `_handle()`, needing no new protocol field.** Every other player's public view (name included) is already tracked in `self.others`, populated from `state_sync`/`player_joined` - the `turn_prompt` branch now writes `"Your turn."` when the payload's `player_id` matches you, or `"<name>'s turn."` (looked up from `self.others`) otherwise, so every connected player always knows whose turn it is, not just the one player whose turn it currently is.
  - **Verified for real against the actual fix**, not just asserted: re-ran the same two-real-client scenario and confirmed both directions - Rowan's screen now reads `"Thrain's turn."` at session start (previously blank), and after Thrain's action resolves, Thrain's own screen now reads `"Rowan's turn."` while Rowan's reads `"Your turn."` - each player always sees the correct state, not just their own turn.
  - **New tests**: `tests/test_transport_e2e.py::test_two_real_clients_trade_turns_over_a_real_session` (two real clients, real out-of-turn rejection, real broadcast-to-both, real turn-passing, plus the whose-turn indicator on both sides) and `tests/test_client_app.py::test_turn_prompt_for_another_player_names_them_not_just_your_own_turn` (the same fix at the `FakeTransport` unit level, faster and network-free). Full suite: 125 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, confirmed unrelated as before).
  - **What this still doesn't cover**: 3+ simultaneous players (turn_order's cycling logic is generic over list length and structurally shouldn't behave differently, but hasn't been directly exercised past two), and real live narration content during a multiplayer turn (the stub narrator used here stands in for Ollama/Anthropic, same honest limitation as every other real-infra entry in this file - this environment has no live LLM access).
- **`scripts/compare_reliability_reports.py` (2026-08-08), closing item 6's "promote model comparisons into something reusable" candidate — half of it, honestly.** The candidate as originally written offered two shapes: a script that runs the harness across a list of models live, or one that diffs saved JSON reports directly. Attempted in an environment with **no live LLM access at all** - no Ollama installed/running, no `ANTHROPIC_API_KEY` - which rules out the live-list-of-models half entirely; only the report-diffing half is buildable and meaningfully testable here, so that's what shipped. Not silently narrowed - flagged in the script's own module docstring as a real, currently-open half.
  - **Reuses `run_stats()`/`aggregate_stats()` from `live_reliability_check.py` rather than reimplementing scoring** - the same "one place defines what 'correct' means" principle this workspace already applies elsewhere (Vulcan's `format_change()`, Atlas's `_trend_summary()`). `load_report()` normalizes both a single-run report and a `--repeat`-shaped one into one consistent stats view - a `--repeat` report's headline `rate` is the *pooled* rate (matching what item 6's own repeat experiments already treated as the number that matters), with `mean_rate` carried alongside since a flat pooled number can hide real per-run spread underneath it (the exact thing item 6's `qwen2.5:7b` repeat-5 entry already found: 29% pooled every time, but 0-2 real tool-call attempts per run underneath that flat number).
  - **`format_comparison()` prints one row per report** - model, backend, repeat count, rate, real-call and leaked-text counts - column widths computed from real content instead of a fixed layout, so a longer model name (`llama3-groq-tool-use:8b`, the longest one this project has actually used) doesn't get truncated or misalign the table.
  - **Verified two ways**: 6 new tests (`tests/test_compare_reliability_reports.py`) using the harness's own `ScriptedNarrator` fixtures (no live model needed for the tool's *own* correctness) covering single-run and repeat-shaped reports, leaked-text counting across runs, table formatting, an all-`n/a` report (no scored turns), and a real end-to-end `main()` invocation via `capsys`/`monkeypatch`. Separately smoke-tested for real outside pytest: generated two genuine JSON reports via `write_json`/`write_json_repeat`, then ran the actual `python -m scripts.compare_reliability_reports` CLI against both real files and confirmed a correctly aligned table with accurate figures - not just that the unit tests passed.
  - **Still open, honestly**: the live-list-of-models half of the original candidate, and therefore also re-running this tool against genuinely new saved reports from a live model (this environment has produced none) - both blocked on the same missing live-backend access already noted throughout this file's GPU-migration/Claude-verification entries.
- **Mid-session `_save()` failures made visible instead of an unhandled exception (2026-08-08)**, closing the known limitation the 2026-08-07 startup-writability-check entry explicitly left open at the time: that check catches a misconfigured `SESSION_STORE_DIR` at boot, but not the same directory vanishing later, mid-process-life - the exact incident logged that day (a repo moved out from under a long-running server process), which had silently killed the affected connection with nothing shown to the player.
  - **`GameEngine._save()` is now `async` and catches `OSError`** (not a bare `Exception` - narrowed deliberately to the realistic failure class here: a missing directory, a full disk, permissions changing mid-run, not a catch-all that would also mask a real bug in what's being serialized) around the underlying `SessionStore.save()` call, and sends the relevant player a `system_message` warning (`"Your progress may not be saving right now..."`) instead of letting the exception propagate uncaught into whichever `_on_*` handler called it. All 4 call sites (`_on_join_session`, `_on_start_session`, `_narrate_opening_scene`, `_on_player_action`) now `await self._save(<the relevant player_id>)` - join/start/turn all still complete normally even when the save itself fails, the same "report, don't block" pattern `_narrate_and_apply`'s own narration-failure handling already established.
  - **A real, separate test-suite staleness bug found and fixed while touching this file for something unrelated**: `test_missed_change_heuristic_does_not_fire_during_opening_scene` only ever called `join()`, never `start_session` - since the pre-game lobby slice decoupled the opening scene from `join_session` entirely, this test's assertion had been trivially true regardless of whether the false-positive guard it claims to verify actually worked, for as long as that slice has existed. Fixed by adding the missing `start_session` call, confirmed the test now genuinely exercises (and passes for the right reason on) the opening-scene code path.
  - **4 new tests** (`tests/test_engine.py`): a `FailingStore` reconstructing the real incident (`save()` raises `FileNotFoundError`, an `OSError` subclass) verifies join/start/turn-action all warn the right player and still complete normally (character created, `state_sync` sent, session genuinely started, turn genuinely advances) rather than silently dropping the connection; a fourth confirms the overwhelmingly common real path (`store=None`, every other test in this file) never warns about a "failure" that never happened. `docs/protocol.md`'s `system_message` row updated to describe this new use case, same discipline the missed-change heuristic's own row update already established. Full suite: 135 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
- **`dice_result` client rendering (2026-08-08)**, closing `docs/protocol.md`'s own former "Known client gap" note - the concrete payoff that note itself named was highlighting a natural max/min, previously impossible since the structured roll payload never reached client-side rendering logic at all.
  - **`server/dice.py`'s `roll()` now returns `sides` too** (`(total, rolls, sides)`, not just `(total, rolls)`) - it already parsed the die size internally and discarded it, and a client can't know what counts as "natural max" for an arbitrary die (a 6 on a d6 vs. a 20 on a d20) without that number, without duplicating this module's own notation parser. Threaded through both call sites (`_on_dice_roll`, the `request_roll` tool closure inside `_narrate_and_apply`) into the `dice_result` envelope's payload.
  - **The client now renders every roll from the structured `dice_result` envelope, not the plain-text `log_entry` it used to silently ignore** - `client/app.py`'s `_handle()` gained a real branch, plus a new `_dice_result_line()`/`_roller_name()` pair on `DungeonMasterApp` (needs `self.others`/`self.my_character` for the roller's display name, so these live on the App, not a Screen). The server still broadcasts both `log_entry(kind="dice")` and `dice_result` for every roll, unchanged (four existing engine-level tests already assert on the plain-text broadcast, not worth touching for this) - the client's own `log_entry` branch now explicitly skips the `"dice"` kind instead, so a roll renders exactly once, not twice.
  - **Highlighting generalizes beyond "natural 20"** to whatever die was actually rolled: any individual die landing on its own max face renders green-bold, landing on 1 renders red-bold - the same natural-max/natural-min convention, just not hardcoded to d20 specifically.
  - **Verified three ways**: `tests/test_dice.py` updated for the 3-tuple return with real `sides` assertions per test case (not just widened to compile); 3 new `tests/test_client_app.py` tests (a highlighted natural max, no duplicate line when both envelopes arrive, correctly naming another player's roll) using `FakeTransport`; and a real addition to the existing two-real-client end-to-end test (`tests/test_transport_e2e.py`) - a genuine `/roll 1d20 perception check` sent over an actual websocket from one real client, confirmed rendering on both real clients' screens exactly once each. Full suite: 138 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
- **D&D-style XP and leveling (2026-08-08) — the primary mechanical foundation asked for directly by the owner**, in their own words: *"I want the mechanics and game play style to be fleshed with a good foundation... essentially oracle is your tabletop emulator for you and your friends to freely go into and enjoy an rpg adventure story and character progression with,"* narrowed with a follow-up: *"D&D-style leveling/XP,"* and reinforced mid-design as the actual priority: *"i want noted that primary end result is having strong mechanics for oracle"* - character export/import (see below) is explicitly the secondary half of that same request, scoped down rather than allowed to expand this slice.
  - **Deterministic trigger, not a DM tool call.** This project's own tool-call reliability investigation (item 6 below) found local-model tool-call reliability plateaus around 29% no matter what's tried - a mechanic that depended on the model remembering to call a dedicated `award_xp` tool would inherit that same failure rate. Instead, `GameEngine`'s existing `apply_update` NPC-branch closure (`server/engine.py`) captures `was_alive = npc.hp > 0` right before applying an update and checks `npc.hp == 0` after - the exact same already-reliable HP-tracking machinery `update_character`/`npc_update` already depend on, not a new code path the model has to remember to invoke. An already-dead NPC (`was_alive` False) never re-triggers on a later hit, however many more times it's damaged afterward.
  - **XP value resolved in priority order** via a new `_xp_for_npc()` (`server/engine.py`): an explicit `xp` on the killing `update_character` call (mirrors the existing `max_hp` override precedent for introducing an NPC) beats an automatic lookup of the NPC's name against `server/rules/srd.json`'s own monster list (the same slug-based lookup `lookup_rule` already uses) and its Challenge Rating run through a new real SRD table (`leveling.xp_by_cr`, the SRD's actual Experience Points by Challenge Rating table, CR 0 through 30) - which beats a flat `DEFAULT_NPC_XP` fallback (50, CR 1/4's real value, the tier every monster in the SRD subset except the orc already sits at) for an NPC whose name matches nothing. This means killing an NPC the DM happened to name "goblin" already gets the real 50 XP automatically, with zero extra DM effort, since "goblin" resolves against the SRD entry that's already in this project's dataset.
  - **Level thresholds are the SRD's own real Character Advancement table**, not an invented curve - a new `leveling.xp_by_level` block in `server/rules/srd.json` (levels 1-20), read via a new `RulesIndex.xp_thresholds()`/`RulesIndex.xp_for_cr()` (`server/rules/__init__.py`). `ATTRIBUTION.md` updated to note these are included in full (bare numeric tables, no flavor text) rather than the hand-curated slice everything else in that file gets - deliberate, so leveling/XP math works correctly for any level or CR even before `monsters`/`classes` grow to match.
  - **`CharacterSheet.gain_xp()` (`server/state.py`)** is the pure mechanical half - increments `xp`, loops (not a single `if`) against the threshold table in case one large award crosses more than one level, returns how many levels were gained. Deliberately takes the threshold table as a parameter rather than reaching into `server.rules` itself, keeping `state.py` free of a rules-module dependency for one method - `server/engine.py` (which already holds a `RulesIndex`) calls it and separately applies HP growth (class hit-die max per level gained, reusing `_hit_die_max()` - the exact same no-ability-scores simplification `build_starting_character`'s own level-1 HP already uses; a character with no recognized class still levels, just without HP growth, a real documented gap rather than a fabricated number) right after, matching the existing "state.py owns bookkeeping, engine.py owns anything needing rules data" split.
  - **XP is split party-wide (real 5e's rule, landed 2026-08-19 - see item 21).** Originally shipped as "XP goes to whoever's turn it is, never split across the party" - a deliberate simplifying default, because Oracle's turn loop anchors every mechanical update (`apply_update`/`request_roll`/`update_world`) on a single acting character, and building a session-wide "who else is present and should share credit" notion was scoped out of the XP slice itself. Revisited once real multi-player sessions showed up: a new `_award_party_xp()` (`server/engine.py`) now divides a kill's XP evenly across every joined player (floor division, remainder dropped), a solo session is an exact no-op that awards everything as before, and the acting character keeps their own share like everyone else - still no DM tool call, the same deterministic trigger this entry already documents.
  - **Public/private split extended, not reinvented**: `level` joins `name`/`character_class`/`hp`/`max_hp`/`conditions` in `_public_character_view()` (`server/engine.py`) - a real fact about a character visible to the rest of the party, same as class or HP already are. Raw `xp` stays owner-only, the same boundary `inventory`/`stats`/`notes` already have - it's bookkeeping toward the next level, not a fact about the character itself. `docs/protocol.md` updated throughout (`player_joined`/`player_update`/`state_sync` payload rows, the "Private vs. shared state" section, a new "Character progression: XP and leveling" section documenting the design in full).
  - **Announcement via a broadcast `system_message`, not a new envelope type** - `"{name} defeats {npc} and gains {xp} XP!"`, with `"{name} reaches level {N}!"` appended on a level-up - matching how every other game-flow event that isn't itself DM narration (a join, an out-of-turn rejection) already reaches clients, so no protocol/client change was needed just to render it.
  - **Client** (`client/app.py`): `CharacterSheetPanel`'s header line gained `Lv N` next to the name/class, and an `XP: N` line (dim, own sheet only - `"xp" in character` is what gates it, since the redacted "Party" view never carries that key) right below the HP line. Party rows gained `Lv N` too, reusing the now-public `level` field. No new widget or CSS - same "small footprint" convention every earlier client change in this project has followed.
  - **19 new unit/integration tests**: `tests/test_state.py` gained 5 direct tests of `CharacterSheet.gain_xp()` (below threshold, crossing one level, crossing several at once, capping at the highest known level while `xp` itself keeps accumulating, a zero/negative amount being a no-op); `tests/test_engine.py` gained 14 (automatic CR-based XP on a known SRD monster, the `DEFAULT_NPC_XP` fallback on an unmatched name, an explicit `xp` override beating the CR lookup, no re-award on repeat hits to an already-dead NPC, the `system_message` announcement's exact text, HP growth by class hit-die on a real level-up plus its own broadcast, no HP growth with an unrecognized class, and the public view carrying `level` but never `xp`) - none of these needed a live LLM, since the trigger is deterministic engine state, not model behavior.
  - **Verified end-to-end over a real websocket too, not just at the engine-unit level** - `tests/test_transport_e2e.py` gained `test_defeating_an_npc_awards_xp_and_updates_the_sheet_panel_over_a_real_session`: a real `DungeonMasterApp`/`ClientTransport` (no mocking) driven through the actual welcome→lobby→start→turn flow against a real `Transport`/`GameEngine`, whose stub DM turn kills a real "goblin" NPC via a real `apply_update` call - confirms the real wire-format `system_message` ("gains 50 XP") reaches the client and that `CharacterSheetPanel`'s new `Lv`/`XP` lines render correctly from real server data, not just from directly-constructed test dicts. Full suite: 152 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
  - **Deliberately deferred, not forgotten**: character export/import (the secondary half of the same owner request - a local save-file format for taking a character's progression, inventory, and notes out of a session) was scoped down to keep this slice focused on "strong mechanics" as instructed; see the checklist under Later, below, for the concrete shape it's expected to take. Ability-score-driven HP growth (a real CON modifier instead of the flat hit-die-max this project's level-1 HP and now level-up HP both use) remains the same standing gap it always has been - see "Real open questions."
- **Character export/import (2026-08-08), the secondary half of the same request built out immediately after XP/leveling** - user's own follow-up: *"start on the character export/import piece next."* Built to exactly the shape scoped in the "Character progression checklist" (Later, below) when it was deliberately deferred out of the XP/leveling slice.
  - **Export is client-side only, zero protocol changes** - a new `DungeonMasterApp.export_character()` (`client/app.py`) writes `self.my_character` (already the exact private full-sheet dict `state_sync`/`character_update` deliver - xp/level/inventory/notes included, nothing new to collect) straight to a local `.json` file. A new `/export [filename]` command works in both `SessionScreen`'s input bar (alongside `/roll`/`/chat`) and `LobbyScreen`'s chat input - the owner's own original lobby framing described reviewing/saving a character as a lobby-time action too, not just mid-adventure.
  - **Import is a `join_session` field, new-character-only** - a new optional `imported_character` payload field (the exported dict, read client-side by a new `_load_character_file()`), gated the exact same way `character_class` already is in `_on_join_session`: only consulted when the join is a genuinely new character, silently ignored on a reconnect (confirmed by a dedicated test - an existing character's real HP loss survives a reconnect attempt carrying a bogus import untouched). When valid, the imported sheet's `name`/`character_class`/`stats`/`inventory`/`xp`/`level`/`notes` all win outright over freshly-typed `player_name`/`character_class` - not a merge. `player_id` is always overridden to the real joining connection's id, never trusted from the file - a new `_character_from_import()` (`server/engine.py`) does this override plus full re-validation via `CharacterSheet(**imported)`, independent of the client's own lighter read/JSON-parse check, since a client is never a trusted boundary for another connection's data.
  - **Graceful fallback on any failure, both layers** - an unreadable path or invalid JSON fails fast client-side (`WelcomeScreen`'s `#welcome-error`, before `join_session` is even sent); a structurally invalid or corrupted import that somehow reaches the server falls back to the exact same fresh `build_starting_character()` sheet a blank/unrecognized `character_class` already falls back to, plus a `system_message` warning telling the player the import didn't take - the same graceful-fallback convention this codebase already established for exactly this class of "untrusted optional input" problem.
  - **A real, immediately-hit layout bug, found the moment the new tests ran, not by inspection** - adding the new `Static`+`Input` import field to `WelcomeScreen` pushed `#join` (and everything below it) past a standard 80x24 terminal's visible height, the identical "adding fields to an already-tall screen" class of bug this workspace's sibling Vulcan/Anvil projects have hit and fixed multiple times, now reproduced and fixed here for the first time in Oracle. Root-caused with the same technique those projects used - measuring real widget regions (`widget.region`, `container.virtual_size`) directly rather than guessing - not by trial and error: `#welcome-box` (a plain `Vertical`, `height: auto`, no cap) let its content grow past the screen with nowhere to go. Fixed properly, not papered over: `VerticalScroll` instead of `Vertical` plus `max-height: 100%` (so overflowing content scrolls within its own bounds instead of pushing later widgets off-screen), then trimmed real vertical waste until nothing needs to scroll at all in the common case - a trailing blank line on the title `Static`, the box's top/bottom padding, and (a second real specificity bug caught by re-measuring after the first fix didn't move the numbers - `#welcome-box Input`'s descendant selector out-specifies a bare `#import-input` id selector in Textual's CSS cascade, confirmed by checking `child.styles.margin` directly rather than assumed) a `#welcome-box #import-input` override for the last input's margin. Verified by measuring the exact same widget regions after each change until `#welcome-box`'s `virtual_size` matched its visible height exactly (22 rows, no overflow) and every existing widget - `#welcome-error` included - landed fully inside the 24-row screen.
  - **14 new tests** (152 → 166 passing): `tests/test_engine.py` gained 4 (a valid import overriding name/class/stats/xp/level while `player_id` is still forced to the real connection's id, an invalid import falling back to a fresh sheet with the right warning, a non-dict import handled the same way, an import on reconnect correctly ignored - the existing character's real HP loss survives untouched); `tests/test_client_app.py` gained 9 (import field visibility on new vs. returning characters, a valid import file's payload reaching `join_session` correctly, an unreadable path and invalid JSON both producing the right welcome-screen error and never connecting, a blank import field sending the unchanged old payload shape, `/export` from both the lobby and session screens writing a real file and not also being sent as chat/`player_action`, and the no-filename default-name case).
  - **Verified end-to-end over a real websocket, not just at the unit level** - `tests/test_transport_e2e.py` gained `test_character_import_over_a_real_session_wins_over_typed_name_and_class`: a real `DungeonMasterApp` reads a real local export file via `#import-input`, joins a real `Transport`/`GameEngine` over an actual socket, and confirms both the real server-side `Session.characters` entry (name/xp/level/player_id all correct) and the real `CharacterSheetPanel` rendering reflect the imported character - not the "Someone Else" deliberately typed into `#name-input` alongside it, which the test confirms was correctly ignored. Full suite: 166 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
- **Real ability scores (2026-08-08), the foundational mechanic that XP/leveling and character creation had both already flagged as a deliberately-deferred gap.** From an explicit brainstorm: asked what other mechanics would strengthen the game, listed several candidates (formal initiative, mechanical conditions, structured equipment, rest/recovery, skill proficiencies), recommended ability scores first since "almost everything else on this list would eventually want to lean on it," and the owner picked exactly that to start with.
  - **Reuses `CharacterSheet.stats: dict[str, int]`, not a new field** - it already existed, was already documented (in code comments) as private alongside `inventory`/`notes`, and was simply never populated by anything. Keyed `str`/`dex`/`con`/`int`/`wis`/`cha` - the exact same key set `server/rules/srd.json`'s monster stat blocks already use (goblin: `{"str": 8, "dex": 14, ...}`), so this closes a second, independent small gap along the way: a player's stats now speak the same shape the DM already sees for every monster via `lookup_rule`, instead of two disconnected conventions.
  - **Generation is deterministic, grounded in real SRD data, not random or invented.** New `STANDARD_ARRAY` (`server/engine.py`) is the SRD's own real Standard Array character-creation option (`15, 14, 13, 12, 10, 8`); new `CLASS_ABILITY_PRIORITY` assigns it per class via a fixed priority order - each class's own two real SRD `saving_throws` (already sitting unused in `srd.json` since the class data was first added) come first, CON always second, the rest in ordinary archetype order. A blank/unrecognized class gets no stats at all, the same fallback its HP/inventory already use. Every class happens to place CON second, so every class gets a +2 CON modifier from this particular array - not a coincidence to read too much into, just what the SRD's own saving-throws-first + universal-survival-second ordering produces from a fixed array.
  - **A new `stat_modifiers` computed field (`server/state.py`, pydantic `@computed_field`), not something the DM computes itself.** Real modifiers (`floor((score-10)/2)`, a new module-level `ability_modifier()`) are precomputed and appear in `model_dump()`/`model_dump_json()` output automatically - the same "don't rely on the LLM to get arithmetic right when the engine can just do it" reasoning the XP-award trigger above is already built around, extended to ability scores. Confirmed pydantic's default `extra="ignore"` behavior means a previously-exported character file (which now legitimately contains a `stat_modifiers` key, since export is just `model_dump()`) re-imports cleanly without erroring on a "field" that's actually read-only.
  - **CON modifier now applies to HP, both at creation and on level-up** - `build_starting_character`'s level-1 HP and the level-up HP growth inside `apply_update`'s NPC-defeat branch (added by the XP/leveling work) both now use the real 5e formula (hit die max + CON modifier, floored at 1 so a bad CON score can't produce 0 or negative HP/growth). Closes the exact gap both of those entries, above, explicitly flagged as deferred at the time.
  - **`request_roll` gained an optional `ability` field, `AnthropicNarrator` only (same reliability-driven gating `request_roll`/`update_world` already have)** - when the DM names an ability tied to a roll (an ability check, a saving throw, an attack), the engine looks up the acting character's real `stat_modifiers` and adds it itself, rather than trusting the model to compute and correctly splice a second signed modifier into the dice notation string by hand. **A real limitation in `server/dice.py`'s existing notation regex, found while designing this, not by trial and error**: `_DICE_RE` only supports one signed modifier group already embedded in the notation (`1d20+3` parses; `1d20+3+2` would not), so bolting an ability modifier onto the notation string as text wasn't viable - `dice.roll()` instead gained a separate `extra_modifier` parameter, summed into the total independently of notation parsing. The modifier is folded into `dice_result`'s `result` and also reported as `ability`/`ability_modifier` so a client can show *why* a roll came out the way it did (e.g. "+1 DEX"), not just an opaque total - `_dice_log_text`/`_dice_result_envelope`/the client's `_dice_result_line` all updated to render it, using `.get()` rather than indexing so the existing player-initiated `/roll` path (which never sets these keys) is untouched.
  - **A real, small bonus found while writing this, not originally scoped**: `server/rules/srd.json`'s monster stat blocks (goblin, wolf, skeleton, orc, zombie) had real ability scores sitting there unused this whole time - `apply_update`'s NPC-introduction branch now reuses the exact same target-name-to-monster lookup `_xp_for_npc` already does for Challenge Rating to copy a matched monster's real `stats` onto its tracked NPC sheet for free. An unmatched name still gets no stats, same as an unrecognized player class.
  - **Client**: `CharacterSheetPanel` gained a real "Ability Scores" section (`STR 15 (+2)` style, D&D Beyond's own convention) below the HP line, owner-only (never rendered for a Party row, matching the existing private/public split) - stale "Oracle has no... ability-score data yet" comments in `client/app.py` updated rather than left to drift.
  - **15 new tests**: `tests/test_state.py` gained 6 (`ability_modifier()`'s formula against real reference points, the computed field empty/populated/negative-modifier cases, `model_dump()`/`model_dump_json()` inclusion, and the extra-key-on-construction regression lock described above); `tests/test_dice.py` gained 2 (`extra_modifier` adding on top of a notation's own modifier, defaulting to zero); `tests/test_engine.py` gained 4 (`request_roll` applying a real modifier and folding it into the total - not just displaying it, an unknown ability key being a graceful no-op, NPC stats populated from a matched SRD monster, left empty for an unmatched one) plus the existing class-kit/fallback/level-up/public-view tests updated for the new real HP values (every class's HP shifted by its real CON modifier); `tests/test_client_app.py` gained 3 (the sheet panel rendering real scores/modifiers, the Party view never showing them, the dice_result ability tag). Full suite: 181 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
  - **Verified end-to-end over a real websocket too** - `tests/test_transport_e2e.py` gained `test_ability_score_modifier_applies_to_a_dm_requested_roll_over_a_real_session`: a real fighter character joins a real session (confirming the sheet panel renders its real generated stats - `DEX 13 (+1)`), a scripted DM calls `request_roll` with `ability: "dex"` over an actual websocket, and both the rendered log line and a direct check against real server-side `Session` state confirm the +1 modifier was genuinely applied, not just displayed.
  - **Deliberately not built**: player-chosen stat allocation (point buy, standard-array self-assignment, or rolling) - the fixed per-class array is a small first slice by design, matching `build_starting_character`'s existing "small, deterministic, not a full character builder" scope; formal initiative, mechanical conditions, structured equipment, and skill proficiencies remain open candidates from the same brainstorm, not yet picked up.
- **Rest and recovery (2026-08-08), the next pick from the same brainstorm - the lowest-open-question candidate of what was left, so picked up autonomously (per the standing "develop and improve where we can" instruction) after the ability-scores steering question went unanswered across several stale/duplicate scheduled PR check-ins.** Closes a gap that's existed since HP was first tracked: healing had always meant the DM narrating a positive `hp_delta` and computing that number itself.
  - **Extends the existing shared `update_character` tool, not a new tool.** A new optional `rest` field (`"short"`/`"long"`) alongside `hp_delta` - deliberately *not* gated the way `request_roll`/`update_world` are, since `update_character` itself already reaches both backends. `CharacterSheet.apply_update()` (`server/state.py`) does the actual computation: `"long"` fully restores HP (real 5e's own actual rule, not a simplification); `"short"` restores half of whatever's currently missing (`(max_hp - hp) // 2`, floored) - a deliberate simplification of the real hit-dice-spending mechanic, chosen specifically because it needs no new resource tracked on the sheet at all, unlike a real hit-dice pool would.
  - **A real, deliberate scope boundary, not an oversight: rest never touches conditions.** Most SRD conditions (poisoned, frightened, stunned, ...) don't just expire with time under the actual rules (`server/rules/srd.json`'s own condition descriptions confirm this) - silently clearing them here would be a rules error, not a simplification. The DM can still pair `rest` with an explicit `remove_condition` in the same call when the fiction actually calls for it.
  - **Applies to NPCs for free** - the logic lives on `CharacterSheet.apply_update()` itself, so a rested/regrouped NPC (`target` set to its name) heals exactly the same way the acting character does, no separate wiring needed in the NPC-targeting branch of `apply_update`'s closure.
  - **9 new tests, all at the unit/engine level - no new client rendering or wire format, so no new e2e test was needed** (rest reuses the exact `character_update`/`player_update` broadcast path `hp_delta` already uses). `tests/test_state.py` gained 7 (long rest fully heals, a no-op at full HP, short rest restores half of missing rounded down, an odd missing amount floors correctly, short rest never overshoots `max_hp`, a no-op at full HP, conditions left untouched); `tests/test_engine.py` gained 2 (a real multi-turn scenario - damage then a long rest - through the actual tool-call path, and the same NPC-targeting reuse confirmed on a tracked NPC). Full suite: 190 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
  - **Both system prompts updated** (`DM_SYSTEM_PROMPT`/`OLLAMA_SYSTEM_PROMPT`) to tell the DM to use `rest` instead of guessing an `hp_delta` when a character/NPC rests for a meaningful stretch of time.
  - **Still open from the same brainstorm**: formal initiative, mechanical conditions (deferred specifically because implementing them properly would need a real advantage/disadvantage roll mechanic that doesn't exist yet, and several SRD conditions reference speed/positioning concepts Oracle doesn't track), and skill proficiencies.
- **Structured equipment: AC and weapon damage (2026-08-08), the owner's explicit pick after asking "what do you recommend" for the next mechanic.** The recommendation: `server/rules/srd.json`'s equipment entries already carry real weapon damage dice (`longsword: "1d8 slashing"`) and armor AC data (`leather_armor: "11 + Dex modifier"`) that nothing had ever used - the same "real data already sitting there unused" pattern ability scores' saving-throws data and XP's Challenge Rating data both already were - and it's the direct payoff of `request_roll`'s `ability` field from the ability-scores work (a real attack roll needs a real damage roll to follow it). Recommended over formal initiative and mechanical conditions specifically because those both need genuinely new game-state concepts (a combat-mode lifecycle; an advantage/disadvantage roll mechanic plus speed/positioning several SRD conditions reference) worth a real design conversation rather than a guess - structured equipment needed neither. Owner confirmed: *"let's do this and work on it."*
  - **`CharacterSheet.ac` is a new plain stored field, not a computed one like `stat_modifiers`** - a real, deliberate distinction, not an inconsistency: a player's AC is *derived* (armor's base + DEX modifier, computed once by `build_starting_character`/a new `_compute_ac()` helper, which has both the SRD equipment data and the character's own stats in hand), but a real 5e monster's AC is a flat *authored* value (natural armor etc. already folded in by the SRD itself) - copied verbatim from the matched monster entry on NPC introduction, reusing the exact same target-name lookup XP/stats already established. One formula can't correctly cover both roles, so this couldn't be a single computed field the way `stat_modifiers` is.
  - **A new `_compute_ac()` (`server/engine.py`) generalizes over whatever's actually in inventory**, matched against real SRD equipment data via `RulesIndex.get_entry()`, rather than hardcoding to the one armor item currently in the dataset - a new `_armor_base_ac()` helper parses the base number out of an SRD armor entry's own `ac` text (e.g. "11 + Dex modifier") via a small regex, so this keeps working without a code change if more armor is added to `srd.json` later. **A real, deliberate simplification, named rather than hidden**: AC doesn't recompute if inventory changes after character creation (e.g. picking up different armor mid-session) - accepted because the SRD equipment dataset currently has exactly one armor item, so the real-world impact of this gap is currently zero.
  - **`request_roll` gained an optional `weapon` field** (`AnthropicNarrator` only, same gating `ability`/`update_world` already have) - naming a real equipment item resolves its actual SRD damage die and uses that as the roll's notation instead of whatever the DM typed, and reports the SRD's own damage type too (a new `damage_type` field, threaded through `dice_result`/`_dice_log_text`/the client's `_dice_result_line` the same way `ability`/`ability_modifier` already were). **Composes naturally with `ability` in the same call, needing zero new code to combine them** - a real 5e damage roll is exactly "weapon's die + ability modifier" (STR for most melee weapons, DEX for finesse/ranged ones), and both fields already resolved independently against the same `character`/`rules` the closure already has in scope. An unmatched weapon name falls back to whatever `dice` was given, the same graceful-miss convention every other name-based lookup in this file already follows.
  - **`ac` joins the public character view** (`_public_character_view`, alongside `level` - both real facts about a character visible to anyone in the scene, not owner-only bookkeeping like `xp`/`stats`).
  - **Client**: `CharacterSheetPanel._hp_line()` gained an optional `ac` parameter, shown right next to HP in the header (D&D Beyond's own "core combat stat block" grouping) - both the owner's own sheet and Party rows now show it, since it's public; the dim NPC status log line gained it too. `_dice_result_line()` renders `damage_type` the same way it already renders `ability`/`ability_modifier`.
  - **13 new tests**: `tests/test_state.py` gained 1 (`ac` defaults to the real unarmored baseline, 10); `tests/test_engine.py` gained 8 (`_compute_ac()` unarmored and armored directly, case/whitespace-insensitive armor matching, NPC AC copied from a matched SRD monster vs. left at the default for an unmatched one, the public view carrying `ac`, a real weapon damage roll resolving the correct die/type/total, an unmatched weapon name falling back gracefully) plus the existing class-kit/fallback tests extended with real expected AC per class; `tests/test_client_app.py` gained 4 (the sheet panel rendering AC next to HP, a Party row showing it since it's public, `dice_result` rendering the damage-type tag, and it combining correctly with an ability tag). Full suite: 202 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
  - **Verified end-to-end over a real websocket too** - `tests/test_transport_e2e.py` gained `test_structured_equipment_ac_and_weapon_damage_over_a_real_session`: a real fighter character joins a real session (confirming the sheet renders its real computed AC - `AC 12`, leather armor's base 11 + the fighter's real +1 DEX), a scripted DM calls `request_roll` with `weapon: "longsword"`/`ability: "str"` over an actual websocket, and both the rendered log line and a direct check against real server-side `Session` state confirm the real damage die/type/modifier were genuinely resolved, not just displayed.
  - **Still open from the same brainstorm**: formal initiative, mechanical conditions, and skill proficiencies - all three still have the same open design questions named above, not resolved by this slice.
- **Mechanical conditions: disadvantage (2026-08-08), the owner's explicit next pick ("let's tackle mechanical conditions next") - the item this project's own earlier brainstorm had flagged as needing real design work rather than a guess, worked through properly rather than skipped.** `server/rules/srd.json`'s five tracked conditions (poisoned, prone, grappled, frightened, stunned) have always had real SRD mechanical text sitting unused - `conditions` was purely a display list before this, narrated around but never enforced.
  - **The real design questions from the original brainstorm note, resolved by reading each condition's actual SRD text closely rather than guessing at a uniform treatment.** Only `poisoned`/`frightened`/`prone` (`server/engine.py`'s new `DISADVANTAGE_CONDITIONS`) get a mechanical effect - each is the only one, of the five, with a real SRD-described effect where the *bearer's own* rolls suffer. `grappled` correctly gets nothing at all: its only real effect is a speed-0 movement restriction, and since Oracle has no speed/movement system to hook that to, this isn't a gap the feature needs to fill - the condition genuinely has no self-roll effect in the text to model. `stunned`'s real effects (incapacitated, automatic failure on STR/DEX saving throws, attacks *against* it gain advantage) are a structurally different, bigger class of mechanic entirely - target-side and turn-blocking, not "the bearer rolls worse" - and stayed deliberately out of scope: a stunned character still acts and rolls completely normally today, a real, honestly-named gap rather than a silently-dropped promise.
  - **A real advantage/disadvantage primitive added to `server/dice.py`'s `roll()`** - real 5e's own "roll two d20s, keep the better/worse" mechanic, as new `advantage`/`disadvantage` parameters. Deliberately only applies to a genuine single d20 (never a damage roll or any other notation, matching real 5e's own restriction exactly) - anything else is a silent no-op, the same "not applicable" convention this module already follows for its other parameters. Both flags true cancel out to neither, real 5e's own rule for when both would apply at once - a caller doesn't have to resolve that itself.
  - **Fully automatic, deliberately no new tool field at all** - a new `_has_disadvantage(character)` checks the acting character's own tracked `conditions` (case-insensitively, the same robustness NPC name-matching already established) against `DISADVANTAGE_CONDITIONS` and the engine applies disadvantage itself. `request_roll`'s schema gained *nothing* for this - the same "the engine computes this from real tracked state, not the model's judgment" reasoning every other mechanic in this project already follows, taken to its natural conclusion: there's no model-supplied field to design at all when the trigger is purely deterministic tracked state. Applies equally to a DM-requested `request_roll` and a plain player-typed `/roll` - a real tracked condition is real state regardless of who initiated the roll.
  - **A real, broader simplification than strict RAW, named rather than hidden**: poisoned/frightened's actual SRD text excludes saving throws specifically - but `request_roll` has no field distinguishing a save from a check from an attack roll today, so this applies disadvantage to *any* roll while a qualifying condition is active, not narrowly scoped per roll type. Disadvantage never stacks (multiple qualifying conditions still just roll twice once, real 5e's own rule) - a new `disadvantage_reasons` list still names every condition that contributed, even though only one roll-twice happens.
  - **A real, subtle display bug caught while designing the client rendering, not by running anything yet** - `dice_result`'s `rolls` now holds *both* real d20s when disadvantage applied (the discarded one shown for transparency, matching this project's "explain the roll, don't just show a bare number" convention already established for `ability`/`damage_type`), but the client's existing natural-20/natural-1 highlight logic checked "does *any* entry in `rolls` equal the die's max/min" - which would wrongly highlight green off a high *discarded* roll even though the low *kept* roll is what the total (and DC comparison) actually reflects. Fixed in `client/app.py`'s `_dice_result_line()` by narrowing the highlight check to the genuinely-kept die specifically when disadvantage applied, confirmed by a dedicated regression test using exactly this scenario (`rolls: [20, 4]`, disadvantage kept the 4) before the fix would have caught it.
  - **Client**: `_dice_result_line()` renders a `(disadvantage: poisoned)`-style tag the same way it already renders `ability`/`damage_type` tags, naming the real condition(s) responsible rather than a bare unexplained low roll.
  - **`DM_SYSTEM_PROMPT` gained one informational line, no new tool schema** - tells the model this happens automatically and it doesn't need to ask for it or account for it, but should narrate the result it gets back (which may come out lower than expected). `OLLAMA_SYSTEM_PROMPT` wasn't touched - Ollama has no `request_roll` tool to be affected by this at all, and the player-initiated `/roll` path this also covers needed no prompt change either way, since no model is involved in triggering it.
  - **21 new tests**: `tests/test_dice.py` gained 6 (disadvantage/advantage each keep the correct one of two real mocked d20s - not just checked probabilistically, both real modifiers still apply on top of the kept die, both flags together cancel to a normal single roll, disadvantage is a genuine no-op for non-d20/multi-die notation); `tests/test_engine.py` gained 6 (a tracked condition triggers real disadvantage through a full `request_roll` turn with mocked dice confirming the *kept* value specifically, case-insensitive condition matching, two qualifying conditions still only roll-twice once while naming both reasons, `grappled` correctly has zero effect, the same automatic behavior on a plain player `/roll`) plus the existing ability-roll test's assumptions held (no conditions present, so unaffected); `tests/test_client_app.py` gained 2 (the disadvantage tag renders with both real rolls shown, and the highlight-uses-kept-die regression test described above). Full suite: 215 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
  - **Verified end-to-end over a real websocket too** - `tests/test_transport_e2e.py` gained `test_mechanical_conditions_disadvantage_applies_over_a_real_session`: a scripted DM calls a real `add_condition: "poisoned"` and then a real `request_roll` in the same turn over an actual websocket (mocked dice for a deterministic kept-die assertion, everything else genuinely real), confirming the condition tracked moments earlier in the same turn was picked up by the very next roll - not a pre-seeded condition or a mocked `dice.roll()` the way the unit-level tests use.
  - **Deliberately not built**: advantage from any source (nothing currently grants it - the `dice.py` primitive fully supports it, ready for e.g. a future target-side "attacking a prone creature" effect, but nothing calls it that way yet), stunned's incapacitation/auto-fail-saves/attacks-against-it-have-advantage effects, and roll-type-aware (save vs. check vs. attack) precision matching strict RAW. Formal initiative and skill proficiencies remain the two still-open items from the original brainstorm.

- **`character_edit` implemented (2026-08-08), a non-mechanic, non-model-dependent pick from the "Later" checklist's own item, per the standing "develop and improve where we can" instruction — formal initiative and skill proficiencies both still have real open design questions worth a real conversation rather than a guess, so this deliberately picked an already-scoped, already-protocol-defined gap instead of guessing at either of those.** `character_edit` (`{field, value}`) had existed in `shared/protocol.py`'s `EventType` literal and `docs/protocol.md`'s own table since early in this project, but nothing ever implemented a server handler for it - a player had no way to touch their own sheet's bookkeeping without going through the DM's `update_character` tool call, even for something as low-stakes as jotting a note.
  - **`server/engine.py`'s new `_on_character_edit` deliberately only ever touches `notes`/`add_item`/`remove_item`** (`CHARACTER_EDIT_FIELDS`) - the mirror image of `update_character`'s own mechanical fields, not a general-purpose sheet editor. `hp`/`conditions`/`stats`/`xp`/`ac` all stay DM- or engine-only on purpose: a player editing their own sheet directly could otherwise grant themselves healing, clear a condition, or add gear the DM never actually narrated, undermining the same "mechanical state needs adjudication" principle every other feature in this project has been built around. An unrecognized `field`, or a falsy `value`, gets a `system_message` warning rather than a silent no-op or an exception; removing an item not actually in inventory is handled the same way rather than a silent `KeyError`-adjacent no-op.
  - **Exempt from turn order for free, not a special case that needed its own code** - `GameEngine.handle`'s dispatch (`getattr(self, f"_on_{envelope.type}")`) only ever checks `current_turn` inside `_on_player_action` itself; every other handler, this one included, was already turn-order-exempt by construction, matching `chat_message`/`dice_roll` and docs/protocol.md's own existing (if previously unimplemented) claim.
  - **Private only, and genuinely simpler than a DM-driven sheet change because of it** - `notes`/`inventory` are never part of `_public_character_view` (see "Other players' presence" above), so unlike `_narrate_and_apply`'s `sheet_changed` branch (which pushes a private `character_update` *and* a public `player_update`), a successful edit here only ever needs the one private `character_update` back to the editing player - there's no public counterpart to also compute, since nothing here is visible to anyone else regardless.
  - **Client**: three new `SessionScreen` input-bar commands - `/note <text>` (overwrites the player's own notes), `/item add <name>`, `/item remove <name>` - each a thin `character_edit` send. No new client-side rendering logic needed at all: the resulting `character_update` already flows through the exact same `_handle()` branch every other sheet change uses, so the sheet panel just re-renders with the new inventory/notes like it would for any other change.
  - **9 new tests**: `tests/test_engine.py` gained 7 (notes update privately with no public broadcast, add/remove item, removing an absent item warns without mutating state, a disallowed mechanical field like `hp` is rejected without mutating state, exemption from turn order confirmed directly, editing before joining warns instead of crashing); `tests/test_client_app.py` gained 3 (`/note`, `/item add`, `/item remove` each send the correct envelope, not routed to `chat_message`). 1 new real end-to-end test (`tests/test_transport_e2e.py`): a real client sends `/item add`/`/note` over an actual websocket with no DM/narrate() call involved at all, confirming both the live sheet panel re-render (inventory) and a direct server-state check (notes, which never render client-side). Full suite: 226 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
  - **Not built (yet)**: a `LobbyScreen` equivalent for these commands (today they only exist in `SessionScreen`'s input bar, matching the client's existing `/roll`/`/export` split), and any structured inventory shape beyond a flat list of name strings (no quantity, no per-item notes).

- **Session-transcript export (2026-08-08), the other item explicitly flagged as "cheap, no model/GPU/API dependency" on the "Later" checklist - a second non-mechanic pick, same reasoning as `character_edit` just above.** Distinct from character export/import (a single `CharacterSheet` snapshot with a real import path back in) - a transcript is the running narration log itself, a record to keep, with no import counterpart at all.
  - **Entirely client-side, no protocol envelope added.** A new `/transcript [filename]` command (`SessionScreen`) reads straight off the real `#log` `RichLog` widget already on screen via a new `_transcript_text()` helper, and writes it to a local `.txt` file - nothing requested from or sent to the server, since every line the player has seen this session already lives in that widget's own accumulated `.lines`. Confirmed `RichLog`'s `max_lines` defaults to unbounded (neither of this client's two `RichLog` instances sets it) before relying on this, rather than assuming it - a bounded default would have silently dropped older lines from a long session's export.
  - **Plain text, not the underlying Rich-markup source** - `Strip.text` (the exact technique the test suite's own `_log_text` helper already used, reused here in production code for the first time rather than just tests) strips color/bold markup down to the characters actually shown on screen, since a transcript is meant for a human to read back later, not to be re-rendered.
  - **`SessionScreen` only, not `LobbyScreen`** - unlike `/export` (genuinely useful in the lobby too, reviewing/saving a character before the adventure starts), there's no narration to transcribe before a session has actually started, so no lobby-side command was added.
  - **5 new tests**: `tests/test_client_app.py` gained 2 (a real `log_entry` broadcast round-trips into the exported `.txt` file with nothing sent to the server, and the no-filename default-name path); `tests/test_transport_e2e.py` gained 1 real end-to-end test - a real client, a real websocket, a real opening scene actually streamed in (`NarratesOpeningDM`), then `/transcript` genuinely reads that real narration back out of a real file on disk. Full suite: 229 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).
  - **Not built (yet)**: any structured format beyond flat plain text (no per-line timestamps or kind labels, e.g. `[narration]`/`[chat]`, even though `log_entry`'s own `kind` field would make that straightforward to add later). The lobby-chat-log equivalent flagged here is now closed - see below.

- **A distinct visual treatment for the missed-change warning (2026-08-08), a third already-scoped "Later" pick per the standing "develop and improve where we can" instruction.** The gap, named explicitly when the missed-change heuristic itself shipped: every `system_message` rendered identically client-side regardless of `level` - the heuristic's own "your sheet might be out of sync" nudge looked exactly the same as an ordinary "It's not your turn" rejection, even though it's a genuinely different category (an advisory the player should weigh, not a plain fact about what just happened).
  - **A new optional `advisory` field on `system_message`, not a `level`-styling overhaul.** `_system_envelope()` (`server/engine.py`) gained an `advisory: bool = False` parameter, included in the payload only when true - the same "don't carry an always-false field" convention `dice_result`'s own optional fields already established. Only the missed-change heuristic's own call site sets it; every other `system_message` in the codebase (out-of-turn rejection, save-failure warning, join/turn announcements, `character_edit` validation) is completely unchanged, still carrying no `advisory` key at all.
  - **Deliberately narrow scope, named rather than silently expanded**: this doesn't give `info`/`warning`/`error` each their own look - every `level` still renders identically outside this one case. The real gap was the missed-change warning specifically blending in with ordinary warnings, not a general "system messages need color-coding" redesign nobody had actually asked for.
  - **Client**: `_handle()`'s `system_message` branch (`client/app.py`) renders `[yellow]⚠ {text}[/yellow]` when `advisory` is true, the exact prior `[dim]{text}[/dim]` styling otherwise - applies in both `SessionScreen`'s log and `LobbyScreen`'s status line, the same two render sites the un-styled version already covered.
  - **5 new tests**: `tests/test_engine.py` gained 2 (the missed-change heuristic's own warning carries `advisory: True`; an ordinary out-of-turn warning carries no `advisory` key at all, not even `False`); `tests/test_client_app.py` gained 2 (an advisory system_message renders with a real yellow styled segment; an ordinary one doesn't). 1 new real end-to-end test (`tests/test_transport_e2e.py`): a scripted DM narrates unambiguous lethal damage with zero `update_character` call over a real websocket session, confirming the real client genuinely renders the resulting advisory with yellow styling, not just that the flag round-trips at the payload level. Full suite: 234 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).

- **Objective expiry and failure (2026-08-08), a fourth already-scoped "Later" pick per the standing "develop and improve where we can" instruction.** The gap, named explicitly when the immersion pass first shipped objectives: `Objective.status` only ever had `active`/`completed`, so nothing could represent a quest going stale on its own or being genuinely failed outright - every objective either stayed open forever or eventually got marked a success, even when the fiction called for something else.
  - **Two real terminal states, not one generic "closed."** `Objective.status` (`server/state.py`) gained `expired` and `failed` alongside the existing `active`/`completed` - "the caravan already left without you" (expired) reads differently from "you drove off the bandits, but the merchant died anyway" (failed), and a DM/player shouldn't have to infer which happened from an objective simply vanishing from the active list.
  - **`update_world` gained matching `expire_objective`/`fail_objective` fields** (`AnthropicNarrator` only, the same gating every other `update_world`/`request_roll` field already has), each an exact-text match mirroring `complete_objective`'s own shape - no new lookup mechanism, and the shared `TOOL_INSTRUCTIONS` text now spells out that an objective can resolve three real ways, not just complete, so the DM doesn't default to `complete_objective` or silently leave a dead quest active forever.
  - **A real correctness fix found while adding this, not introduced by it.** `complete_objective`'s own guard (`status != "completed"`) only ever worked because `active`/`completed` were the only two states that could exist - with `expired`/`failed` added, that guard would have let a later `complete_objective` call silently flip an already-failed or already-expired objective back to completed, since neither `"failed"` nor `"expired"` equals `"completed"`. All three verbs now only fire from `status == "active"`, so a real terminal state can never be overwritten by a different one - caught and fixed while writing the regression test for the new verbs, not by running anything in production.
  - **Deliberately no client rendering added.** The sheet panel's Objectives section already only shows `status == "active"` entries (a completed objective already just silently drops out of view today, no distinct fanfare), so expired/failed objectives are correctly excluded by that same existing filter with zero client changes needed. Matches an existing precedent in this codebase: `flags` (`set_flag`/`clear_flag`) are real, DM-usable world state with no dedicated UI either, visible via `state_sync`/`world_update`'s full payload rather than a rendered panel section - this isn't a new kind of gap, just a second field following the same shape.
  - **4 new tests**: `tests/test_state.py` gained 2 (`expire_objective`/`fail_objective` each transition a real active objective and report it in the result text; a second, different terminal-state call afterward is correctly a no-op, confirming the corrected guard); `tests/test_engine.py` gained 1 (a real turn through the engine expires an objective seeded on an earlier turn, and the resulting `world_update` broadcast carries the new status). Full suite: 237 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).

- **A lightweight NPC disposition field (2026-08-08), a fifth already-scoped "Later" pick per the standing "develop and improve where we can" instruction.** The gap, named explicitly when this was originally flagged: nothing gave the DM a structured value to stay consistent against turn to turn for an NPC's attitude toward the party - only free-text `notes`, with no cheaper option short of the bigger, still-deferred who-knows-whom relationship graph.
  - **`CharacterSheet.disposition: Literal["hostile", "neutral", "friendly"] = "neutral"`** (`server/state.py`) - the same shared-model tradeoff `notes`/`ac` already make: meaningful for a tracked NPC (`Session.npcs`), unused and left at its default on a real player character, rather than a second NPC-only sheet class for one field.
  - **`update_character` gained a sibling to `notes`, `disposition`** (shared by both backends, not Claude-gated - the same tool every `notes` update already goes through, both `UPDATE_CHARACTER_TOOL`'s schema and the two backends' own instruction text updated together). Settable on an NPC's introduction or updated later when the relationship meaningfully changes (a fight ends and they surrender, a favor is repaid) - the same "update the note when the relationship changes" guidance `notes` already carries, extended to a second, structured field alongside it.
  - **A real model-input boundary, not decorative, caught while writing this, not by running anything in production.** `disposition` is a closed enum on the model, but the update dict comes straight from a tool call - the JSON schema's own `enum` constrains `AnthropicNarrator`, but nothing stops `OllamaNarrator`'s shared `update_character` path from sending an arbitrary string, and a plain attribute assignment in `apply_update()` wouldn't re-validate against the `Literal` (pydantic v2 doesn't, by default, on direct attribute sets). Guarded explicitly: only one of the three known values is accepted, an unrecognized one is silently ignored rather than written past the field's own declared contract.
  - **A real client-rendering bug found only by running the test, not assumed safe.** The first version rendered a non-neutral disposition bracketed (`[hostile]`), matching the missed-change advisory's own `[yellow]...[/yellow]` bracket convention at a glance - but `RichLog` is `markup=True`, and Rich's own markup parser treats square brackets as a style tag, silently swallowing an unrecognized one like `"[hostile]"` from the rendered output entirely (confirmed directly: `Text.from_markup("HP 3/7 [hostile]").plain` strips it to `"HP 3/7 "`). Fixed by rendering it as a plain word instead (`goblin: HP 3/7 hostile`), no brackets at all - the same reason `conditions` already renders with parens rather than brackets elsewhere in this exact function.
  - **7 new tests**: `tests/test_state.py` gained 2 (`apply_update` sets a real disposition and reports it; an unrecognized value is silently rejected, confirming the guard); `tests/test_engine.py` gained 1 (a real turn introduces an NPC with a disposition, and a later turn updates it, both reflected in the tracked NPC and the `npc_update` broadcast); `tests/test_client_app.py` gained 4 (`_npc_status_line()` omits the default `"neutral"`, shows a real non-neutral value, handles a legacy dict with no `disposition` key at all with no crash, and a live `npc_update` envelope renders it in the log). 1 new real end-to-end test (`tests/test_transport_e2e.py`): a scripted DM introduces a real hostile goblin over an actual websocket session, confirming both the rendered log line and a direct server-state check. Full suite: 245 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).

- **Death saves (2026-08-08), closing the gap that's existed since HP was first tracked: hitting 0 HP was previously just a number sitting there, cosmetically red on the client's HP bar, with no real stakes attached.** Picked over the two still-open mechanics from the original brainstorm (formal initiative, skill proficiencies) since both of those still have real open design questions worth a real conversation rather than a guess (see "Mechanical conditions: disadvantage," above) - death saves had no such open question, just an unbuilt real 5e mechanic. Developed concurrently with the NPC disposition field just above, on a separate branch (`feat/death-saves`), and merged into `main` first - the two features landed independently and were reconciled via a real merge (both touch `CharacterSheet`/`client/app.py`, but in non-overlapping ways) rather than either one blocking the other.
  - **Scoped to player characters only, never NPCs - a real, deliberate boundary, not an oversight.** A defeated NPC already has its own, different, already-shipped 0-HP behavior: the XP/leveling entry above already treats an NPC's own hp>0-to-0 crossing as "defeated," awarding XP immediately - real 5e's own actual rule for ordinary monsters, which die outright at 0 HP rather than making death saves. Only player characters (and, in real 5e, important NPCs the DM chooses to grant the same treatment to - not modeled here) get this.
  - **Entering `dying`, and taking a further automatic failure while already down, are both the engine's own observed state transitions - never a DM tool call.** `CharacterSheet.apply_update()` (`server/state.py`) detects a real `hp_delta` crossing from >0 to 0 itself (sets `dying: true`, resets both counters) and detects more damage landing while already at 0 HP (an automatic failure, real 5e's own rule) - the same "the engine computes this from real tracked state" reasoning XP-on-defeat and disadvantage-from-conditions already follow, extended here. **A real, named simplification**: real 5e doubles the auto-failure to two on a critical hit, but nothing in this project tracks whether a hit was a critical at all (`server/dice.py` has no crit concept), so every hit while down counts as one failure.
  - **The roll itself is a new, dedicated `death_save` event (`{}`), not folded into `dice_roll`** - a death save is always a fixed 1d20 with no notation to choose, and needs outcome bookkeeping (successes/failures/stabilize/died) no other roll carries. `GameEngine._on_death_save` (`server/engine.py`) rolls it and applies real 5e's own thresholds (10+ succeeds, a natural 20 regains 1 HP and wakes the character immediately, a natural 1 counts as two failures) via a new `CharacterSheet.record_death_save()` - the one place the 3-successes/3-failures threshold logic lives, shared with the automatic damage-while-down trigger above so the two triggers can't drift apart from each other.
  - **Reported as `dice_result` with `dc: 10`, reusing the envelope's existing rendering wholesale** - the client already shows "vs DC 10 — success/failure" and highlights a natural 20/1 on any roll, so the roll itself needed zero new client-side rendering code, only a `/deathsave` command to send it. The narrative outcome (now dying, stabilizes, has died, claws back to consciousness) is a separate `system_message`, the same "announce it, don't just let a redacted Party view silently reflect it" treatment the XP-award announcement already gets - compared against state captured before narration started (`was_dying`/`was_dead` in `_narrate_and_apply`), so an already-dying character doesn't get re-announced every subsequent turn.
  - **Exempt from turn order, like `dice_roll`/`character_edit` - and deliberately *not* tied to "the start of the dying character's own turn," the way real 5e's actual trigger works.** Automating that would mean hooking into turn advancement/`turn_prompt` to roll for a dying player automatically and skip their turn for them - a bigger, riskier change to the core turn loop than this slice needs, so this is a real, named simplification rather than a silently-dropped nuance: a player can `/deathsave` whenever they like, not just once per their own turn, and nothing enforces a once-per-turn cap.
  - **A normal `player_action` is rejected outright while `hp == 0`** (dying, stabilized-but-unconscious, or dead - all three get a distinct message), the same "It's not your turn" rejection pattern applied to a different reason an action can't proceed - real 5e's own "an unconscious creature can't take actions" rule. Doesn't advance the turn, so a dying player keeps getting reprompted toward `/deathsave` rather than silently skipped past.
  - **A real, honestly-scoped limitation, found while designing this rather than after shipping it: nothing in this protocol lets one player's turn apply healing to a *different* player's sheet.** `update_character`'s `target` only ever resolves to the acting character or an NPC - a pre-existing limit this feature doesn't attempt to close. A downed player can only recover via their own natural-20 death save, or the DM narrating an external rescue entirely outside the mechanical system.
  - **`dying`/`dead` join the public character view** (`_public_character_view`, alongside `level`/`ac`) - the rest of the party urgently needs to know a teammate is down, the same way they'd see it happen at a real table. The raw `death_save_successes`/`death_save_failures` counts stay owner-only, the same boundary `xp` already has.
  - **Client**: `CharacterSheetPanel._hp_line()`/`_other_player_line()` gained a `DYING`/`STABLE`/`DEAD` status label next to HP (own sheet and Party rows both, since `dying`/`dead` are now public) via a new shared `_death_status_label()`. A new `/deathsave` command, `SessionScreen` only (matching the client's existing `/roll`/`/export` split for session-only commands).
  - **28 new tests**: `tests/test_state.py` gained 10 (entering dying on an hp_delta drop to 0, an automatic failure from damage while already down, three automatic failures killing, healing clearing dying via both `hp_delta` and `rest`, dying never re-triggering once dead, `record_death_save`'s stabilize/death thresholds directly, a natural-1-as-two-failures both stopping early at death and not phantom-recording a fourth); `tests/test_engine.py` gained 15 (public view carries `dying`/`dead`, `player_action` rejected in all three 0-HP states with the right message per state, a real `_narrate_and_apply` dying announcement, `/deathsave` before dying/after death both warn without rolling, a natural 20 revives with 1 HP, success/failure counting, third-success-stabilizes and third-failure-kills each with their own announcement, a natural 1 counting as two failures, exemption from turn order); `tests/test_client_app.py` gained 5 (`/deathsave` sends the bare envelope, `_death_status_label`'s three states and their precedence). 1 new real end-to-end test (`tests/test_transport_e2e.py`): a real client's character is dropped to 0 HP over an actual websocket, a real subsequent action is genuinely rejected (not just asserted at the unit level), and a real `/deathsave` (mocked dice for a deterministic natural 20) revives the character with 1 HP, confirmed against both real server-side `Session` state and the real re-rendered sheet panel.
  - **Deliberately not built**: once-per-turn enforcement on `/deathsave` (see above), any way for one player to heal another's sheet (see above - a real gap this feature surfaces but doesn't attempt to close), and any client-visible countdown/warning as a character's failure count climbs toward 3 (today the raw counts are owner-only and only ever surface through the plain outcome text, not a dedicated "2 failures - one more and it's over" indicator).

- **Roll kind (2026-08-08), the highest-leverage pick off the same brainstorm's own list - picked specifically because it unblocks other already-flagged gaps rather than being one more standalone mechanic.** `request_roll` gained an optional `roll_kind` field (`"attack"`/`"save"`/`"check"`, `AnthropicNarrator` only, same gating `ability`/`weapon` already have) - purely descriptive of what a roll represents, never changing its own math.
  - **Its one real effect: narrowing `_has_disadvantage`'s scoping to match real per-condition SRD text, closing a gap this project's own "Mechanical conditions" entry explicitly named as a known simplification.** A new `ROLL_KIND_DISADVANTAGE_EXCLUSIONS` table (`server/engine.py`) - poisoned/frightened's real text is "disadvantage on attack rolls and ability checks," saving throws are never mentioned, so `roll_kind: "save"` excludes both; prone's real text only ever mentions attack rolls, so both `"save"` and `"check"` are excluded for it, a narrower scope than poisoned/frightened get.
  - **Backward compatible by construction, not by special-casing.** `_has_disadvantage(character, roll_kind=None)` only consults the exclusion table when `roll_kind` is actually given - an omitted `roll_kind` (a plain player `/roll`, or any request_roll call written before this shipped) keeps the exact prior "applies to any roll" behavior, so no existing call site or test needed to change for this to be additive.
  - **An unrecognized `roll_kind` value is treated as omitted**, the same graceful-miss convention every other name-based `request_roll` field (`weapon`, `ability`) already follows - a model that gets the enum slightly wrong falls back to the old broad behavior rather than erroring.
  - **Reported on `dice_result` and rendered as a `(save)`/`(check)`/`(attack)` tag**, the same "explain the roll, don't just show a bare number" transparency convention `ability`/`damage_type` already established - `client/app.py`'s `_dice_result_line()` needed one small addition, no new rendering logic.
  - **`DM_SYSTEM_PROMPT` updated to actually ask for it**, not just silently support it - explains *why* it matters (poisoned/frightened don't affect saves, prone only affects attacks) so the DM has a real reason to pass it, not just an unexplained optional field it might never reach for.
  - **13 new tests**: `tests/test_engine.py` gained 11 (roll_kind="save" excluding poisoned and frightened each on their own, roll_kind="attack" still applying poisoned's and prone's disadvantage, roll_kind="check" excluding prone specifically - the narrower-than-poisoned/frightened case, an omitted roll_kind keeping the legacy broad behavior, an unrecognized value falling back gracefully, roll_kind appearing on `dice_result`/the tool-result text when given and absent when not); `tests/test_client_app.py` gained 1 (the `(save)` tag rendering). 1 new real end-to-end test (`tests/test_transport_e2e.py`): a real `add_condition: "poisoned"` followed by a real `request_roll` naming `roll_kind: "save"` over an actual websocket, confirming disadvantage is genuinely absent from the real client's rendered log line, not just asserted at the unit level.
  - **Deliberately not built**: this ships the primitive and its one real usage (disadvantage scoping), not the two gaps it was built to unblock - skill proficiencies and stunned's auto-fail-on-saves effect remain separately not-yet-built, now with the field they'd need already in place.

### Mechanics brainstorm, continued (2026-08-08)

Death saves (just above) closed the "hitting 0 HP is a real stake" gap. Three more candidates surfaced while scoping that work, none built yet - logged here rather than guessed at, the same discipline the original initiative/proficiencies/equipment/conditions/rest brainstorm already followed.

- [x] **Ability Score Improvements (ASI) on level-up** - done (2026-08-08), see the "Ability Score Improvements" entry under Current state, below. Real 5e's own rule: most classes gain an ASI (+2 to one ability, or +1 to two) at levels 4, 8, 12, 16, and 19. Right now `CharacterSheet.gain_xp()`'s level-up path grows HP (hit die + CON modifier per level, see "Character progression: XP and leveling") but never touches `stats` - a level-10 fighter has the exact same ability scores and modifiers as it did at level 1, a growing staleness gap the more this leveling system actually gets played. **Well-scoped for a small first slice**: deterministic like every other generation step in this project (`STANDARD_ARRAY`/`CLASS_ABILITY_PRIORITY` already are) - e.g. always the class's own top-priority ability from `CLASS_ABILITY_PRIORITY`, +2, capped at 20 (real 5e's own hard ceiling) - not a player-chosen allocation (feats as an ASI alternative, real 5e's other option, deliberately out of scope - Oracle has no feat system at all). Ties directly into the already-built level-up branch in `apply_update`'s NPC-defeat closure (`server/engine.py`).
- [x] **A `roll_kind` field on `request_roll`** - done (2026-08-08), see the "Roll kind" entry under Current state, below. (`"attack"` / `"save"` / `"check"`), `AnthropicNarrator`-only like `ability`/`weapon` already are. **The single primitive most other already-flagged gaps are blocked on, not a standalone feature**: poisoned/frightened's disadvantage is currently a broader-than-RAW simplification specifically because nothing distinguishes a save from a check/attack (see "Mechanical conditions: disadvantage," above); stunned's deferred auto-fail-on-STR/DEX-saves effect needs the same distinction to know which rolls to intercept; skill proficiencies (still open from the original brainstorm, below) would need "this is a check" to know when a proficiency bonus applies at all. Building this once unblocks three separately-flagged gaps rather than three bespoke mechanisms.
- [x] **Spellcasting for wizard/cleric** - done (2026-08-08), see the "Spellcasting" entry under Current state, below - the biggest current gap between classes as *classes*, not just stat blocks. A real design conversation preceded this (as anticipated below), resolving the open questions that had kept it deferred: spell slots (a plain stored field, refreshed via the existing `rest` field), a small deterministic spell list per class (the SRD dataset expanded from 5 to 10 spells for this), and a real save-DC formula (8 + proficiency bonus + ability modifier - `proficiency_bonus` now exists, built for skill proficiencies above).
- [x] Skill proficiencies - done (2026-08-08), see the "Skill proficiencies" entry under Current state, below - unblocked by `roll_kind`, above, exactly as anticipated when `roll_kind` shipped.
- [x] Formal initiative - done (2026-08-08), see the "Formal initiative" entry under Current state, below - the combat-mode-lifecycle design question that kept this deferred was resolved through a real conversation first (explicit player-triggered start/end, not auto-detected), closing the last still-open item from the *original* brainstorm.

- **A lobby-chat-log equivalent to session-transcript export (2026-08-08), closing the exact gap that entry's own "Not built (yet)" bullet named.** Deliberately picked to avoid overlapping with concurrent work on this repo's core-mechanics track (death saves, `roll_kind` - see above) - a pure client-side, non-mechanical feature with zero risk of touching the same code those slices were already working through.
  - **`LobbyScreen` gained a matching `/transcript [filename]`**, reusing the exact `_transcript_text()` helper `SessionScreen`'s own version already established - the only real difference is which `RichLog` it reads (`#chat-log`, not `#log`) and its default filename (`lobby-chat.txt`, not `transcript.txt`, so the two never collide if both get exported into the same directory). Each screen's `/transcript` only ever sees its own log - a lobby export never includes in-session narration, and a session export never includes pre-adventure lobby chat.
  - **3 new tests**: `tests/test_client_app.py` gained 2 (a real lobby chat exchange exports to a `.txt` file with nothing sent to the server, and the no-filename default-name path). 1 new real end-to-end test (`tests/test_transport_e2e.py`): two real websocket clients in the lobby, one sends a real `chat_message`, the other's `/transcript` genuinely reads it back out of a real file on disk - not a single-client FakeTransport test. Full suite: 277 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).

- **A real NPC status panel (2026-08-08), replacing the "dim log line" first-pass UI footprint the original NPC-tracking feature explicitly flagged as revisit-later.** Picked deliberately for the same collision-avoidance reason as the lobby-chat-log transcript above - a client-only rendering feature, no overlap with the concurrent core-mechanics track (death saves, `roll_kind`).
  - **`CharacterSheetPanel` gained `render_npcs(npcs: dict)` and an "NPCs" section**, positioned the same way `Objectives`/`Party` already are - a divider, a bold header, one line per tracked NPC via a new `_npc_line()` classmethod, reusing the same 6-wide `_hp_bar()` the Party section already uses for its own "quick glance" rows.
  - **A real staleness bug found and fixed while wiring this up, not by running anything in production first.** `DungeonMasterApp.npcs` was previously only ever set once, from `state_sync` - a live `npc_update` broadcast only ever appended a fresh dim log line, never actually updated the `self.npcs` dict itself. Anything that read `self.npcs` after the first live NPC change during a session would have seen stale data (nothing in this codebase happened to read it that way yet, so this was a latent bug, not an observed one - but a real one, caught before it could bite). Fixed: `_handle()`'s `npc_update` branch now updates `self.npcs[name]` and calls `refresh_sheet_widgets()`, in addition to (not instead of) the existing log line - the log stays a narrative "something happened" beat, the panel becomes the always-current view, the same split `character_update`/`system_message` already establish for a player's own sheet.
  - **A real, deliberate difference from the player-only `DYING`/`STABLE`/`DEAD` trio, not a lazily-reused label.** An NPC dies outright at 0 HP under real 5e's own rule for ordinary monsters (see "Death saves," below) - no death-saves, no stabilize concept at all - so `_npc_line()` shows `(defeated)` at 0 HP instead of calling `_death_status_label()`, which would misleadingly imply a monster could still come back the way a player character's own `STABLE`/`DYING` states can.
  - **Additive, not a replacement** - the dim log line (`_npc_status_line()`) is unchanged and still writes on every `npc_update`; the panel is a second, persistent view alongside it, not instead of it.
  - **8 new tests**: `tests/test_client_app.py` gained 7 (`_npc_line()`'s defeated label at 0 HP, its absence above 0 HP, disposition/conditions rendering; a `state_sync` carrying real `npcs` renders them in the panel immediately on join; a live `npc_update` refreshes the panel, and a second update for the same NPC replaces its entry rather than duplicating it - the exact staleness bug this feature fixes, confirmed via a direct regression test). 1 new real end-to-end test (`tests/test_transport_e2e.py`): the same goblin takes damage across two separate real turns over an actual websocket session, confirming the panel shows exactly one up-to-date entry (`HP 0/7`, `(defeated)`) after the second turn, not two stale ones. Full suite: 283 passing (same 2 pre-existing root-sandbox `test_persistence.py` failures, unrelated).

- **Ability Score Improvements on level-up (2026-08-08), the second pick off the same brainstorm - closing the "HP grows but stats never do" gap that entry itself named.** A level-10 fighter previously had the exact same ability scores as it did at level 1, a growing staleness gap the more the leveling system actually gets played.
  - **Real 5e's own baseline progression** (`server/engine.py`'s `ASI_LEVELS`: 4, 8, 12, 16, 19) - a loop over every ASI level actually crossed between the character's level before and after `gain_xp()`, the same "one big award can cross more than one threshold" handling `gain_xp()`'s own level-up loop already has for HP, extended here rather than duplicated. Subclass-specific extra ASIs (Fighter's own 6/14 in the full rules) aren't modeled - Oracle has no subclass system at all.
  - **Deterministic, not a player choice - the same "no allocation system yet" scope `_generate_stats`'s initial array already has.** A new `_apply_ability_score_improvements()` always targets the class's own top `CLASS_ABILITY_PRIORITY` entry (already used for initial stat generation), +2, capped at the real 20 ceiling - falling through to the next-priority ability if the top one is already capped, rather than wasting a real improvement silently. Real 5e's alternative to an ASI (a feat instead) isn't modeled - Oracle has no feat system at all.
  - **AC deliberately doesn't recompute**, even when the improved ability is DEX (a rogue's own top priority) - the exact same already-accepted "AC doesn't recompute if stats/inventory change after character creation" simplification the Structured Equipment entry already established for gear changes, extended to cover this rather than treated as a new, separate gap.
  - **Announcement folded into the existing XP/level-up `system_message`**, not a new one - `"{name}'s STR increases!"` (or `"...STR and CON increase!"` for two distinct abilities in one award) appended the same way `"reaches level N!"` already is. A new shared `_asi_announcement()` builds this text once, used by both the DM's own `tool_result` and the real player-facing broadcast, so the two can't drift apart the way two independent string-building call sites could. **Deduplicates by design**: crossing two ASI levels in one huge XP award (rare, but possible) can improve the same ability twice - the text names it once ("STR increases!") rather than the grammatically awkward "STR and STR increase!", trusting the sheet's own real number as the actual source of truth for magnitude.
  - **17 new tests**: `tests/test_engine.py` gained 15 (a real level-4 crossing bumps the top-priority ability and broadcasts the right announcement; level 3 - not an ASI level - leaves stats untouched; falling through to the next-priority ability when the top one is capped; never exceeding the real 20 ceiling; crossing two ASI thresholds in one huge award applies both and dedupes the announcement; no ASI at all with an unrecognized class; `_apply_ability_score_improvements()` and `_asi_announcement()` tested directly - no-op cases, empty/singular/plural/deduped text). 1 new real end-to-end test (`tests/test_transport_e2e.py`): a real fighter joins with its real Standard-Array STR (15), a real kill worth exactly the level-4 XP threshold (2700) crosses it over an actual websocket, and both the real system_message and the real re-rendered sheet panel show STR 17 - not directly-constructed test dicts. **A real test-infrastructure gap found and fixed while writing that e2e test, not a production bug**: the RichLog widget wraps long lines, which split "STR increases!" across two wrapped lines with a newline in between - a naive single-substring `_log_text()` check failed even though the real text was genuinely present; fixed by checking "STR" and "increases" as two separate substrings instead of one combined phrase.
  - **Deliberately not built**: player-chosen ASI allocation (matches the same "no allocation system yet" scope every ability-score generation step in this project already has) and feats as an ASI alternative (no feat system at all).

- **Skill proficiencies (2026-08-08), closing the second of the two gaps `roll_kind` was explicitly built to unblock - the last easy item from the original mechanics brainstorm.** Real 5e skill checks, with a real per-class proficiency bonus, not just a bare ability-modifier roll.
  - **`request_roll` gained an optional `skill` field** (all 18 real SRD skills, `AnthropicNarrator` only, same gating `ability`/`weapon`/`roll_kind` already have) that resolves three things automatically from one name: the skill's real governing ability (`SKILL_ABILITIES`, moved to `server/state.py` specifically so `server/narrator.py`'s tool schema could import it without a circular import - `narrator.py` already imports from `state.py`, and `engine.py` already imports from `narrator.py`), whether the acting character is proficient (`CLASS_SKILL_PROFICIENCIES`, `server/engine.py`), and - if proficient - their real level-scaled bonus (`CharacterSheet.proficiency_bonus`, a new computed field mirroring `stat_modifiers`'s own "don't rely on the LLM for arithmetic" reasoning). The DM only needs to pass `skill` for a fully correct check, not three separate fields - an explicit `ability` still wins if given anyway, the same "explicit override beats an automatic default" priority `_xp_for_npc`'s own explicit-xp-beats-CR-lookup already establishes.
  - **A skill check is, definitionally, a "check"** - naming `skill` defaults `roll_kind` to `"check"` automatically (only when the DM didn't already say otherwise), so it also gets `roll_kind`'s own per-condition disadvantage scoping for free - the exact synergy anticipated when `roll_kind` shipped.
  - **Deterministic per-class proficiencies, the same "no allocation system yet" scope every other stat-generation step here already has.** Real 5e lets a player choose (2 skills for most classes, 4 for rogue) from a class's own longer list; `CLASS_SKILL_PROFICIENCIES` picks a fixed, thematically sensible subset from that real list instead of modeling the choice itself - the same simplification `STANDARD_ARRAY`'s fixed assignment already makes for ability scores. A blank/unrecognized class has no entry and is proficient in nothing.
  - **Reported on `dice_result` as `skill`/`proficient`/`proficiency_bonus`** and rendered as a `(Stealth)` or `(Athletics, +2 proficiency)` tag, the same transparency convention every other roll tag already established - `proficiency_bonus` only included when actually proficient, the same "don't carry an always-zero field" convention `roll_kind`/`disadvantage` already follow.
  - **26 new tests**: `tests/test_state.py` gained 3 (`proficiency_bonus_for_level()`'s real table at several reference points, the computed field tracking a character's level, inclusion in `model_dump()`); `tests/test_engine.py` gained 5 (a proficient skill adds the real bonus and auto-resolves its ability, a non-proficient skill still auto-resolves the ability but adds no bonus, naming a skill defaults `roll_kind` to "check" and gets real disadvantage scoping from it, an unrecognized skill is a graceful no-op with no ability auto-resolved either, an explicit `ability` overrides the skill-derived one while proficiency itself is unaffected); `tests/test_client_app.py` gained 2 (the proficient and non-proficient tag renderings). 1 new real end-to-end test (`tests/test_transport_e2e.py`): a real fighter joins over an actual websocket, and a DM-requested roll naming only `"skill": "athletics"` - no `ability`, no `roll_kind` - resolves the real STR modifier and real proficiency bonus together, confirmed against the real rendered log line.
  - **Deliberately not built**: player-chosen proficiency allocation, and proficiency's other real 5e uses (saving throws, tool checks) - `CharacterSheet.proficiency_bonus` is a real, correct number already available for either, but nothing wires it into saves or tool checks yet.

- **Formal initiative (2026-08-08), closing the last still-open item from the original mechanics brainstorm - the one item that had explicitly waited on a real design conversation rather than a guess.** Real 5e initiative, with several real open questions (does it replace the session-wide turn queue or only apply during combat, when does "combat" start/end, do NPCs get real turn slots) resolved deliberately before any code, not discovered mid-implementation.
  - **Explicit, player-triggered start/end - not detected from narration.** New `start_combat`/`end_combat` events, symmetric with `start_session`'s own precedent (any joined player, no host/GM role, idempotent - a second `start_combat` while already in combat, or `end_combat` while not, is a silent no-op). Deliberately not auto-detected from what the DM narrates: this project's own tool-call reliability investigation found the DM model can't be trusted to reliably notice and act on state changes on its own, and "did combat just start" is exactly that kind of judgment call - a player saying so is the one signal that's actually reliable. This also directly answered the "does it replace the whole-session queue" question: yes, but only for combat's duration, matching Oracle's own existing "one consistent flow, not two divergent ones" precedent (docs/protocol.md's pre-game-lobby reasoning) rather than running a second parallel turn-tracking system.
  - **`Session.pre_combat_turn_order`, a temporary snapshot, not a second persistent "join order" field.** Captured the instant combat begins (still plain join order at that point, since combat hasn't started yet) and restored - plus anyone who joined mid-combat, appended at the end - when combat ends. A new field defaulting to `None`, only ever consulted while genuinely in combat, so it needed no backward-compatibility handling for sessions saved before this shipped - a real, deliberate alternative chosen specifically to avoid the migration concern a permanently-tracked separate join-order field would have created for existing real `sessions/*.json` saves.
  - **A real 1d20 + DEX modifier per participant, real 5e's own formula** (`server/dice.py`'s `roll()`, the same primitive every other roll here already uses) - for every present player *and* every currently-tracked NPC. Oracle already had everything this needed: real DEX modifiers for players (Ability scores) and for NPCs matched against a known SRD monster (stats copied on introduction) - no new data required. A participant with no stats at all gets a modifier of 0, the same fallback every other stat-dependent mechanic here already uses. Ties broken by the higher DEX modifier (real 5e's own tiebreak); beyond that, Python's stable sort preserves original order - deterministic, not random, matching real 5e's own "DM decides" for a tie that persists past DEX.
  - **NPCs are announced in the rolled order but never occupy a real `turn_order` slot - the deliberate answer to the biggest open question, resolved as the smaller first slice rather than the larger one.** Giving NPCs real mechanical turn slots would need a genuinely new engine mechanism - an autonomous "it's the goblin's turn" step with no player input at all, nothing like it exists anywhere in this project today (the closest precedent, the opening scene's synthetic turn, still exists to serve a real player, not to run turns with nobody at the wheel). Scoped out as a real, bigger, separate follow-up rather than guessed at or silently smaller-scoped without saying so.
  - **Client**: two new `SessionScreen` commands, `/combat start`/`/combat end` - each a thin envelope send, no new rendering logic needed since the resulting `system_message` announcement and `turn_prompt` already flow through existing generic handling.
  - **14 new tests**: `tests/test_engine.py` gained 11 (initiative genuinely reorders players by roll+DEX-modifier, the announcement names everyone with their roll, NPCs named in the announcement but excluded from `turn_order`, the real DEX-modifier-0 fallback for an NPC with no matched SRD monster, `start_combat`/`end_combat` both idempotent, a defensive no-op with no players joined, exemption from turn order, `end_combat` restoring pre-combat order with a mid-combat joiner correctly appended at the end not lost, `advance_turn()` genuinely cycling through the new order); `tests/test_client_app.py` gained 2 (`/combat start`/`/combat end` send the correct bare envelopes). 1 new real end-to-end test (`tests/test_transport_e2e.py`): two real clients join in one order, a real `/combat start` reorders `turn_order` by a real DEX-based roll with a genuine tie broken correctly, both real clients see the identical broadcast announcement, and the resulting `turn_prompt` genuinely reflects the new order - confirmed against real server-side `Session` state, not just the rendered log.
  - **Deliberately not built**: real NPC turn slots/autonomy (see above), and re-rolling or inserting a latecomer into the *middle* of an already-rolled order rather than appending them at the end. The client-visible in-combat indicator/initiative-order display named here originally landed 2026-08-22 - see item 31 below.

- **Spellcasting for wizard/cleric (2026-08-08), the biggest remaining gap between classes as *classes*, closed by a real design conversation first - it touched more genuinely open questions than any prior mechanic here, so guessing at a slice risked getting the shape wrong rather than just the scope.**
  - **`server/rules/srd.json`'s spell data expanded from 5 to 10 entries** - real SRD 5.1 cantrips (`fire_bolt`, `ray_of_frost`, `sacred_flame`, `guidance`, previously zero cantrips existed at all) and a 1st-level spell (`bless`), every spell tagged with a new `classes` field. A new `spell_slots_by_level` table (the SRD's own real full-caster progression, levels 1-20) included in full even though Oracle's spell list only reaches 3rd level today - the same "works correctly before content grows to match" reasoning `xp_by_level` already established. `ATTRIBUTION.md` updated to note the expanded table.
  - **`known_spells`, a fixed deterministic list assigned once at creation (`CLASS_KNOWN_SPELLS`), not real 5e's daily-prepared-from-a-larger-list mechanic** - the same "no player-chosen allocation yet" scope every other stat-generation step here already has (`STANDARD_ARRAY`, `CLASS_SKILL_PROFICIENCIES`, ASI's own automatic-target choice). A leveled spell above what a character can currently cast (fireball at level 1, no 3rd-level slot until level 5) is still "known" - no special-casing, the slot check at cast time is the only real gate.
  - **`spell_slots`/`max_spell_slots`, plain stored fields, not computed** - real resources spent and restored constantly (casting, resting via `update_character`'s existing `rest` field - the hook point already existed, no new plumbing needed there; leveling up). On level-up, slots grow by the real delta between old and new max, not a full reset - the same "grants more, isn't a free rest" reasoning HP growth already follows, extended to a resource that can also be partially spent already.
  - **`spell_save_dc`, a new computed field** - real 5e's own formula (8 + proficiency bonus + spellcasting ability modifier), the same "don't rely on the LLM for arithmetic" reasoning `stat_modifiers`/`proficiency_bonus` already follow. `None` for a non-caster, a real "not applicable" signal.
  - **Two independent, composable fields on the two tools that already exist - no new tool, no new event type.** `update_character` gained `cast_spell` (deterministically consumes a slot, or refuses with a clear reason - not known, no slot left); `request_roll` gained `spell` (auto-resolves a real attack-shaped spell's damage die/type, ability, and proficiency bonus - always added for a spell attack, unlike a skill check's conditional proficiency). Neither depends on the other.
  - **A real, deliberate scope boundary resolved before writing code, not discovered mid-implementation: `request_roll`'s `spell` field only ever applies to attack-roll-shaped spells.** A save-based spell (`sacred_flame`, `fireball`) has no roll of the *caster's* own to make - only the target saves, and `request_roll` has never rolled on behalf of an NPC or another player. Rather than building that new capability, the caster's real `spell_save_dc` is simply always available on their own sheet for the DM to narrate against directly - resolved this way specifically because it sidesteps a structurally new "roll for someone else" mechanism this slice didn't need.
  - **Concentration is not modeled** - a real, nontrivial mechanic (one active spell, broken by a CON save on damage) deliberately deferred, the same way death saves deferred crit-doubling and conditions deferred stunned's full effects.
  - **Client**: `CharacterSheetPanel` gained a "Spells" section (own sheet only - `spell_save_dc`/`known_spells`/`spell_slots` are all private, the same boundary `stats`/`xp` already have) showing the real save DC, slot counts, and the known-spell list (no cantrip/leveled distinction client-side - the client has no SRD spell data of its own). `_dice_result_line()` gained a `(Fire Bolt, +2 proficiency)` tag, always including proficiency (never conditional like `skill`'s tag), matching the real rule that a spell attack always gets it.
  - **A real slug-normalization helper made public, not duplicated.** `server/rules/__init__.py`'s private `_slug()` became a public `slug()` - `cast_spell`'s known-spells check needed the exact same name normalization `RulesIndex.get_entry()` already used internally, and a second independent implementation could have drifted from it.
  - **25 new tests**: `tests/test_state.py` gained 5 (`spell_save_dc` for a real caster, scaling with proficiency bonus, `None` for a non-caster and a blank class, `known_spells`/`spell_slots` defaulting to empty); `tests/test_engine.py` gained 15 (real known-spells/slots for wizard and cleric, no spells for a non-caster, `RulesIndex.spell_slots_by_level()` at a real level and an out-of-range one, `cast_spell` consuming a real slot and broadcasting `character_update`, a cantrip costing nothing, an unrecognized spell name and a not-known spell both gracefully refusing, no slots remaining refusing, `cast_spell` never applying to an NPC target, level-up growing slots by the real delta, `request_roll`'s `spell` field resolving damage/ability/proficiency/`roll_kind` automatically, a non-attack spell being a graceful no-op, an explicit `ability` overriding the default); `tests/test_client_app.py` gained 4 (the spell/proficiency tag, the Spells section rendering for a caster and its absence for a non-caster, spells never appearing in the redacted Party view). 1 new real end-to-end test (`tests/test_transport_e2e.py`): a real wizard casts a real leveled spell (a real slot genuinely spent, confirmed against both server state and the re-rendered sheet panel) and rolls a real cantrip attack (real damage/ability/proficiency resolved) in the same turn over an actual websocket. **A real test-authoring bug caught while writing that e2e test, not a production bug**: the first draft tried to assert slot consumption on Fire Bolt, which is a cantrip and correctly costs no slot at all - fixed by casting a real leveled spell (Magic Missile) for that half of the test instead.
  - **Deliberately not built**: player-chosen spell preparation/allocation, concentration (see above), upcasting (the SRD's own `at_higher_levels` text exists in the data but isn't read), and wiring `spell_save_dc`/`proficiency_bonus` into saving throws (real 5e uses proficiency there too, not modeled here).
- **A real, long-standing multi-session bug, found while setting up a second machine for live GPU dry-run testing (2026-08-09).** `server/main.py` loaded exactly one `Session` at process startup, keyed off a `SESSION_ID` env var, and `Transport` held exactly one `GameEngine` for the whole process's lifetime - every connection joined that same game no matter what `session_id` it actually sent. The client's own welcome screen has had a "Session ID (blank for default)" field the whole time (`client/app.py`), so this was a real, reachable bug: two people picking different Session IDs on purpose, expecting separate games, would have silently ended up in the same one instead. Caught live, not in a test - a first dry-run script deliberately used a `dry-run-test` session_id to avoid touching real save data, and still joined the real persisted `default` session instead, because the running server ignored the envelope's session_id entirely.
  - **Fixed**: `Transport` (`server/transport.py`) now keeps a `session_id -> GameEngine` dict instead of a single engine, creating one lazily on that session's first `join_session` and reusing it after. `EngineFactory`'s signature gained a leading `session_id` parameter; `server/main.py`'s factory now does `store.load(session_id) or Session(session_id=session_id)` per session instead of once at boot, so `SESSION_ID` as a startup env var is gone entirely (removed from `.env.example`) - which session a game loads is now purely a per-connection client choice, matching what the welcome screen's field already implied. `dm` (the `NarratorBackend` instance) is shared across every session's engine - confirmed stateless (just a client/model/rules holder, no per-session data) so this is safe, not a corner cut.
  - **Broadcasts are now genuinely scoped per session, not just per-engine.** Connections are tracked as `player_id -> (session_id, connection)`; `Transport._broadcast` takes a `session_id` and only sends to that session's own connections - two unrelated games sharing one server process no longer see any of each other's traffic (chat, narration, turn prompts, anything).
  - **1 new regression test** (`tests/test_transport_e2e.py::test_two_different_session_ids_are_fully_isolated`): two real websocket clients join different session_ids on the same `Transport`, confirmed to get genuinely separate `Session` objects (`is not`, not just differently-rendered), and a chat broadcast in one never reaches the other.
  - **A second, pre-existing bug this fix immediately surfaced**: `test_lobby_transcript_command_saves_real_chat_over_a_real_websocket` mixed a TUI client (which defaults to the `"default"` session when its Session ID field is left blank, the same as any real player who doesn't type one) with a second, raw-websocket client explicitly joining a differently-named `"e2e-session-14"` - the two had never actually shared a session, only appeared to because the transport-level bug above silently merged every connection into one game regardless. Fixed by having the TUI client fill in the same Session ID, which is what the test's own naming already implied it meant to do.
  - Verified live end-to-end on the GPU-hosted server (`192.168.10.222`) against a real, isolated temporary process (scratch `SESSION_ID`/port, not the persistent deployment) - join, `start_session`, and a real player_action all round-tripped correctly with live streamed narration from `qwen2.5:7b`, and the temp session/process were torn down afterward without touching the real persisted session. All 424 tests pass.
- **TUI layout restructure and two real UX gaps, prompted directly by the owner's own first live playtest of a real client against the GPU server (2026-08-09).** Three things came out of that session: a layout complaint ("horizontal instead of vertical - main on top, player+tabs as a row underneath"), a report that a typed player name never showed up, and a question about whether a minimap existed at all.
  - **Layout: `LobbyScreen`/`SessionScreen` restructured from a side-by-side `Horizontal(sheet 30%, log 70%)` to a stacked layout** - the narrative log full-width on top (`height: 1fr`, the majority of the screen - it's the primary content, and `RichLog`'s own wrapping benefits from the extra width far more than the sheet's list-shaped content did), `CharacterSheetPanel` as a compact fixed-height band (`height: 12`) underneath, its own tab-label row now genuinely visible instead of hidden in a narrow side column. Each tab's `Static` content is now wrapped in its own `VerticalScroll` (previously bare) so a longer sheet (many known spells, a big inventory) scrolls internally within the compact band instead of needing the old tall column to stay fully visible. Chosen over two other mocked-up options (a "log-maximized" version hiding the sheet behind a keybind, and a minimal-diff version just narrowing the side column) after presenting all three as ASCII mockups for a real side-by-side comparison - see this session's own conversation log, not reproduced here.
  - **A real, found-live UX gap: reconnecting into an existing character silently discards whatever name was just typed.** `_on_join_session` (`server/engine.py`) only ever reads `player_name`/`character_class` from the payload inside its `is_new_character` branch - a genuine reconnect (most often triggered by a stale local `.player_id` file from a previous run, exactly what happened live here) keeps the existing character's name unconditionally, with zero indication to the player that their fresh input was ignored. **Fixed**: a new private (`send_to`, not broadcast - this is about what *this* player should understand about their own reconnect, not news for anyone else) `system_message` fires only when the typed name genuinely differs from the existing character's real name, e.g. *"Welcome back - you're reconnecting as your existing character 'Adventurer', not a new 'Player'."* No notice on a plain reconnect (blank name, or the same name typed again) - the same "only surface it when it'd actually be surprising" discipline the missed-change advisory already established. 2 new tests (`tests/test_engine.py`): the notice firing on a genuine mismatch, and firing on neither a blank nor a matching name.
  - **The "is there a minimap" question traced to a real, already-documented scope boundary, not a bug**: the Map tab (`CharacterSheetPanel._map_text`) only ever populates from `update_world`'s `add_location`/`connect_locations` fields, and that tool has been Anthropic-only since it shipped (`OLLAMA_TOOLS = [LOOKUP_RULE_TOOL, UPDATE_CHARACTER_TOOL]` - no `update_world` at all, the same reliability-driven gating `request_roll` already has). The GPU deployment runs `DM_BACKEND=ollama`, so the Map tab genuinely never populates there. Confirmed against real GPU session data, not assumed from reading the code alone.
  - **A real regression caught during this pass, unrelated to the layout work itself**: `scripts/generate_screenshots.py`'s `engine_factory` closures still used the pre-multi-session-fix two-argument signature (`(broadcast, send_to)`), so the script had been silently broken by the multi-session fix above (`Transport` now calls factories with a leading `session_id`) - never caught because this script isn't part of the pytest suite. Fixed alongside this pass since regenerating screenshots for the new layout required running it.
  - **The two genuinely-live screenshots (`opening-scene.svg`, `turn-in-progress.svg`) were finally recaptured for real**, closing a gap `scripts/generate_screenshots.py`'s own docstring had documented since it was written ("this environment has no live LLM access... must stay as they are until someone with real Ollama/Anthropic access recaptures them") - the GPU machine set up earlier this session is exactly that access. A new `scripts/generate_live_screenshots.py` (kept separate from the mocked-narration script, since it genuinely can't run without a live server behind it) drives a real client through a real join/start/turn against a real running server and captures both. **A real timing race caught writing it**: clicking "Start Adventure" immediately after `isinstance(app.screen, LobbyScreen)` became true - without an intervening `await pilot.pause()` - silently failed to reach the button, since `push_screen()`'s mounting is genuinely asynchronous (the exact same class of race `_handle()`'s own doc-comment already flags elsewhere in this codebase, just newly encountered in a different call site). No error surfaced anywhere - the symptom was simply `start_session` never arriving server-side (`started: false`, empty `log`, confirmed by reading the session file directly). Fixed by adding the same pause the working exploratory script had used all along.
  - All screenshots (`welcome-import.svg`, `xp-level-up.svg`, `spellcasting.svg`, `opening-scene.svg`, `turn-in-progress.svg`) regenerated against the new layout; README updated to match (captions, the Play section's layout description, and the stale "this dev environment has no live Ollama/Anthropic access" line). 426 tests pass.
- **`request_roll` for the Ollama backend, opt-in and off by default (`OLLAMA_ROLL_REQUESTS`) - a real revisit of a deliberate exclusion, prompted by a direct owner question after watching a rogue "sneak past the guard" turn resolve as pure narrated vibes with zero mechanics behind it (2026-08-09).** `request_roll` has been Anthropic-only since it shipped (item 2, above item 6's investigation) - reasoned around *native tool-calling*, where this session's own investigation found small local models already miss the one existing required call on most clearly-warranted turns, so a second required call seemed likely to compound that, not help. That reasoning predates structured output (item 6's seventh experiment), which sidesteps tool-selection entirely by changing "decide whether to call this" into "fill in a field" - the same shape plausibly applies to deciding a roll is needed, not just to `update_character`.
  - **The real wrinkle native tool-calling doesn't have**: a single JSON response can't both decide a roll is needed *and* write narration guaranteed to match its real outcome, since the roll hasn't happened yet when that response is generated - a fabricated "you slip past unnoticed" sitting next to a roll that, moments later, actually comes back a failure. Solved with two model calls, not one, but *only* on turns the model itself judges genuinely uncertain: a first call decides `roll_requested`/`roll_skill`/`roll_ability`/`roll_dc`/`roll_kind` (narration/mechanical_change here are placeholders, discarded if a roll fires); the engine's existing `request_roll` closure - already backend-agnostic, already passed into every `narrate()` call, previously just dropped on the floor by `OllamaNarrator` - rolls for real; a second call writes the actual narration and mechanical outcome already knowing that real result. Most turns still cost exactly one call, unchanged.
  - **Not validated at the same rigor as structured output's own 66%/29% comparison** - that was a real 5-repeat harness study; this is three live 5-turn spot-checks against `qwen2.5:7b` on the GPU box (2026-08-09), and the honest numbers argued against shipping it as anyone's default: 6/6 obviously-certain actions correctly triggered no roll (zero false positives across every run - a genuinely good property), but genuinely-uncertain ones only triggered a roll 7/9 times, with real run-to-run variance on the *same* three turns (3/3, then 1/3, then 3/3) - the identical "doesn't reproduce" pattern item 6's own investigation already documented for native tool-calling decisions, just now showing up in a different decision shape. Field completeness when a roll did fire was weaker still: only 1 of those 7 had a fully correct `skill`+`ability`+`dc` (a stealth check, done right - real DEX, real proficiency, real DC 15); the rest either picked a semantically wrong skill (a lock-picking attempt tagged `deception` instead of `sleight_of_hand`) or left `skill`/`ability` blank entirely (including every melee attack across all three runs - none once got a real STR/DEX modifier attached), which resolves server-side as a bare unmodified `1d20`, not the stat-backed roll the feature is meant to provide. One prompt tightening (explicit skill-selection examples, "always set roll_kind") was tried between runs 1 and 2 and didn't obviously fix anything - the same "measure, don't assume a tweak helped" discipline the missed-change heuristic and the numbered-list narration fix both already established.
  - **Shipped anyway, deliberately scoped as opt-in rather than shelved** - the mechanism is real, tested (23 unit tests, `tests/test_narrator_ollama.py`), and demonstrably *can* produce a fully correct, stat-backed roll; it's just not yet reliable enough to be the default experience the way structured output itself earned. `OllamaNarrator` gained a `roll_requests` constructor flag (default `False`, independent of `structured_output`); `create_ollama_narrator()` reads a new `OLLAMA_ROLL_REQUESTS` env var (default off, same "1"/"true"/"yes" opt-in convention `OLLAMA_STRUCTURED_OUTPUT` already uses). Off by default means byte-identical behavior to before this existed - the base `STRUCTURED_OUTPUT_SCHEMA`/`STRUCTURED_OUTPUT_SYSTEM_PROMPT` are unchanged from what shipped in item 6; the roll-aware variants are new, separately-named constants (`STRUCTURED_OUTPUT_ROLL_SCHEMA`/`_ROLL_SYSTEM_PROMPT`) used only when the flag is on.
  - **6 new tests** on top of the 5 covering the mechanism itself (`tests/test_narrator_ollama.py`, 29 in the file total now): the flag genuinely gates behavior (off ignores `roll_requested` entirely even if a payload somehow set it; on uses the real roll schema/prompt), plus `create_ollama_narrator()`'s env var wiring (default, explicit opt-in, case-insensitivity) mirroring `OLLAMA_STRUCTURED_OUTPUT`'s own test coverage. 437 tests pass project-wide.
  - **A real follow-up, not assumed done here**: a proper `--repeat`-driven study (matching item 6's own bar) against `scripts/live_reliability_check.py` or a purpose-built roll-scenario variant, before this could honestly move from "opt-in experiment" to "default." Worth trying a further-tightened prompt or an example-driven schema description first, given the one tweak tried here wasn't conclusive either way on a 3-run sample.
- **`update_world` for the Ollama backend, opt-in and off by default (`OLLAMA_WORLD_UPDATES`) - the natural next extension of the same pattern, prompted by a direct owner question about what would actually help the DM be a good host and immerse players (2026-08-09).** `update_world` (location, objectives, campaign continuity) has been Anthropic-only since it shipped, same reasoning as `request_roll`. Unlike a roll, a world-state change has no ordering problem - it's just a consequence of the turn's outcome, decided alongside `mechanical_change`, so this needed no two-pass mechanism: world fields (`world_change`/`location`/`add_objective`/`complete_objective`/`add_location` - a small slice of the real tool, no `summary`/`expire_objective`/`fail_objective`/`remove_objective`/`set_flag`/`clear_flag`/`connect_locations`) are simply additive on top of whichever schema is already selected, including the roll-deciding/follow-up pair when `OLLAMA_ROLL_REQUESTS` is also on (independent flags - either can be on without the other). `add_location` doubles as a fix for the earlier-diagnosed "no minimap" gap (the Map tab only ever populates via `add_location`/`connect_locations`, which the Ollama deployment has never had access to).
  - **Live spot-checked the same way `OLLAMA_ROLL_REQUESTS` was (three runs, five turns each, `qwen2.5:7b` on the GPU box) - and the honest numbers are weaker, not stronger.** A running scenario (arrive in town → an NPC hands over a real quest hook → a neutral turn → travel to the quest location → resolve it) found the model reliably caught the very *first* scene-establishing change (3/3 runs correctly set `location`+`add_objective` on turn 1) but essentially stopped updating world state for the rest of the same growing conversation - only 1 of the remaining 11 opportunities across all three runs fired at all, including two turns per run (arriving at the mill, finding the described amulet) whose narration explicitly and unambiguously resolved a location change or objective completion that never got recorded. Total: **4/12 real changes correctly detected (33%)**, distinctly worse than `request_roll`'s already-imperfect 78%. The one property that did hold: **zero false positives across all 15 turns** - it never invents a change that isn't there, same as the roll feature's own clean precision record.
  - **A real, if speculative, pattern worth naming**: the failure isn't random miss-anywhere noise, it's concentrated - reliable on the first turn of a session, unreliable on every turn after in the same conversation. Consistent with (though not proven to be caused by) growing conversation history crowding out a "also remember to check this" side-decision in favor of narration quality - the same shape of problem this project's entire tool-call reliability investigation (item 6) has documented before, just showing up in a third decision (was there a change to the *world*, not just the acting character) rather than the first two (was there a change to a *sheet*, is a *roll* needed).
  - **Shipped opt-in anyway, same posture as `request_roll`** - real, tested (10 new tests, 39 total in `tests/test_narrator_ollama.py`), demonstrably correct when it does fire, just not reliable enough across a whole session to be anyone's default. `OllamaNarrator` gained a `world_updates` constructor flag (default `False`); `create_ollama_narrator()` reads `OLLAMA_WORLD_UPDATES` (default off, same convention). 447 tests pass project-wide.
  - **Not pursued further this session, deliberately** - two consecutive "real signal, real reliability gap, shipped opt-in" experiments in one day is enough new unproven surface area for now; a third round of prompt-tuning (or the same `--repeat`-harness validation both `request_roll` and this still need) is a genuine next step, not something to rush through without checking in on direction first.
- **A Solo/Multiplayer choice on the welcome screen, closing a real recurring point of confusion caught live: "every time I test I submit an action and it says I must wait" (2026-08-09/10).** Root cause, confirmed against the real GPU deployment's session data: `default` had accumulated two orphaned test characters (`Elowen`, `Adventurer`) from earlier sessions, nobody actively playing either, so `current_turn` was permanently stuck on the first one - any fresh join (or a stale local `.player_id` reconnect) landed behind her in `turn_order` with a wait that could never actually end. The client's Session ID field defaulting to blank/`"default"` made this trivially easy to hit again on every retest, even after being told once to type a fresh Session ID each time.
  - **Fixed at the UI level, not just by clearing the stale data (which would only have deferred the same problem to next time)**: `WelcomeScreen` (`client/app.py`) gained a `RadioSet` - **Solo** (default-selected) or **Multiplayer**. Solo hides the Session ID field entirely and derives `session_id` as `f"solo-{player_id}"` - not a fresh random id each join (that would prevent ever resuming a solo game), but stable per local client identity (the same `.player_id` file that already gives a returning character continuity) and guaranteed collision-free with `default` or anyone else's session, since `player_id` is already a random per-client uuid. A player who's always alone in their own game is now always `turn_order[0]` - the class of bug this session kept hitting can't happen in Solo mode at all, structurally, not just by convention. Multiplayer reveals the Session ID field exactly as before (blank still means `"default"`), now explicitly labeled "same ID as whoever you're playing with."
  - **A genuinely useful side effect, not by design but real**: since a fresh session_id always makes a `player_id` "new" server-side (`is_new_character` is evaluated per-session in `_on_join_session`, not globally), picking Solo also sidesteps the *other* found-live bug from earlier the same day (a stale `.player_id` silently reconnecting into an old identity, discarding whatever name was just typed) - Solo always gets you a genuinely fresh character now, no `.player_id` deletion needed. The one rough edge: the class picker is still gated on the *client's own* `is_new_character` flag (decided once at `client/main.py` startup from local file presence, not session-aware), so a stale `.player_id` choosing Solo still won't see the class field even though server-side it's a real new character - a known, minor, pre-existing limitation, not introduced here.
  - `DungeonMasterApp` gained a public `player_id` property (previously `_player_id`-only) so `WelcomeScreen` can read it without reaching past the class's own privacy convention. `scripts/generate_live_screenshots.py` simplified to rely on Solo's own derived session_id instead of its previous manual random-session-id workaround, now redundant. **A real regression caught by the existing test suite, not found live**: three real-websocket, two-real-TUI-client e2e tests (`tests/test_transport_e2e.py`) had always relied on the *old* default (blank Session ID = shared `"default"`) to put both clients in the same game - Solo becoming the default would have silently split them into two isolated one-player games instead. Fixed by having those tests explicitly select Multiplayer and type a shared Session ID, the same way a real pair of friends now would.
  - 6 new tests (`tests/test_client_app.py`): Solo selected by default with the session field hidden, selecting Multiplayer reveals it, Solo's derived session_id, its stability across separate runs with the same player_id, Multiplayer with a typed id, Multiplayer falling back to `"default"` when left blank. 453 tests pass project-wide. Live-verified against the real GPU-hosted server: joined via Solo with zero session-id interaction, got `"Your turn."` immediately.
- **A real setting: Aetherfall, the Veil, and Ashwren the Warden - an isekai default premise, researched and brainstormed with the owner before building anything (2026-08-10).** Every fresh campaign used to open on a generic, fully model-improvised scene, with no consistent world behind it beyond whatever the DM happened to invent that particular turn - a different setting every session, and nothing stopping it from contradicting itself within the *same* session once the opening scene scrolled out of the rolling history window (`MAX_HISTORY_MESSAGES`, `server/state.py`). Prompted by a direct owner ask: players transported to another world after a near-death moment, greeted by a guardian who explains the who/what/where/when/why, free-roam from there but grounded in something real.
  - **Research first, not just invention.** A quick survey of comparable projects found the closest open-source sibling, [NarrativeEngine-P](https://github.com/Sagesheep/NarrativeEngine-P) (also a self-hosted, any-LLM solo campaign engine) - its "World Lore Builder" (a structured, static pre-game setting definition, distinct from what actually happens during play) directly validated the shape of what Oracle needed and didn't have. Its "World Arc"/timeskip machinery was deliberately *not* borrowed - built assuming a strong model doing heavy in-context judgment, the opposite of this project's own hard-won lesson (`OLLAMA_WORLD_UPDATES` landed at 33% recall the same day) that small local models need determinism, not more judgment calls.
  - **New `server/lore` package**, mirroring `server/rules`'s own `__init__.py` + bundled-data-file structure exactly: a `WorldBible` pydantic model (`setting_name`, `guardian`, `regions`, `central_tension`, a structured `who_what_where_when_why`, `tone_guidance`) plus a shipped default setting (`server/lore/isekai.json`) - Aetherfall, a world at the edge of the Veil between realities, Ashwren (the Warden of the Veil) as its guardian, four named regions, and a real central tension (the way home exists and is known, but costs something real to take - not a hidden macguffin hunt).
  - **Two integration points, both deterministic - data composed by the engine, the DM only narrates around it, the same split every other mechanic in this project already uses:**
    - `WorldBible.system_prompt_block()` is appended to *every* `NarratorBackend`'s system prompt - `AnthropicNarrator` and all four `OllamaNarrator` prompt variants (tool-calling, structured, roll-aware, follow-up) - computed once per narrator instance, so it's present on every single turn regardless of the rolling history window's size. This is the actual fix for "the world reinvents itself," not just a nicer opening line.
    - `WorldBible.opening_scene_prompt()` replaces the old generic "set an opening scene" cue for a genuinely new campaign's very first turn (`server/engine.py`'s `_on_start_session`) with the real near-death/transport/Guardian-greeting premise - the actual who/what/where/when/why facts supplied directly, not left for the model to invent (and potentially contradict later).
  - **A real, live-caught quality issue and a same-session fix**: the first live opening scene generated (`qwen2.5:7b`, GPU box) leaned heavily on quoting the injected facts almost verbatim rather than dramatizing them in the Guardian's own voice - coherent and on-brand, but noticeably flatter than this project's other narration. Tightened the prompt ("paraphrase... don't quote them verbatim or recite them as a list"), re-verified live: the second generation dramatized the same facts naturally in-character and even worked the central tension into a real hook unprompted ("The way home is always open, but at a cost. Are you ready to know the price?") - the same "measure a real generation, tweak, re-verify" discipline as the numbered-list narration fix and the missed-change heuristic elsewhere in this file, just turned around same-session instead of across sessions.
  - `GameEngine` and both narrators gained a `world_bible` constructor parameter (default: the bundled Aetherfall setting, loaded once) - swappable for an entirely different setting later without touching engine code, the same "load once, sensible default" precedent `rules` already established.
  - 17 new tests (`tests/test_lore.py`, plus engine/narrator coverage confirming the lore genuinely reaches both the system prompt and the opening scene, and that a custom `world_bible` override works). 460 tests pass project-wide. Live-verified twice against the real GPU-hosted model in an isolated worktree (not the persistent deployment) - the Guardian's identity and the who/what/where/when/why facts came through faithfully both times, and a direct follow-up question on turn 2 ("what is this place") recalled the same lore correctly without contradiction.
  - **Deliberately not built in this pass** (see conversation log for the fuller brainstorm, not reproduced here): the random-background-generator + near-death-scenario pre-game flow, and the class-recommendation survey - both real next steps, scoped but not started.
- **A random pre-Aetherfall origin for every new character, closing the "same generic greeting for everyone" gap the world bible above still had (2026-08-10).** Every character's opening scene was grounded in real setting facts, but *identical* ones - the Guardian's greeting never actually felt personal to who this specific character was before arriving. `server/lore/origins.json` (mirrors `isekai.json`'s own bundled-data pattern) adds curated background/trait/near-death-event tables; a new `Origin`/`OriginTable` pair plus `random_origin()` (plain `random.choice`, not the engine's own `dice.roll()` - a curated-table pick has no real probability distribution to get right, and it's mockable the same way `tests/test_engine.py` already mocks `server.dice.random.randint`).
  - `build_starting_character` generates one regardless of class choice - the near-death/transport premise applies to every character, not just ones who picked a recognized class - stored as `CharacterSheet`'s new `background` field, owner-only for free via the existing `model_dump()` spread (no new visibility wiring needed, same boundary `notes`/`stats` already have). `_on_start_session` passes a solo player's own background into `WorldBible.opening_scene_prompt`'s new `origin_detail` parameter; deliberately left blank for a multi-player start, since no single character's origin should be singled out over the others'. Client renders it on the Features & Notes tab, same "don't render the absent default" convention `notes`/`class_features` already follow.
  - 11 new tests (`server/lore`, engine join/opening-scene wiring, client rendering). 471 tests pass project-wide. Live-verified against the real GPU-hosted model in an isolated worktree: a cleric's opening scene had the Guardian directly reference her specific randomly-generated background ("a mechanic by trade, I sense") and trait ("can't resist helping a stranger even if it means risking your own life") - both real, paraphrased matches to that character's actual generated `Origin`, not the generic phrasing every character got before this.
  - **Still deliberately not built**: the class-recommendation survey (the other half of the original brainstorm) - a real next step, not started in this pass either.
- **A class-recommendation survey on the welcome screen - the last piece of the original isekai brainstorm (2026-08-10).** A "Not sure? Get a class recommendation" button (collapsed by default - not asked of everyone, matching the existing "recommend, don't require" tone `request_roll`/`world_updates`'s own opt-in flags already established) reveals three quick multiple-choice questions; the class with the most picks pre-fills `#class-input`. Purely a suggestion, same as the origin generator's own class-vs-recommendation split - answering never bypasses the field, only pre-fills it, still fully editable/clearable exactly as if typed by hand. Entirely client-side (`client/app.py`'s `CLASS_SURVEY_QUESTIONS`, `_recommend_class`), no server or LLM involvement, no new protocol - the same "just data + a plain function" scope discipline the SRD dataset's own class list already established.
  - **A real regression found and fixed while building this, not shipped and forgotten**: adding just one new visible `Button` widget to the welcome screen's default (collapsed) state pushed its total content one row past what a standard 80x24 terminal shows, silently sending `pilot.click("#join")` in **65 unrelated tests** out of the visible scrollable region - not a hypothetical, a real measured regression caught by running the suite, not assumed safe. Root-caused to two Textual defaults neither previously mattered at this screen's prior size: `Button` and `RadioSet` both render with `border: tall` (2 extra rows each) unless told otherwise. Fixed by making the toggle and every `RadioSet` on this screen genuinely compact (`border: none`) rather than working around it by inflating 65 tests' terminal size - the CSS bug was the real fix, not the symptom.
  - **A second, narrower real bug caught testing the toggle's own round-trip (collapse → expand → collapse again)**: revealing the survey shifts the scrollable welcome box's layout enough to trigger an animated auto-scroll, and a second click issued before that animation settles can land on the button's *stale* pre-scroll position - a `pilot.pause()` alone doesn't wait long enough for it; a real sleep does. Confirmed this is a scripted-back-to-back-click artifact, not a real user-facing bug (no real click cadence is that fast) - fixed in the test, not the app.
  - 9 new tests (`_recommend_class`'s tally/tie-break logic in isolation, the toggle's show/hide round-trip, answering pre-fills the field, and that the recommendation stays genuinely overridable end-to-end - `_join()` sends whatever `#class-input` actually contains, not whatever the survey computed). 477 tests pass project-wide. Live-verified against the real persistent GPU deployment: an all-rogue survey answer correctly recommended Rogue, joined successfully, and produced a real, correctly-built Rogue sheet (real stats, Shortbow/Leather Armor, Sneak Attack/Expertise/Thieves' Cant) with its own random `Origin` composing cleanly alongside it.
  - **Closes the original owner brainstorm's full scope**: random background generation, the near-death/transport/Guardian-greeting premise, a real grounded setting, and the class-recommendation survey are all shipped now (across this entry and the three before it). Not built, and not assumed to be needed yet: a broader stats-recommendation survey beyond class, and the two smaller non-mechanical ideas floated in the same brainstorm (a session-resume recap, a content-preference/safety-tools step) - real, scoped, genuinely open next steps, not silently dropped.
- **A private "story so far" recap for anyone joining a session already underway - one of the two smaller brainstorm ideas above, now built (2026-08-10).** A returning player reconnecting, and a brand-new player joining an in-progress multiplayer game (arguably needs it even more), both previously got nothing beyond whatever their own client silently reconstructs from `state_sync` - no scrolled-back log, no restated objective, no reminder of where they even are.
  - Entirely deterministic (`WorldState.summary`/`location`/`objectives`, `Session.log`), not an LLM call - always available instantly regardless of narrator reliability or latency, the same "engine composes it" discipline the opening-scene premise (`server/lore`) already established. Three-tier graceful fallback: `WorldState.summary` (only ever set by `update_world`, Anthropic-only/opt-in today - see item 6 - so frequently empty) → the most recent real narration line in the log → a bare acknowledgement. Always appends current location and active-only objectives when known. Skipped entirely while a session is still in its pre-game lobby - nothing has happened yet to recap.
  - **A real ordering bug caught before it ever shipped, the same class of race this file has already logged twice before at different call sites**: the recap must be sent *after* `state_sync`, not before - the client only leaves `WelcomeScreen` once it processes `state_sync` (`client/app.py`'s `_handle`), so a `system_message` arriving first has nowhere to render into yet and is silently dropped.
  - 12 new tests (6 engine-level covering gating/fallback-tiers/location/objectives and both the reconnect and brand-new-player-into-in-progress-game paths; 1 client-level that specifically locks in the real envelope *ordering*, not just the recap's content, so this exact race can't quietly regress). 484 tests pass project-wide. Live-verified against the real GPU-hosted server: disconnected mid-session, reconnected with the same identity, and the recap correctly appeared right after the replayed log tail - `"Welcome back. Last thing that happened: ..."`, truncated cleanly, matching the fallback path exactly since this Ollama-backed deployment has never called `update_world`.
- **A session-zero content-preference (tone) choice - the other smaller brainstorm idea, now built (2026-08-10).** A real tabletop practice (agreeing on tone/intensity before play starts) that Oracle never offered at all - every session got whatever tone the model happened to default to, with no way for a table to say "keep this lighter" or "don't hold back" up front.
  - `Session` (`server/state.py`) gained `content_preference: Literal["lighter", "standard", "intense"] = "standard"`, set once by whoever presses Start Adventure via a new compact `RadioSet` on `LobbyScreen` (`client/app.py`, same borderless-compact CSS fix `WelcomeScreen`'s own `RadioSet`s already needed), sent as part of the `start_session` payload. An unrecognized/missing value falls back to `"standard"` - the same graceful-miss convention every other name-based field in this file already follows, rather than a `ValidationError` on a malformed payload.
  - **Session-scoped, deliberately not baked into the narrator's system prompt** - unlike `WorldBible`, which is genuinely shared/global across every session one server process hosts (so belongs in the system prompt, computed once per narrator instance), `content_preference` can differ per session on the same process. Instead, a new `CONTENT_PREFERENCE_HINTS` dict (`server/engine.py`, alongside `CLASS_STARTING_EQUIPMENT`/`STANDARD_ARRAY`) is prepended to `action_text` on every turn in `_narrate_and_apply` whenever the session's choice isn't `"standard"` - `"standard"` has no entry at all, so the common/default case costs zero extra tokens, the same "durable, not a one-time mention" reasoning that keeps `WorldBible`'s own facts alive past the rolling history window. The hint never reaches the visible action log - `_on_player_action` already broadcasts the raw envelope text before `_narrate_and_apply` is even called, so prepending inside it only reaches the DM, never what players see themselves having typed.
  - 6 new tests (2 engine-level for payload validation/fallback, 2 for the hint actually reaching `narrate()`'s `action_text` when non-standard and staying absent for `"standard"`, 2 client-level for the selector defaulting to standard and a chosen preference reaching the payload). 489 tests pass project-wide. Live-verified against the real GPU-hosted model in an isolated worktree: an `"intense"` session correctly persisted `content_preference` server-side (confirmed on-disk in the session's own JSON) and completed a real combat turn cleanly end-to-end.
  - **Closes the full original owner brainstorm** - random background generation, the near-death/transport/Guardian-greeting premise, a real grounded setting, the class-recommendation survey, the session-resume recap, and this content-preference step are all shipped now. **Still open**: a broader stats-recommendation survey beyond class - real, scoped, not started.
- **An optional stat-priority override on the welcome screen - the broader stats survey, closing the very last item from the original isekai brainstorm (2026-08-10).** Ability scores were previously 100% derived from class (`CLASS_ABILITY_PRIORITY`, `server/engine.py`) with no way for a player to say otherwise - a real, named gap `_generate_stats`'s own docstring had flagged since the ability-score system first shipped ("Player-chosen stat allocation is real future work, not attempted here").
  - `_generate_stats(character_class, stat_priority=None)` now accepts an explicit ability-priority order and zips the SRD's real Standard Array against it instead of the class default, whenever `stat_priority` is exactly the 6 real ability keys once each - anything else (missing/duplicate/wrong length/wrong type) falls back to the class default, the same graceful-miss convention `character_class` itself already gets. `build_starting_character` and `_on_join_session` thread a new `join_session` payload field (`stat_priority`, a plain list like `["str", "con", "dex", "wis", "cha", "int"]`) through to it.
  - **Purely client-side and purely a suggestion, same posture as the class-recommendation survey**: a new `#stat-priority-input` on `WelcomeScreen` (`client/app.py`), blank by default - blank already means "use the stats recommended for my class," so there's nothing to pre-fill even after answering the class survey; only a player who wants a genuinely different spread needs to type anything.
  - **A real regression caught and fixed the same way the class-survey's own row-budget bug was, not assumed safe**: adding just one more field to the welcome screen's default (collapsed) state pushed `#join` one row behind the `Footer` at a standard 80x24 terminal, again sending scripted clicks out of the visible region - 66 of 92 client tests failed on the first run. Root-caused the same way as before (measuring `.region` directly, not guessing): `Input` also defaults to `border: tall` like `Button`/`RadioSet` already did. Fixed with the same "make the new widget genuinely compact" approach (`border: none`), plus reclaiming one more row by trimming `#mode-select`'s own margin - the one row this screen's layout could actually spare.
  - 4 new tests (2 engine-level for the override winning and the invalid-value fallback, 2 client-level for a typed priority reaching the payload and blank omitting it entirely). 493 tests pass project-wide. Live-verified against the real GPU-hosted model in an isolated worktree: an explicit str-primary override on a Rogue (normally dex-primary) correctly produced `str: 15, dex: 13` in the real synced character sheet, and a second join with no override correctly kept the Rogue's own `dex: 15` class default.
  - **Closes the original isekai brainstorm in full, no open items left**: random background generation, the near-death/transport/Guardian-greeting premise, a real grounded setting, the class-recommendation survey, the session-resume recap, the content-preference step, and now this stat-priority override.
- **An extended solo campaign dry-run against the real GPU-hosted model, deliberately including human quirks (2026-08-10).** A real 12-turn playthrough, driven through the actual `DungeonMasterApp`/`Pilot` (not a mocked narrator) against `qwen2.5:7b`, on the latest merged `main` in an isolated worktree - a fumbled/corrected name, uncorrected typos left in real actions, a fat-fingered `/rol` (missing a letter) that silently fell through to a real `player_action` instead of the dice-roll command, and a deliberate second-guess of the class-recommendation survey's own suggestion.
  - **Found a real, live gap, not a hypothetical one**: editing the survey-prefilled `#class-input` field (backspace old value, type new) landed the new text at the *start* of the field instead of replacing it, submitting a garbled `"clericrogue"` at Join. Oracle's own existing graceful-fallback behavior correctly avoided crashing - but silently built a blank, classless, stat-less character with **zero player-facing indication anything had gone wrong**. The character played the entire session that way before this was even noticed.
  - [x] **Fixed**: `_on_join_session` (`server/engine.py`) now sends a private warning (mirrors the existing import-failure warning exactly in shape/tone) whenever a player typed something for `character_class` that didn't resolve to a real class - `"'clericrogue' isn't a recognized class - starting without one (no starting stats, HP bonus, or kit). Recognized classes: fighter, wizard, rogue, cleric."` A genuinely blank field (the welcome screen's own explicit "blank to skip" option) is not treated as a mistake and stays silent, same as before.
  - 2 new tests (`tests/test_engine.py`): the warning fires and names the exact typed string for an unrecognized class, and confirms a genuinely blank class sends no warning at all. 500 tests pass project-wide. Live-verified against the real GPU-hosted server: reproduced the exact `"clericrogue"` scenario and confirmed the warning arrives correctly, then confirmed both a valid class and a blank class stay silent.
  - **Everything else through the full live session worked correctly**, incidentally re-confirming several recent features under real, sustained play rather than isolated single-turn checks: the persistent turn indicator (PR #48) stayed accurate through the whole session with no stale/blank states; the origin/world-bible lore stayed consistent and specific across every turn (the Guardian correctly referenced the character's own randomly-generated background); a manual `/roll` and a hidden-but-functional `/note` both worked correctly end-to-end; and `update_character` correctly tracked a real NPC's HP mid-encounter (`qwen2.5:7b`'s well-documented reliability gap for this exact call, item 4 below, didn't reproduce on this particular run - consistent with this project's own repeated finding that this call's success rate is real but inconsistent, not a hard 0%).
  - **A minor, non-actionable observation, not a bug**: the same fat-fingered `/rol 1d20 stealth` that fell through to a real `player_action` (instead of the dice-roll command) got a DM response that *narrated its own invented dice result* ("rolls 1d20 and gets a result of 14") rather than treating the text as plain dialogue - the model pattern-matching on dice-shaped text and improvising a fictional mechanical outcome. No real state changed (no real roll or update ever fired), so this is cosmetic, not a correctness issue - logged here as a genuinely interesting small-model behavior, not something to chase a fix for.
- **A second, open-ended "wander" campaign dry-run - no fixed plot, both experimental opt-in features on (`OLLAMA_ROLL_REQUESTS`, `OLLAMA_WORLD_UPDATES`) - and a real bug found and fixed as a direct result (2026-08-10).** Same live setup as the campaign above (real `DungeonMasterApp`/`Pilot`, real GPU-hosted `qwen2.5:7b`, isolated worktree), 15 turns of curiosity-driven free-roam instead of a directed story. Character creation went cleanly this time (class typed directly rather than re-editing a pre-filled field, avoiding the previous run's own mistake) - a real Rogue with real stats, auto-equipped gear, and real class features.
  - **Roll-requests fired correctly and repeatedly** (a genuine skill check investigating a shimmer, cutting off-path, the ambush) - consistent with this feature's own already-documented reliability. **World-updates never fired once across the entire session**, despite an unambiguous location name ("The Hollow March") being stated explicitly in the very first travel turn - a starker, freshly-reproduced data point consistent with (and worse than) the already-documented 33%-recall finding (item 6 below).
  - **A real, concrete bug found, not a hypothetical**: during the ambush, the DM's first `update_character` call for the encounter targeted `"Veil-Touched"` - not an NPC, but the acting character's own *condition string*, already sitting on her sheet from the transport premise (`server/lore`'s origin system applies it to every new character). This spawned a bogus phantom NPC with its own tracked HP and an "Alert" condition. The very next call targeted `"Ashwren"` - the Guardian, a real, legitimate lore character, but one who wasn't actually present in this scene at all - before a third call finally landed correctly on `cloaked_figure`.
  - **A real, demonstrated downstream consequence, not just a cosmetic log-line oddity**: `_on_start_combat` (`server/engine.py`) rolls initiative for every currently-tracked NPC with no scoping to what's actually present in the scene (a known, deliberately-named simplification - see its own docstring). The phantom `"Veil-Touched"` entry existed by the time `/combat start` was called, so it got swept into initiative and announced (`"Sable (16), Veil-Touched (14)"`) - while the real attacker, not yet tracked as an NPC at that point, was never in the fight's initiative order at all.
  - [x] **Fixed the root cause, not the symptom**: `apply_update`'s existing self-target guard (`server/engine.py`) already treated a target matching the acting character's own `player_id` or `name` as a misrouted self-reference rather than a new NPC (a live-found gap from earlier live testing, documented where that guard was added) - extended to also cover a target matching one of the character's own **current conditions**. Scoped to the acting character's own live condition list, not a fixed lookup table - a static list would need to know every condition any origin/narration could ever apply, and the coincidence of an unrelated real NPC sharing a name with a condition string is negligible.
  - **Deliberately not attempted**: the `"Ashwren"` mistarget (hitting a real, legitimate, already-known lore character who simply wasn't present in the scene) is a fundamentally different, harder problem - it has no deterministic signal to catch it against, unlike a condition string. Real scene-presence tracking (who's actually here right now) would be a genuinely new, much bigger feature, not a quick guard - explicitly out of scope for this fix, not silently dropped.
  - 2 new tests (`tests/test_engine.py`): the exact live scenario (a condition-name mistarget lands on the real sheet, not a phantom NPC) at the `apply_update` layer, and an end-to-end confirmation that `/combat start`'s own initiative announcement no longer includes the bogus name. 502 tests pass project-wide. Live smoke-tested against the real GPU-hosted server (join, start, a full turn, no crash) - the fix itself is deterministic and precisely covered by the unit tests reproducing the exact recorded scenario; re-triggering the same nondeterministic model mistargeting live on demand isn't reliable enough to be the correctness check here.
- **The full SRD 5.1 equipment catalog - a direct owner ask for "references or a database for items, weapons, equipment" (2026-08-10).** `srd.json`'s `equipment` table had only 8 entries (2 armors, 4 weapons, 1 potion) - real gaps a small demo dataset never needed to close until now. Grew to 140: the full weapon table (simple/martial, melee/ranged), the full armor table (light/medium/heavy, plus a reference-only `shield`), a broad adventuring gear list, all 7 standard equipment packs, a representative tool/instrument set, trade goods, and the SRD's own small generic magic item list (`+1 Weapon`, `Bag of Holding`, ...) - all real SRD 5.1 CC-BY-4.0 data, no invented items. See `server/rules/ATTRIBUTION.md`'s new "Equipment coverage" section for the two deliberate scoping choices (a representative rather than exhaustive tool list; `shield` as reference-only data, not wearable).
  - **Shipping the data alone would have been a real, known correctness bug, so it didn't ship alone.** `_compute_ac` (`server/engine.py`) previously only understood light armor's uncapped Dex bonus - exactly the gap this file's own `ATTRIBUTION.md` had already named as the reason heavier armor was deliberately left out of the original 8-item set. `_parse_armor_ac` (new) now correctly distinguishes all three real 5e shapes: light (uncapped Dex), medium (Dex capped at a real max - the cap only limits the positive side, a negative modifier still applies in full), and heavy (zero Dex contribution, positive or negative). A first version of this fix had its own real bug, caught by its own test before shipping: encoding heavy armor as `dex_cap = 0` and computing `min(dex_modifier, 0)` would have still applied a *negative* Dex modifier as a penalty under heavy armor, which isn't how it actually works - fixed with an explicit `heavy: bool` instead of overloading the cap value.
  - **A second real bug caught by this change's own new data-integrity test, not assumed clean at 132 entries**: several item names ("Chalk (1 piece)", "Rope, Hempen (50 feet)") contained parentheses/commas that `slug()` (`server/rules`) doesn't normalize, so `get_entry("equipment", entry["name"])` - the exact call a DM's `lookup_rule` tool makes - silently failed to round-trip back to its own entry. Fixed by simplifying ~26 names to a single clean noun phrase (moving quantity/descriptor detail into `description` where it wasn't already there) rather than teaching `slug()` to guess at arbitrary punctuation. `RulesIndex` also gained a small public `all_entries(category)` accessor so this integrity check (and any future one) doesn't need to reach into `_data` directly.
  - **A pure insertion diff, not a reformat, the same discipline the earlier SRD expansion (item 11 below) already established after getting it wrong once** - generated via a script that keys every new entry off `slug(entry["name"])` automatically (closing the whole class of name/key-mismatch bug above at the source, not just the instances caught this time) and surgically spliced only the `equipment` block's own text, asserting every other section of the file stayed byte-identical before writing anything.
  - 4 new tests for the AC formula (medium-armor cap on a positive and negative modifier, heavy armor ignoring both), a data-integrity test walking all 140 entries (no missing `name`, no duplicate display name, every entry round-trips through `get_entry`), and a representative spot-check across every weapon/armor category this expansion added. 507 tests pass project-wide. Live-verified against the real GPU-hosted server: added and equipped a real Breastplate (medium) and Plate Armor (heavy) via `/item add`+`/equip`, confirming `12→15` (dex+1, under the medium cap) and `→18` (heavy, no dex at all) computed correctly end-to-end; a real turn asking the Guardian for gear narrated cleanly with the expanded dataset loaded, no crash.
- **`update_world` reliability, dug into for real (2026-08-10) - a genuine, measured improvement shipped, one real gap left honestly unresolved.** The prior 33% figure (item 6, `OLLAMA_WORLD_UPDATES` entry) was a single live run, never turned into a reusable, `--repeat`-capable measurement the way `update_character`'s own investigation already has - the first real step here was closing that gap, not guessing at a fix blind.
  - **`scripts/live_world_reliability_check.py` (new)**: reconstructs the exact scenario shape that 33% figure described in prose (arrive somewhere → an NPC hands over a real quest hook → a neutral turn → travel to the quest location → resolve it), scored against real `WorldState` changes (did `location`/`objectives` actually change the way the turn warranted) rather than just "did a call happen" - four different valid update shapes exist here, so ground-truth state comparison is more precise than event-presence checking alone. 6 new tests using a `ScriptedWorldNarrator`, mirroring `live_reliability_check.py`'s own existing precedent.
  - **The real baseline, once actually measured with `--repeat 3`: 20% pooled (3/15), not 33%** - and every one of those 3 correct turns was the single trivial "nothing should change" turn, not a real positive. **0 of 12 real opportunities fired at all, across all 3 repeats.** Confirmed this wasn't a harness bug by inspecting the raw model response directly: narration unambiguously described arriving in a named village, yet the model's own JSON came back `"world_change": false` every time.
  - **Root cause, found by inspection, not guessed at**: the instruction lived as a same-priority afterthought ("Also decide `world_change`...") tacked onto the end of the base prompt, competing with `mechanical_change` for attention, with no concrete example of what should actually trigger it.
  - **Two candidate rewrites tried and rejected on real evidence, not shipped on a first promising sample** - matching this project's own established "don't ship on one run" discipline (item 6, the `qwen3:8b` lesson) applied here to prompt iteration itself, not just to trusting a baseline number:
    - A version giving location an explicit "always true on arrival" anchor, plus a blanket "any task-like dialogue is an objective" instruction and a "prefer true when unsure" bias: fixed location cleanly (recall on arrival/travel turns went from 0% to ~100%, reproduced across 6 repeat runs) but introduced a **new** false positive - the neutral "just making conversation" turn started inventing a spurious objective out of ambient rumor dialogue, every single run.
    - A version narrowing the objective trigger to a direct, explicit request only: fixed that false positive (the neutral turn correctly went back to no-change every run) but then missed even a genuinely explicit, direct quest request 3 out of 3 times - a net wash for objectives specifically, not an improvement.
  - **What actually shipped**: the location-specific "always true" anchor (the one piece that measured as a clean win with no observed regression across every variant it appeared in), kept alongside more conservative objective-add/complete wording that measured as no worse than the original - not a claimed fix for objective tracking, just not making it worse while fixing location. `complete_objective` in particular **never fired successfully in any variant tried** (0% in every configuration tested) - a real, still-open reliability gap, documented rather than papered over with wording that only looked like it helped on a small sample.
  - **Final, honestly-reported number**: re-measured against the actual shipped code (not the diagnostic monkey-patches used to iterate) across 8 total repeat runs (3 + 5, run separately to check for exactly the kind of run-to-run variance a single 3-run sample could hide) - pooled **21/40 (52.5%)**, individual per-run rates ranging 40-60%, versus the original wording's clean, repeated 20%. A real, roughly 2.5x improvement, concentrated specifically in location tracking, not a uniform fix across every world-update mechanism.
  - 1 test updated (`tests/test_narrator_ollama.py`'s own prompt-content assertion, matching the new wording). 513 tests pass project-wide.
- **A real, deliberate attempt at `complete_objective` specifically, ending in a genuine negative result - reported honestly, not smoothed over (2026-08-10).** Leading hypothesis going in: 0% recall was a *recall* problem - a small model being asked to retype an objective's exact text correctly from several turns back in the rolling history window, against `WorldState.apply_update`'s own exact-string match. A real, testable idea, not a guess left unverified.
  - **`NarratorBackend.narrate()` gained a `world_summary` parameter** (`WorldState.narrator_context()`, `server/state.py` - current location + active objective texts), passed on every real turn (`_narrate_and_apply`, `server/engine.py`) and threaded into both backends' own per-turn prompt as a "World state:" section. `AnthropicNarrator` gets it unconditionally (it never needed an opt-in gate to use `update_world` to begin with); `OllamaNarrator` only when `world_updates` is on. The `complete_objective` instruction was updated to say "copy the matching one's text from there character-for-character - don't retype it from memory."
  - **The hypothesis was wrong.** Re-measured across 10 repeat runs (5+5, run separately) with the exact objective text sitting directly in the DM's own prompt on the resolution turn: `complete_objective` still fired successfully **0 out of 10 times** - identical to before this existed. Not just an aggregate number - confirmed with a raw-response diagnostic showing the model answering `world_change: false` on a turn whose narration unambiguously freed a goat and resolved the one active objective explicitly listed, character-for-character, in that same call's own "World state" section it had just been given.
  - **The real bottleneck is a different, deeper problem than assumed**: the model doesn't reliably recognize "this narration resolves an active goal" as a `world_change`-worthy event category at all - having the exact text available to copy doesn't help if the model never decides this is a category-4 event to begin with. This is a genuinely different failure mode from the location-tracking gap the same investigation *did* manage to fix (item above) - location only ever needed a stronger, clearer trigger condition; objective completion apparently needs something else, not yet identified.
  - **Kept anyway, not reverted, on its own separate merits**: `world_summary` measured as roughly neutral overall (24/50 = 48% pooled with it, vs. 21/40 = 52.5% without - well within this scenario's own already-documented 40-80% per-run noise band, not a demonstrated regression) and is real, defensible infrastructure - grounding the DM in the session's actual current state rather than nothing. Every comment written before the `--repeat` verification actually ran and prematurely claimed this "closes the gap" was found and corrected in a same-day follow-up commit once the real numbers came back - the same "don't let an untested claim survive in the code" discipline this file's own reliability-investigation entries have tried to hold to throughout.
  - 5 new tests (`WorldState.narrator_context()` in isolation - empty/location-only/objectives-only/combined; each backend's own prompt-content check that `world_summary` is included when given and, for Ollama, correctly gated behind `world_updates`; an engine-level test confirming the real session's current world state actually reaches the DM call, not just a mocked one). 522 tests pass project-wide.
- **A real, wearable shield - closing the gap the equipment expansion above deliberately left open (2026-08-10).** `shield` had shipped as reference-only data specifically because a shield's +2 AC is additive on top of body armor, not a replacement base value the way `equipped_armor`'s own `ac` field is - stuffing it into that single slot would have silently computed AC wrong (`ATTRIBUTION.md`'s own "Equipment coverage" note named this as a real, not-yet-built gap, not an oversight).
  - `CharacterSheet` gains a genuine third slot, `equipped_shield` (`server/state.py`); `shield` gets a real `ac_bonus` field (`server/rules/srd.json`) distinct from armor's `ac` field, so `_compute_ac` (`server/engine.py`) can tell "replace the base" from "add on top" apart cleanly. `/equip`/`/unequip`/`/item remove` all route a shield-shaped item (any entry with `ac_bonus`) to the new slot correctly, including auto-unequipping it if removed from inventory - the exact same consistency guarantee weapon/armor already had, extended rather than reinvented.
  - Client's Inventory tab gained a `Shield:` line (only shown when actually equipped - no current class starts with one, so showing an empty slot on every character by default would be noise nobody asked for, the same "don't render the absent default" convention this project's other optional fields already follow).
  - 9 new tests (`_compute_ac` stacking correctly on top of no-armor/light/heavy, an unrecognized shield contributing nothing; real `/equip`/`/unequip`/`/item remove` end-to-end coverage for the new slot; client-side rendering). 528 tests pass project-wide. Live-verified against the real GPU-hosted server: added and equipped a real Shield alongside starting Leather Armor via `/item add`+`/equip`, confirmed AC went `12 → 14` (additive, not replaced), then back to `12` on unequip.
- **Real saving-throw proficiency, closing a gap this file had named explicitly twice** (the "Skill proficiencies" and "Formal initiative" entries both flagged it: `proficiency_bonus` was a real, correct number already available, but nothing wired it into saves). `request_roll` now adds it when the DM marks a roll `roll_kind: "save"` and the rolled ability is one of the character's own class's two proficient saves.
  - `CLASS_SAVING_THROW_PROFICIENCIES` is a new, separately and correctly authored table matching `server/rules/srd.json`'s own real class `saving_throws` field directly - deliberately **not** reused from `CLASS_ABILITY_PRIORITY`, whose own "CON placed second for every class" convention only actually coincides with real saving-throw proficiency for fighter (Str, Con). Checked directly against the real SRD data while building this: wizard's real saves are Int+Wis (not Int+Con), rogue's are Dex+Int (not Dex+Con), cleric's are Wis+Cha (not Wis+Con) - `CLASS_ABILITY_PRIORITY`'s own comment had claimed otherwise for years; corrected it once the mismatch was found rather than left standing now that it's known to be wrong.
  - **A second, separate gap this surfaced, not just a feature add**: `_dice_result_envelope` only ever included `proficient`/`proficiency_bonus` in the broadcast payload alongside a skill or spell tag - a proficient save's own bonus was silently missing from what players actually saw, even though it was already correctly folded into the roll's own total server-side. Both the server's own tool-result text and the client's log line now show a `(save, +N proficiency)` tag, the same transparency skill/spell rolls already had.
  - 5 new tests (a proficient save adding the bonus, a non-proficient save adding nothing, the same ability with no `roll_kind: "save"` correctly *not* gaining proficiency it wasn't asked for; 2 client-side render checks). 533 tests pass project-wide. Verified end-to-end via the deterministic engine-level tests above (this mechanism has no model-judgment component at all once `roll_kind`/`ability` are given - real 5e's own fixed class table, not something reliability-dependent); a live smoke test against the real GPU-hosted server confirmed no crash, though the model didn't happen to produce a `roll_kind: "save"` roll in either of two live attempts tried - not unexpected, since nothing about this session's own testing has ever controlled *when* the DM decides a roll is save-shaped, only what happens once it does.

## Next — highest priority, in rough order

**Recommended sequencing (2026-08-07, revised 2026-08-09)**: every *native-tool-calling* local model tested so far (`qwen2.5:7b`, `qwen2.5:14b`, `llama3.1:8b`, `qwen3:8b`, `llama3-groq-tool-use:8b`) plateaued around 29% best-case, non-reproducibly, regardless of scale, native tool-use training, prompt reminders, or history-window size (item 6's first six experiments). **That pattern no longer holds unconditionally** - the seventh experiment (structured JSON output, above) roughly doubled `qwen2.5:7b`'s own correctness (66% vs 29% pooled) by changing the response *mechanism*, not the model or its inputs, and is now `OllamaNarrator`'s default. **item 2 - verify the Claude backend live** remains valuable and still untried (it's the one way to know whether structured output's local gain also matters for a model that was already comparatively strong at native tool-calling), but it's no longer the *only* lever with real headroom the way this note previously framed it - structured output is a second, already-shipped one. Worth revisiting on other local models (`qwen3:8b`, `llama3.1:8b`) now that the mechanism itself has changed, and worth remembering **use `--repeat N` before trusting any single-run result** either way - `qwen3:8b`'s one "promising" native-tool-calling run famously didn't reproduce (item 6, fifth experiment), the exact failure mode `--repeat` exists to catch. The **visible-mitigation heuristic** (item 6, sixth experiment) stays a real safety net regardless of how any of this resolves - it decouples player trust from model reliability on its own.

**Item-1 calibration point (large local model) — resolved (2026-08-19): scale did not help; the 7B beat the 32B on the identical scenario.** The "large-model calibration" candidate was tried locally (owner refuses hosted API credits): `qwen2.5:32b`, structured output, via the harness — the same fixed scenario, `--repeat 5`.
   - **Pooled 22/35 (63%)**, spread 57/71/71/71/43%, 0 leaked text across all 5 runs — *below* `qwen2.5:7b`'s repeat-5 baseline of 26/35 (74%) run on the same scenario in the same session. A bigger local model is not a reliability lever for this task: consistent with the fifth experiment's finding (scale alone, without a change in training approach, doesn't move this number) and the seventh's (the response *mechanism*, not the model, was the first real win).
   - Evidence: `evidence/qwen25_32b_repeat5.json` / `.log` (single-run 71% in `qwen25_32b_run1.json`). The 32B runs competed with a live `qwen2.5:7b` test for CPU; correctness is sampling-dependent, not timing-dependent, so wall-clock contention doesn't invalidate the comparison.
   - **Candidate closed as answered**: a genuinely larger local model is not the lever; the 7B stays the better local default on this scenario.

**decision_split experiment (the two-request split, revisited with a structured decision phase) — tried live and reverted (2026-08-19): worse, same root cause as the 2026-08-07 two-request split.** A narration-free structured decision call (`DECISION_SPLIT_SCHEMA`) followed by a narration-only call was built, then A/B'd against the combined structured-output baseline on `qwen2.5:7b`, identical scenario, `--repeat 5` each.
   - **Baseline: 26/35 pooled (74%)** (spread 71/71/71/71/86). **Split: 16/35 pooled (46%)** (spread 43/43/43/43/57). Real-change call attempts went *up* (6-7/8 vs 6/8) but wrong-target calls jumped 9→16 across the five runs and false positives 5→8 — the narration context the combined call reads while deciding is what disambiguates *who got hit*. Same conclusion as the 2026-08-07 split (which used native tool-calling for the decision phase): splitting the turn removes exactly the context that makes targeting work.
   - **Reverted, not shipped** — the prototype commit was reverted; the tree is back to the combined structured-output design.
   - Evidence: `evidence/qwen25_7b_baseline.log` / `evidence/qwen25_7b_ds.log` (per-turn JSONs lost when the local repo was replaced by the submodule clone during the 2026-08-19 machine reorg).

1. **Revisit memory if sessions run long.** The rolling window (last 6 turns) fixes the "forgets what just happened" problem completely but still forgets anything older than the window — a very long session could still see the DM drop an early plot thread or NPC. Not worth solving until it's actually observed happening in play; the fix, if needed, is a summary of everything outside the window, kept updated via a DM tool call the same way `update_character` works. **The "kept updated via a DM tool call" half already existed (`update_world`'s `summary` field) but wasn't reaching the DM's own context — fixed as item 29 below.** Still open: whether `summary` is kept reliably updated over a long session (item 6's own weak-on-Ollama finding) and whether any of this needs revisiting further once actually observed in play.

2. **Verify the hosted Claude path live too**, once there's a way to (credits, or just to compare narration quality against the local models side by side) — the code path is identical to Ollama's. Would also be useful as a reliability baseline: Claude is specifically trained for reliable tool use in a way small local models aren't always, so this comparison would show whether `llama3.1:8b`'s turn-2 flakiness (see above) was purely a small-local-model thing or something more general. Now also the only way to verify `request_roll` (see Current state, above) actually works end-to-end — it's Anthropic-only and still unverified beyond mocked tests.

3. **`qwen2.5:7b`'s earlier "3-for-3" framing no longer holds — a much bigger live sample (see item 4) found a real, substantial drop in tool-call reliability.** That "3-for-3" was 3 turns, all on the simple `self`-target path, in a short session. It's since been contradicted by a much larger, harder sample. Correcting the record here rather than leaving the old framing standing: across every live `qwen2.5:7b` turn run against this project so far (13 total, spanning three separate sessions), only 2 of roughly 8 clearly-warranted calls actually happened. See item 4 for the full breakdown — this item is kept separate to flag explicitly that the original claim was too optimistic, not just incomplete.

4. **NPC/monster state tracking, live-verified twice against `qwen2.5:7b` — a real, substantial tool-call reliability gap found, not yet fixed or explained.**
   - **First pass (2 turns, one session, one NPC):** turn 1 narrated real damage to a bandit but never called `update_character` at all; turn 2 did call it correctly (`target="bandit"`, proving the dispatch mechanism itself works end-to-end), but the narration's own stated HP arithmetic ("health drops from 7 to 1") didn't match what was actually applied (`hp=4`, `max_hp=10` — there was no real prior "7," since turn 1's call never happened), and two lines of raw internal instruction text leaked verbatim into the in-character prose (*"Deal 6 damage to the bandit on a hit."*, *"Update the bandit's HP and add a new condition (wounded)..."*).
   - **Second pass (8 turns, one session, two sequential bandits, deliberately mixing NPC-target/self-target/non-mechanical actions):** **zero `update_character` calls across all 8 turns** — not one, on either the NPC or `self` path. This includes turns 1, 2, 4, 5, and 6, all of which narrated a bandit taking unambiguous, eventually lethal damage ("driving his longsword deep into the bandit's gut... life drains from his form") with no ambiguity about whether something mechanical happened. Turns 3 and 8 (pure observation/looting) correctly warranted no call; turn 7 (pressing a cloth to a wound, no proper healing) is genuinely ambiguous, same as the original comparison's turn 5. Unlike the first pass, this run's narration stayed clean — no leaked instruction text — it simply never reached for the tool at all, a different failure shape than the leaking seen before.
   - **The honest combined tally, not smoothed over**: across all three live sessions this project has run against `qwen2.5:7b` (13 turns total — 3 from the original head-to-head, 2 from the first NPC pass, 8 from the second), only 2 of the turns that clearly warranted a call actually got one. The 3 clean self-target turns from the original comparison are still real and still stand, but they no longer represent the model's overall behavior once NPCs and longer combat sequences enter the picture.
   - **A real, unresolved question, not a conclusion**: whether this is specific to NPC-targeting (a newer tool-input shape than plain `update_character`, genuinely less represented in whatever this model was trained on) or a broader degradation as a session's rolling history grows (the second pass's 8 turns is longer than any prior live test, and the *later* turns weren't noticeably worse than the earlier ones — turn 1 already missed). The data so far doesn't cleanly separate these two hypotheses, since every clearly-mechanical turn in the second pass happened to be an NPC-target case (Torvin was never narrated as taking damage himself). Worth a future session that mixes in genuine `self`-target damage partway through a longer combat sequence, specifically to tell these two explanations apart.
   - **Not fixed this session, deliberately** — matches this project's own standard of logging a real finding rather than iterating prompts until a clean run happens to appear. Whatever the fix turns out to be (a prompt change, a smaller/different history window, or accepting this as a known limitation of a free local 7B model), it should be designed against a clearer picture of *why* this is happening, not against this one data point.

5. **Follow-up investigation, one session later: streaming ruled out, history-driven degradation confirmed, one candidate fix tried and reverted after it didn't hold up under real-engine testing.**
   - **Streaming was a real, tempting suspect — ruled out empirically, not assumed.** `ollama-python`'s own GitHub issues (#279, #12557, #9632) document real streaming+tool-calling bugs — tool-call data landing in `content` instead of `tool_calls`, inconsistent chunking — matching this project's exact `stream=True` usage closely enough to be a plausible root cause. Checked directly against this project's real installed version (`ollama` 0.6.2) rather than assumed from the issue reports: a minimal single-shot request (no history) correctly returned a real `tool_calls` entry with `stream=True`, every time. A second, harder test — the same request but with a realistic 6-turn rolling-history window prepended, matching a real mid-session turn — got a real tool call only **1 of 4 times** with `stream=True`, and the *identical* history-heavy request with `stream=False` also got **1 of 4** — same rate either way. Streaming is not the cause.
   - **The real, confirmed lever is accumulated conversation history, not streaming.** The minimal no-history case worked every time tested; the realistic history-heavy case dropped to ~25% regardless of streaming. This matches a well-known small-model pattern: instructions placed once at the start of a long context (the system prompt's tool-use policy) can lose effective weight relative to more recent content as that context grows — a real, if still not fully explained, mechanism worth keeping in mind for future investigation (e.g. of the deferred "revisit memory if sessions run long" item above).
   - **A second, distinct problem found in the same history-heavy testing: mistargeting, not just missing calls.** When a tool call *did* fire under history-heavy conditions, it sometimes targeted the acting character (`'self'`, or the character's own name) even though the narration was entirely about an NPC being killed by that character's own attack — a real semantic error, not just an omission.
   - **A candidate fix — a short, explicit reminder repeated at the end of every turn's own prompt (not just once in the system prompt), disambiguating "target = whoever got hurt, not whoever acted"** — was designed and tested in two stages. Stage one, isolated diagnostic testing (single requests, no real engine involved): the reminder fixed the mistargeting cleanly (0/2 correct targets without it, 2/2 with it, in back-to-back small batches) with call-rate roughly unchanged (~40% either way). Promising enough to implement for real and verify against the actual production code path, not just the isolated snippet — this project's own standard, applied here deliberately before claiming a fix.
   - **Stage two — implemented in `server/narrator_ollama.py`, then re-ran the *exact same* 8-turn scenario from item 4's second pass above, through the real `GameEngine`/`OllamaNarrator`, for a direct before/after comparison. Result: not a net improvement, and a new, more visible defect appeared.** The engine-measured success rate was still **0 of 8** — identical to the no-reminder run, no actual improvement in what matters (state getting tracked). But narration in 6 of the 8 turns now contained a literal, player-visible line like `update_character {"target": "Bandit", "hp_delta": -8}` — the model expressing its tool-call intent as leaked text in the story itself, rather than through Ollama's real function-calling channel, where `apply_update` would have actually run. This exact leaking pattern was **not present at all** in the no-reminder run of the identical scenario — a new regression the fix introduced, not something already there. Interestingly, the *reasoning* visible inside that leaked text mostly got the target right (`"Bandit"` for enemy damage, the character's own name only for the genuinely self-inflicted turns) — weak evidence the reminder did influence the model's reasoning about *who*, just not *how* to actually invoke the tool, and arguably made the visible failure mode worse (exposed raw mechanics to the player) instead of better.
   - **Decision: reverted, not shipped.** A change that doesn't measurably improve the metric that matters (real invocations reaching `apply_update`) and introduces a new, more immersion-breaking visible defect isn't a fix, regardless of what the isolated diagnostic suggested. `server/narrator_ollama.py` and its tests are back to exactly what they were before this investigation started. The reminder's exact wording is preserved here in case a future attempt wants a documented starting point, not because it's believed to be close to correct:
     ```
     (Reminder: if this turn's outcome changes anyone's HP, inventory, or
     conditions, call update_character now. target must be whoever actually
     got hurt or changed, not simply whoever acted - if the acting character
     attacks someone else and that other creature takes the damage, target
     is that NPC's name, never the acting character's own name or 'self'.
     Only use 'self' when the acting character is the one taking damage or
     being changed.)
     ```
   - **What's actually been ruled out vs. still genuinely open, for whoever picks this up next**: ruled out — streaming as the cause (confirmed via direct A/B test against the real installed `ollama` version), and this specific reminder-based fix (confirmed via real-engine before/after testing). Still open — *why* history accumulation degrades tool-use this much, whether a structurally different intervention (e.g. two separate requests — one forcing a tool-use decision with no narration, then a second for the narration itself, rather than one combined request) would fare better, whether a different/larger local model changes this materially, and whether the same history-driven pattern affects Anthropic's Claude backend at all (still fully unverified live, pending API credits — see item 2 above).

6. **Reliability harness promoted to a real, version-controlled tool: `scripts/live_reliability_check.py`.** Closes the last bullet of this item as originally brainstormed. Runs a fixed 8-turn scenario (a faithful reconstruction of item 4's "second pass" shape — two sequential bandits, mixed call/no-call/ambiguous turns) through the real `GameEngine` against a real, live `NarratorBackend` (`--backend ollama|anthropic`, either model overridable via `--model`) — not a mock. Scores each turn by watching for the `character_update`/`npc_update` envelopes the engine actually broadcasts (the same engine-level signal the investigation itself used to measure "0 of 8" in item 5), separately flags leaked pseudo-tool-call text via a regex on the narration, and can dump a JSON report (`--out`) for diffing between runs — model vs. model, or a candidate fix's before/after, the way item 5's reminder experiment had to be done by hand. 5 new tests (`tests/test_live_reliability_check.py`) cover the scoring logic itself against a scripted fake narrator, independent of any live model. The scenario is a *reconstruction*, not a byte-exact replay — item 4's exact original action strings were only paraphrased in this file, not preserved verbatim.
   - **First real run with the new harness, live against `qwen2.5:7b` (2026-08-06): 0/8 real `update_character` calls, 0 leaked pseudo-tool-call text.** Turns 3 and 8 (pure observation/looting) correctly drew no call; turns 1, 2, 4, 5, 6 all narrated unambiguous damage to a named bandit and got no call regardless; turn 7 (cloth on a wound) is the deliberately ambiguous case and isn't scored. This closely reproduces item 4's "second pass" finding (also 0/8) on an independently-run scenario through the new tool rather than by hand, which is itself useful signal: the failure is consistent and harness-reproducible, not a one-off. Full per-turn JSON kept out of version control (a throwaway scratch run, not a fixture) — rerun `python -m scripts.live_reliability_check --backend ollama --out <path>` to reproduce.
   - **Second real run, live against `llama3.1:8b` (2026-08-06), same scenario: worse, not better — 1/7 (14%) vs. `qwen2.5:7b`'s 2/7 (29%), plus two new failure shapes.** This directly answers the "try a different/larger local model" candidate below: at the same ~7-8B size class, a bigger model was not a fix.
     - **A new, distinct correctness bug found, not just a reliability gap — and fixed.** Turns 3 and 7 *did* produce a real tool call (via the genuine Ollama function-calling channel, not leaked text) with `target` set to the literal `player_id` string (e.g. `"player-1"`) instead of `"self"` — almost certainly because `character_summary` is the character sheet's own JSON, which includes its `player_id` field, and the model echoed it back verbatim. `GameEngine._on_player_action`'s dispatch only special-cased the literal string `"self"`, so any other string — including the acting player's own id — fell into the NPC branch and silently created a phantom NPC sheet named `"player-1"`, completely disconnected from the real character. **Fixed in `server/engine.py`**: the self/NPC check now also matches `target == player_id`. Regression test added (`test_update_character_target_matching_own_player_id_treated_as_self`, `tests/test_engine.py`). This is a real, independently-worthwhile correctness fix regardless of the broader call-rate question — a silent phantom NPC is a data-integrity bug, not a missing call.
     - **A harness gap found and fixed in the same run**: turns 4 and 5 leaked `lookup_rule(category="monster", name="Goblin")` as visible prose instead of a real call, but `LEAK_PATTERN` only matched `update_character`-shaped leaks, so this went undetected in the first printed report. Broadened to catch `lookup_rule` leaks too (`scripts/live_reliability_check.py`), with a regression test (`tests/test_live_reliability_check.py`).
     - Turns 1-2 also leaked `update_character(...)` as prose (the same failure mode `llama3.1:8b` showed in the very first head-to-head comparison, turn 2, at the top of this file) — so this model shows *three* distinct failure shapes in one 8-turn run: prose-leaked `update_character`, prose-leaked `lookup_rule`, and the real-call-wrong-target bug above (turn 6 mistargeted `self` when the narration was entirely about the NPC, the same known bug from item 5).
     - **Reading on the original candidate**: doesn't rule out that a *genuinely* larger model (13B+) would behave differently — `llama3.1:8b` is the same size class as `qwen2.5:7b`, not the qualitative jump the candidate had in mind. Worth revisiting with a larger model if one gets pulled locally, but "try a same-class model" specifically is now answered: no improvement.
   - **Third experiment: the two-request split, tried for real and reverted (2026-08-07).** The most promising untried lever from item 5 — split each turn into a tool-only decision call (no narration, tools enabled) followed by a separate narration-only call (tools disabled, told what was already decided) — was implemented in `server/narrator_ollama.py` and tested live through the harness against both models on the identical scenario, exactly like the reminder-fix experiment in item 5. **Result: worse on both models, not better, plus two new defects. Reverted.**
     - **Correctness dropped for both models.** `qwen2.5:7b`: 2/7 (29%) combined-request baseline → 1/7 (14%) split. `llama3.1:8b`: 1/7 (14%) baseline → 0/7 (0%) split. Real tool-call *activity* actually went up under the split (qwen fired on 5/8 turns vs. 0/8 baseline) — the decision-only framing does get the model to attempt more calls — but targeting accuracy got worse, not better, so the net effect on the metric that matters (correct state changes) was negative.
     - **The self-vs-NPC mistargeting bug (item 5) got more pronounced, not less.** Under the split, `llama3.1:8b` defaulted to `target: self` on 5 of its 8 turns regardless of whether the narration was actually about an NPC (turns 2, 4, 6, 8 all wrongly self-targeted; only turn 7's genuine self-target was correct) — a stronger, more consistent version of the same failure the split was meant to help with.
     - **Two new defects, not present in the combined baseline.** (1) `qwen2.5:7b` turn 1 fired *two* real calls in the same turn — one correctly on the NPC, one wrongly on `self` — something the single-combined design's tighter loop didn't produce. (2) `llama3.1:8b` turn 5 produced **completely empty narration** — the decision phase correctly made no call, but the narration-only follow-up call returned zero content, a silent turn with nothing shown to the player at all, worse than a wrong tool call.
     - **A related, more subtle problem: phase 2 launders wrong phase-1 decisions into coherent-sounding but false fiction.** `qwen2.5:7b` turn 6: phase 1 wrongly applied lethal self-damage instead of NPC damage, and because phase 2 is explicitly told "don't contradict what was already decided," it dutifully narrated Torvin's own death from a blow he landed on a bandit ("your HP is now at 0 out of 10") — a wrong mechanical decision made harder to notice, not easier, because the two phases now agree with each other.
     - **A new same-family variant of the player_id-echo bug, also found and fixed.** `qwen2.5:7b` turn 5 and `llama3.1:8b` turn 3 both targeted the literal string `"Torvin Ironheart"` — the character's own *name*, also present in `character_summary`'s JSON — not just its `player_id` (the shape item 6's second bullet already fixed). **Fixed in the same `GameEngine._on_player_action` dispatch check**: `target in ("self", player_id, character.name)`. Regression test added (`test_update_character_target_matching_own_name_treated_as_self`, `tests/test_engine.py`). Independently worthwhile, kept despite the split itself being reverted — same reasoning as the player_id fix: correct dispatch for a self-referential call is right regardless of whether the model should have called at all.
     - **Decision: reverted, not shipped** — `server/narrator_ollama.py` and its tests are back to the combined single-request design, matching this project's standard from item 5: a change is only kept if it measurably improves the metric that matters under real-engine testing, not if it merely looks plausible. The engine-level dispatch fix above is the one part of this round kept, since it's a correctness fix independent of the split experiment's own outcome.
   - **Fourth experiment: quantifying the history-window tradeoff — a clean null result (2026-08-07).** `Session.max_history_messages` was pulled out of a module-level constant into a per-instance field (same default, `server/state.py`) so the harness could vary it directly via a new `--max-history-messages` flag, rather than guessing. Ran the identical scenario against `qwen2.5:7b` at three window sizes: the production default (12 messages / 6 turns, already measured at 2/7 correct, 29%), a shrunk window (4 messages / 2 turns), and zero history at all.
     - **Correctness was flat at 2/7 (29%) across all three window sizes** — 12, 4, and 0 messages produced the exact same score, with 0 real `update_character` calls at 12 and 4 messages, and only one (mistargeted) real call at 0. Shrinking or even eliminating the rolling history window bought back nothing on this scenario/model — it doesn't move the needle on tool-call correctness in either direction on its own.
     - **The zero-history condition paid real costs instead.** Two turns (7, 8) leaked pseudo-tool-call text (`update_character {"target": "self", ...}` verbatim in the narration) — 0 leaks were seen at either 12 or 4 messages. A third turn (6) leaked what reads as raw internal instruction text ("Update the character sheet for Torvin Ironheart to reflect the end of the battle.") — not caught by `LEAK_PATTERN`, which only matches the literal tool names, not paraphrased instruction-like text; noted as a known detection gap, not worth chasing with a fuzzier regex given the false-positive risk. Turn 6 also flipped the recurring monster's name from "bandit" (turns 1-5, correctly consistent) to "goblin" — exactly the "forgets what just happened" cost the rolling-history design was originally built to prevent (see "DM memory" in Current state, above), now directly observed rather than theoretical.
     - **Reading**: this complicates, rather than confirms, item 5's earlier finding that "accumulated conversation history is the real driver" of degraded tool-use. That finding came from isolated diagnostic requests measuring raw single-call invocation rate; this experiment measured engine-level, harness-scored correctness across a full scripted scenario, and found no relationship in that setup — the history-window knob is not an effective lever for this problem, and shrinking it only pays the well-understood continuity cost while buying nothing back. This closes the candidate: not "not yet tried," but tried and found not to help.
   - **Fifth experiment, complete (2026-08-07): does training for tool use beat scale?** Prompted by asking whether any model is actually "catered" to this project — research (see the immersion-pass entry above's citations, plus fresh searches on tool-calling benchmarks) surfaced a real distinction this project hadn't considered yet: `qwen2.5`/`llama3.1` are general-purpose chat models with tool-calling bolted on, not trained specifically for it. Two real alternatives exist and are pullable via Ollama: **Qwen3** (native tool-calling training across every size, reported as having the lowest dropped-tool-call rate among local models) and **`llama3-groq-tool-use:8b`** (a Meta/Groq/Glaive collaboration fine-tuned specifically for function calling, 89% on the Berkeley Function Calling Leaderboard). Neither is DM/RP-specific — the search also confirmed no model is specifically tuned for "AI Dungeon Master," and the RP-focused fine-tunes that do exist (MythoMax, Natsumura-storytelling, Chronos Hermes) optimize for narrative voice, not tool reliability, which is the opposite of Oracle's actual bottleneck (narration quality has been fine across every model tested so far).
     - **`qwen2.5:14b` (same generation as the default, ~2x the parameters) run first as the "just bigger" control: 2/7 (29%), identical to `qwen2.5:7b`'s score.** Notably, behavior wasn't identical to the 7B baseline even though the final score was — this run made 2 real tool-call *attempts* (turn 1 targeted `"Bandit Guard"`, turn 2 wrongly targeted `self`) versus the 7B baseline's 0 real calls, but both attempts were mistargeted, so it nets out to the same correctness. Confirms the item-6-third-experiment reading again: bigger within the same generation/training approach doesn't help, though it does appear to change *how* it fails, not just whether it does.
     - **`qwen3:8b` — still 2/7 (29%) overall, but the first genuinely different result all session: a real, correctly-targeted call.** Turn 2 fired `update_character(target="bandit", ...)` — an exact-match lowercase target on a real function call, the first true-positive "call" turn any model has gotten fully right across every run this session (all prior 2/7-or-better scores were built entirely from easy true-negative no-call turns, never a real hit). Turn 4 still mistargeted `self` on an NPC-damage turn (the same recurring bug), and turn 8 called `self` to add an item on a looting turn the scenario's ground truth scores as "no call" but arguably shouldn't (picking up a scroll is a real inventory change - a scenario-design nuance flagged before, not a qwen3 defect). 0 leaked text. Net: still not reliable, but the *shape* of the failure changed - qwen3 reaches for the tool more often and gets targeting right some of the time, versus the qwen2.5 family which mostly doesn't reach for it at all. Directionally consistent with "trained for tool use" being a real, if partial, lever - one true positive is a promising signal, not proof, on a 5-call scenario.
     - **`llama3-groq-tool-use:8b` — 2/7 (29%), 0/8 real calls, and a new failure shape not seen in any model tested so far.** This is the BFCL benchmark leader by reputation (89% on Berkeley Function Calling Leaderboard) - and it never called `update_character` once. Instead, on every combat turn it narrated an invitation for the *player* to roll dice, in tabletop-manual phrasing: *"Roll for attack: d20 + 5 (your Strength modifier)."*, *"Roll for damage: d8 + 2..."*, *"Roll for first aid: d20 + 3 (your Medicine modifier)."* - never touching the tool, and turn 6 flatly asserted `"Your current HP is 10/10"` after two full combats, i.e. confidently narrating a mechanical fact that contradicts the fiction (Torvin took real hits) instead of ever checking or changing it. `request_roll` isn't offered to Ollama models (by design, see above), so this looks like the model reaching for tabletop-manual narrative convention it was trained on, in a context where no such tool exists for it to reach for - a mismatch between its training distribution and this specific task shape, not a call it almost made and missed.
     - **Reading across all three: raw tool-calling benchmark performance doesn't transfer to this specific task, but *how* a model was trained still visibly matters.** The benchmark leader (`llama3-groq-tool-use:8b`) did worst in the way that matters most (0 real calls, a new confidently-wrong-fact failure mode), while `qwen3:8b` - not marketed as a tool-calling specialist, just trained with tool-calling natively rather than bolted on - produced this session's only clean, correctly-targeted real call. The likely reason: BFCL-style benchmarks test *isolated* tool-calling given a clear tool-shaped prompt; Oracle's actual task is harder and more ambiguous - stay in character, narrate prose, and *also* decide mid-narration whether this specific moment warrants a mechanical update, with no explicit cue that a call is expected. `qwen2.5:14b`'s within-generation scaling result (same score, different mistargeting pattern) still stands: scale alone, without a change in training approach, doesn't move this number.
     - **Second, independent `qwen3:8b` run (2026-08-07): the promising signal did not reproduce.** Same scenario, same model, rerun cold: **1/7 (14%)**, down from run 1's 2/7. Turn 2 fired a real call again (more evidence the "reach for the tool more often" pattern is real and repeatable) but this time named the NPC `"Bram the Bandit"` instead of matching the scenario's plain `"bandit"` - a real, plausible DM improvisation (giving a named NPC a name) that the harness's exact-string matching can't distinguish from a genuine miss, since a one-shot scenario can't tell whether that name would've been used *consistently* in later turns, which is the actual point of NPC tracking. Turns 5, 6, and 8 all mistargeted `self` this run (worse than run 1's single self-mistarget on turn 4) - the recurring self-vs-NPC bug clearly wasn't fixed by this model, just less consistently triggered. **Combined across both runs: only 1 of 10 "call"-type turn opportunities landed a real, scenario-exact-matching correct call** - a real, repeatable improvement over `qwen2.5:7b`'s 0-for-many in *call attempt rate*, but not yet a reliable model by any honest reading.
     - **Revised working conclusion**: `qwen3:8b` attempts real tool calls more consistently than the `qwen2.5` family (call attempted on 3-4 of 8 turns both runs, vs. `qwen2.5:7b`'s 0) - that part replicated cleanly and is a genuine, useful finding. Targeting/naming accuracy on those attempts did not replicate as promising - one run's clean hit looks more like favorable sampling than a stable capability once a second run is in hand. **Not switching the default on this evidence** - two runs is barely more than one, and the honest picture is "reaches for the tool more, still gets it wrong most of the time," not "solved." Revisit with more repeats once GPU-accelerated iteration (see the GPU-migration section under Later) makes that cheap enough to do properly instead of one run at a time.
     - **This revisit happened (2026-08-07), on the newly GPU-verified machine, and settles the open question with real statistical backing instead of two one-off runs.** `--repeat 5` against both models, identical scenario, `qwen25_7b_repeat5.json`/`qwen3_8b_repeat5.json` kept as the raw evidence.
       - **`qwen2.5:7b`: pooled 10/35 (29%) - identical every single run (29%, 29%, 29%, 29%, 29%) - but that flat aggregate hides real, substantial variance underneath.** Real tool-call attempts per run: 0, 0, 0, 1, 2 (out of 8). Leaked pseudo-tool-call text per run: 0, **8**, 0, 5, 0 - one run leaked on *every single turn*. When it does leak, the leaked JSON itself is mistargeted the same way a real call would be: `update_character {"target": "self", "hp_delta": -2}` on a turn where the narration is entirely about a bandit taking damage, not the acting character - the self-vs-NPC bug shows up identically whether the call is real or hallucinated as prose.
       - **`qwen3:8b`: pooled 10/35 (29%) - statistically indistinguishable from `qwen2.5:7b`'s score, arrived at through a completely different mechanism.** Real tool-call attempts per run: 7, 6, 4, 7, 6 (out of 8) - roughly **10x** `qwen2.5:7b`'s attempt rate. Leaked text: **0 across all 5 runs, all 40 turns** - every attempt goes through the genuine Ollama function-calling channel, none of it gets narrated as prose instead. Despite that, correctness nets out identical to `qwen2.5:7b` because the real calls are frequently wrong: the established self-vs-NPC mistargeting bug (turn 1 of run 1: `expected_target: "bandit"`, `called_targets: ["self"]`), plus **a failure shape not seen in any model before** - false-positive calls on turns the scenario expects no call at all (turn 3 of run 1: `expected: "no_call"`, but `called: true` anyway, on a plain "look around the room" action).
       - **Reading**: `qwen3:8b` is not more *reliable* by the aggregate metric this project has tracked throughout, but it fails through a fundamentally more legible mechanism - a real, clean function call with a wrong target or wrong timing, versus `qwen2.5:7b`'s mix of silent misses and mistargeted prose leaks. That distinction matters for what a future fix should target: a stronger self-vs-NPC disambiguation (the exact reminder-based fix tried and reverted in the streaming/history investigation above) has a real call to correct on `qwen3:8b` far more often than on `qwen2.5:7b`, where there's frequently no real call happening at all to redirect. Still not switching the default on this evidence alone - "wrong more legibly" isn't "solved" - but this is now a properly-powered result (5 repeats, 40 turns per model) rather than the "two runs, barely more than one" the prior entry explicitly flagged as insufficient.
       - **Real operational cost difference, also worth recording**: `qwen2.5:7b`'s 5 repeats took 2m36s total on this GPU; `qwen3:8b`'s took **20m45s** - roughly 8x slower, consistent with this file's earlier note about `qwen3:8b`'s thinking-mode overhead. Even with GPU acceleration removing the CPU-era 30-90s/turn barrier, `qwen3:8b` specifically remains meaningfully more expensive to iterate on than the `qwen2.5` family.
   - **Sixth item, complete (2026-08-07): a visible mitigation, not a fix — shipped and verified.** Doesn't move the correctness numbers above at all (nothing here makes any model actually call the tool more reliably); it's a safety net so a stale sheet isn't silently trusted regardless of which model is running underneath, while the real reliability question stays open. `GameEngine._narrate_and_apply` (`server/engine.py`) gained `POSSIBLE_UNTRACKED_CHANGE_PATTERN` — an outcome-focused regex (`damage`, `wound(s/ed/ing)`, `bleed(s/ing)`, `dies/dead/death/slain/kill(s/ed)`, `unconscious`, `collapse(s)`, `hp`, `health`) — and a check, run after tool-call bookkeeping completes: if neither the acting player's own sheet nor any NPC was touched this turn, *and* the narration buffer matches the pattern, a private `system_message` (`level: warning`) tells that player their sheet may be out of sync with the story. Deliberately narrow and outcome-focused rather than matching any attack verb (`swing`, `strike`, `attack`) — a narrated *miss* shouldn't trip this, only language that reads like a resolved outcome. Still expects both false positives (a near-miss description using "wound" in passing) and false negatives (phrasing the pattern doesn't catch) — a signal for the player to weigh, not a verdict, the same framing this item always had.
     - **A real false-positive class caught before shipping, not after**: an opening scene (`_narrate_opening_scene`) routinely sets flavor using this exact trigger language — a village recently attacked, a wounded NPC met in passing — with no mechanical change ever expected on turn zero. `_narrate_and_apply` gained a `check_for_missed_changes: bool = True` parameter specifically so `_narrate_opening_scene` can pass `False`, rather than letting every campaign opener risk a spurious warning before the player has even taken an action.
     - **4 new regression tests** (`tests/test_engine.py`): warns when damage language has no tool call (a direct reconstruction of the real failure shape this file has documented repeatedly — an NPC narrated dying with zero calls all turn); stays silent when a real `update_character` call *did* fire with the same trigger language, so a turn that already did the right thing isn't double-warned; stays silent on a plain no-trigger narration (most turns correctly involve no mechanical change); and stays silent during the opening scene even with trigger language present, confirming the false-positive guard actually works. Full suite verified green afterward (92 passed, up from 88).
     - `docs/protocol.md`'s `system_message` row updated to describe this new use case alongside its existing "connection/errors" framing, so the protocol doc doesn't go stale the moment this shipped.
   - **Seventh experiment, complete (2026-08-09): structured JSON output, the first real correctness improvement across seven experiments — not a mitigation, an actual fix to the metric that matters.** Picked off the "remaining candidates" list below specifically because it was the one genuinely untried lever that changes the *mechanism* itself (every prior experiment either tuned an input to the same native tool-calling path — the reminder, the history window — or changed which model runs it, not *how* the model is asked to decide).
     - **The mechanism**: `server/narrator_ollama.py` gained a `structured_output` mode - instead of native tool-calling (the model *decides* whether to invoke a separate `update_character` tool, the exact step every model tested has repeatedly missed), the entire response is constrained via Ollama's `format` parameter to a JSON schema (`STRUCTURED_OUTPUT_SCHEMA`) that always has a `mechanical_change` boolean to fill in, plus `target`/`hp_delta`/`add_condition` when true. "Should I call this?" becomes "fill in this field" - a different problem shape than tool-selection.
     - **A real, live, properly-powered comparison, not a single run**: `--repeat 5` against `qwen2.5:7b`, identical harness scenario, structured output vs. native tool-calling. **Real tool-call *attempts* jumped from 0-2/8 to 6-7/8 turns per run** — the same "reaches for the tool more" pattern `qwen3:8b`'s native-tool-training once showed (item 6's fifth experiment), except this time from the *same* `qwen2.5:7b` model that had been the flattest performer of the whole investigation, just asked differently.
     - **A real scoring bug found while analyzing the results, not introduced by this experiment — and one that affected every prior number in this entire item.** The harness's own `correct` check did an exact string comparison between a scenario's literal target (`"bandit"`) and the model's actual call (`"Bandit"`, `"Second Bandit"`) — but the real production engine has keyed NPCs by `target.casefold()` since the case-insensitive-NPC-matching fix (Current state, above) shipped, so a re-cased name is *not* a real miss in production, only in this harness's own stricter scoring. Fixed (`scripts/live_reliability_check.py`'s `correct` computation now casefolds both sides, matching `server/engine.py`'s own real matching exactly) - a new regression test (`tests/test_live_reliability_check.py`) locks a re-cased target as correctly scored. **This changes the headline number materially**: structured output's own pooled rate was 12/35 (34%) under the old buggy exact-match scoring, and **23/35 (66%)** once rescored correctly - more than double the corrected baseline's 10/35 (29%), which the same fix left unchanged (baseline rarely calls the tool at all, so casing was never the reason it failed).
     - **Honest, not "solved":** per-run correctness ranged 57%-86%, not a flat 8/8 - real misses remain, and the harness's own structured-output schema deliberately doesn't cover `rest`/`notes`/`disposition`/`cast_spell` yet (a real, named scope gap - a real session using this mode loses access to those `update_character` fields until the schema grows to match). Framed as a genuine improvement worth shipping as the default, not a claim that local-model reliability is a solved problem.
     - **Shipped as the new default, not left as an opt-in flag.** `OllamaNarrator.__init__`'s `structured_output` now defaults to `True` (was `False`); `create_ollama_narrator()` gained an `OLLAMA_STRUCTURED_OUTPUT` env var (default on, `"0"`/`"false"`/`"no"` opts back into the legacy path - e.g. for a session that needs the fields structured mode doesn't cover yet). The harness's own `--structured-output` flag was replaced with `--tool-calling` (opt *into* the legacy path for comparison), matching the new default rather than requiring an extra flag to get the now-recommended behavior.
     - **10 new tests**: `tests/test_narrator_ollama.py` gained 9 (the structured path yields narration and applies a real change via a real fake-Ollama-client response; the real schema/prompt/`stream=False` are actually passed to the client; no mechanical change never calls `apply_update`; malformed JSON surfaces the raw content instead of crashing; a zero `hp_delta`/empty `add_condition` are omitted rather than passed through as no-op fields; the constructor and `create_ollama_narrator()` default to `True`; the `OLLAMA_STRUCTURED_OUTPUT` opt-out and its case-insensitivity). `tests/test_live_reliability_check.py` gained 1 (the casefold scoring fix itself). Full suite: 344 passing.
     - **Confirmed on a second model (2026-08-09): `qwen3:8b`, `--repeat 5` both ways.** Native tool-calling: 11/35 (31% pooled) — consistent with this model's earlier-documented plateau, not an artifact of the casefold fix (its own tool-call attempt rate stayed low, same as `qwen2.5:7b`'s baseline). Structured output: 20/35 (57% pooled) — roughly 1.8x, the same *kind* of improvement `qwen2.5:7b` saw (2.3x), though a lower absolute ceiling. The effect generalizes across at least two models rather than being a `qwen2.5:7b`-specific quirk, which is the real open question this data point answers - it doesn't establish a universal ceiling, since only two of the five models this item has ever tested have real structured-output numbers yet.
     - **Confirmed on a third model (2026-08-11): `llama3.1:8b`, structured output, `--repeat 5`.** `llama31_8b_repeat5.json` kept as the raw evidence. Per-run: 43%, 57%, 57%, 57%, 29% — pooled **17/35 (49%)**, mean of per-run rates 49%. Real tool-call attempts stayed high across every run (5, 5, 7, 6, 5 of 8) with **zero leaked pseudo-tool-call text in all 5 runs, all 40 turns** — the same clean-mechanism pattern `qwen3:8b` showed under structured output, not `qwen2.5:7b`'s occasional prose-leak failure mode.
       - **The improvement generalizes to a third model, but the effect size is model-dependent, not a fixed multiplier.** This model's own native-tool-calling baseline (item 6's "Second real run" entry, above) was the worst of any model tested at just 1/7 (14%) — structured output brings it to 49% pooled, roughly a **3.5x** relative gain, the largest of any model tested so far. But the *absolute* ceiling still lands below the other two structured-output numbers (`qwen2.5:7b` 66%, `qwen3:8b` 57%) — worth being precise about which claim the data actually supports: "structured output helps every local model tested so far, by a large and sometimes very large margin" is well-supported by three independent models now; "structured output reaches roughly the same ceiling regardless of model" is not - `llama3.1:8b`'s own ceiling is real and meaningfully lower.
       - Run against `msi-laptop` (GPU-verified, RTX 2080 Max-Q) after a fresh rsync to bring it current with the two most recent merges (the race system and structured-items features, neither of which touches this harness or `OllamaNarrator`, so their absence wasn't expected to matter, but syncing first avoids trusting a result against a stale worktree regardless).
   - **Structured-output schema parity: `rest`/`notes`/`disposition`/`cast_spell` added (2026-08-20), closing the scope gap the "Honest, not 'solved'" bullet above named at the time.** A real session running under structured output (the default since the seventh experiment above) previously lost access to four real `update_character` fields entirely — no way to rest without guessing an `hp_delta`, no way to record an NPC's notes/disposition, no way to cast a tracked spell — since `_OUTCOME_PROPERTIES` only ever covered `target`/`hp_delta`/`add_condition`, the minimal slice needed to validate the original hypothesis against the harness's own scenario.
     - **Same field shapes as `update_character`'s own tool schema (`server/narrator.py`), not reinvented** — `rest` (`short`/`long` enum, plus an empty-string member so constrained JSON generation has a valid "not applicable" value the way `hp_delta`'s `0`/`add_condition`'s `""` already did), `notes`/`cast_spell` (plain strings, empty when not applicable), `disposition` (`hostile`/`neutral`/`friendly` enum, same empty-string addition). Threaded through every schema built on `_OUTCOME_PROPERTIES` for free — `STRUCTURED_OUTPUT_SCHEMA`, `MISSED_CHANGE_SCHEMA`, `STRUCTURED_OUTPUT_ROLL_SCHEMA`, `STRUCTURED_OUTPUT_FOLLOWUP_SCHEMA` — since none of them redeclare the shared dict.
     - **A real, deliberate broadening of when an update fires, not just new fields bolted onto the existing gate.** `mechanical_change`'s own schema description only ever promised "changes anyone's HP, inventory, or conditions" — gating the new fields behind it too would have meant a turn that's *only* an NPC introduction-with-a-note, or *only* a rest with no separate `hp_delta`, still silently dropping that field even with the schema now able to carry it. `_narrate_structured` now calls `apply_update()` when `mechanical_change` is true *or* any of `rest`/`notes`/`disposition`/`cast_spell` is present — checked independently, same falsy-omission style `hp_delta`/`add_condition` already used.
     - **`_missed_change_update()` renamed to `_outcome_update()` and shared by both call sites, not duplicated a second time.** It already built an identical `update_character`-shaped dict for `check_missed_change`/`propose_correction`; extending it once and having `_narrate_structured` call it too (rather than growing two independently-maintained field lists) means the missed-change review path picks up the same four fields for free — a narration that reads like a missed rest or a missed spell cast can now be corrected the same way a missed hit already could.
     - **Not yet live-verified — unit-tested against a fake Ollama client only, the same honest bound every prior step in this item was held to before its own live run.** The harness (`scripts/live_reliability_check.py`) still only scores `target`/`hp_delta`/`add_condition`/leaked text against its existing 8-turn scenario, which never exercises rest/notes/disposition/cast_spell at all — extending the scenario (or building a second one that does) is a real prerequisite before a `--repeat` run against a real model could say anything about whether local models actually populate these four fields correctly, not just whether the plumbing accepts them when a test double provides them.
     - 11 new tests (`tests/test_narrator_ollama.py`): all four fields applying together, each of `notes`/`disposition`/`rest`/`cast_spell` independently triggering `apply_update` with `mechanical_change: false`, the four fields correctly omitted when sent as empty strings (mirroring the existing zero-`hp_delta`/empty-`add_condition` test), and `check_missed_change` applying `rest`/`notes`/`cast_spell` through the now-shared `_outcome_update()`. Full suite: 612 passing (the same 2 pre-existing root-sandbox `test_persistence.py` `chmod` failures noted throughout this file, confirmed unrelated).
   - **Harness extended to score the four new fields (2026-08-20), closing the prerequisite the entry above named.** A second, additive scenario (`FIELD_PARITY_SCENARIO`, `scripts/live_reliability_check.py`, selected via `--scenario field-parity`) - never mixed into the original 8-turn `SCENARIO`, so every historical pooled/mean-rate number this item cites stays exactly comparable to any future combat-scenario run.
     - **A real character-setup problem solved first, not assumed away**: `rest` needs the acting character below max HP to be observable at all (a "long rest" at already-full HP is a real no-op in `CharacterSheet.apply_update()`, nothing to score either way), and `cast_spell` needs a spellcasting class with real known spells/slots - the original scenario's blank-class Torvin has neither. The new scenario opens with a real self-damage turn (reusing the existing `EXPECT_CALL` machinery unchanged) before the rest turn, and joins as a real `character_class="cleric"` (`FIELD_PARITY_CHARACTER_CLASS`) so `cast_spell: "cure wounds"` resolves against a real, non-cantrip SRD spell with a real level-1 slot to spend (`srd.json`'s own `spell_slots_by_level("1") == {"1": 2}`).
     - **Scored from real session state directly, not from broadcast events - `rest`/`cast_spell` have no dedicated envelope of their own.** `run_scenario()` now snapshots the acting character's `hp`/`spell_slots` immediately before each turn (`session.characters[PLAYER_ID]`, the real object, not a copy) and compares against the same read after the turn: `rest` is correct when HP genuinely increased (and explicitly unscored - `None`, same as an ambiguous turn - when HP was already full, since that case can't distinguish "the model didn't set rest" from "it did, but there was nothing to heal"); `cast_spell` is correct when total spell slots genuinely decreased. `notes`/`disposition` are checked against the real tracked NPC (`session.npcs.get(target.casefold())`) - `notes` correct when non-empty, `disposition` correct only on an exact enum match (`"hostile"`, not just "no longer neutral"), a stricter bar than `notes`'s free text needs.
     - **`write_json`/`write_json_repeat`/`print_report`/`print_repeat_report` all gained a `scenario_name` parameter** (defaults to `"combat"`, so every existing call site and test stayed unchanged) - an evidence file or console header now says which scenario produced it, so a saved `field-parity` report can't be mistaken for a `combat` one later.
     - **Not yet run against a real model** - unit-tested against `ScriptedNarrator` only, same honest bound the schema-parity entry above was held to before this. A real `--repeat 5 --scenario field-parity` run against `qwen2.5:7b` (or another model already benchmarked on the combat scenario) is the actual next step to learn whether local models populate these four fields correctly, not just whether the harness can score them when a script provides them.
      - 8 new tests (`tests/test_live_reliability_check.py`): the field-parity scenario scoring perfectly when every field is set correctly; the rest turn scored `None` (not penalized) when the earlier self-damage call never landed, vs. scored incorrect when healing genuinely was possible and didn't happen; a missing `cast_spell`/`notes` each scored incorrect; a wrong (non-matching) `disposition` value scored incorrect even though the NPC was still introduced; the plain no-call control turn still scoring correctly; and `write_json` recording the real scenario name. Full suite: 20/20 in `test_live_reliability_check.py`.
    - **Missed-change/correction paths taught about the four new fields (2026-08-21), closing a real drift the schema-parity entry above left behind.** The schema (`MISSED_CHANGE_SCHEMA`) gained `rest`/`notes`/`disposition`/`cast_spell` for free via shared `_OUTCOME_PROPERTIES`, and `_outcome_update()` maps them — but both review prompts (`MISSED_CHANGE_SYSTEM_PROMPT`, `PROPOSE_CORRECTION_SYSTEM_PROMPT`) still told the model to fill in only `target`/`hp_delta`/`add_condition`, so a constrained model had no instruction to ever populate the other four; and both call sites (`check_missed_change`/`propose_correction`) gated strictly on `mechanical_change` while `_narrate_structured`'s own trigger was deliberately broadened to fire on any of the four independently — meaning a notes-only or rest-only review response would have been built by `_outcome_update()` and then silently discarded. Exactly the two-copies-drift shape item 15 already fixed once elsewhere.
      - **One shared gate instead of three copies**: `_has_outcome_change(data)` (`server/narrator_ollama.py`) is now the single apply/propose predicate — used by `_narrate_structured`, `check_missed_change`, and `propose_correction` alike — and both review prompts document the four fields with the same rules the main structured prompt uses (rest instead of guessing hp_delta; cast_spell only the acting character's own known spells; notes/disposition as a real correction independent of mechanical_change). The Anthropic side's `check_missed_change` prompt got the matching one-line widening (its tool schema always carried all fields; its prompt never mentioned them), and `NarratorBackend.propose_correction`'s docstring no longer names just the original three.
      - **Deliberately not touched: `POSSIBLE_UNTRACKED_CHANGE_PATTERN`** — it still keys on damage/condition language only, so a missed *rest* whose narration says nothing regex-matches never reaches these review calls at all. Widening it is a live-behavior tradeoff (every incidental match on an innocent turn costs an extra model call and risks a spurious yellow advisory when the DM declines) that can't be validated without a real backend; noted here rather than guessed at.
      - **2 new tests** (`tests/test_narrator_ollama.py`): a notes-only correction (`mechanical_change: false` + `notes` set) now applies through `check_missed_change` and returns true; a rest-only proposal survives `propose_correction`'s gate — each also asserting the review system prompt actually names all four fields, so this exact drift can't silently reappear. Full local suite: 625 passing (the same 2 pre-existing root-sandbox `test_persistence.py` chmod failures).
   - Remaining candidates from the original brainstorm, now cheap to test through this harness:
     - **Rule-adherence robustness against a determined player.** Research this session (`arxiv.org/html/2607.02802v1`, "Seduced by the Narrative") found LLM adjudicators are vulnerable to "rhetorical injection" — a player using pseudo-logical or authoritative-sounding framing to talk the DM into bending rules it would otherwise enforce. Not urgent for solo play with a trusted single player, but worth keeping in mind given Oracle's stated "grounded in real rules, not just vibes" goal — nothing currently defends against it, and it's a distinct failure class from the missed-call problem this session has mostly focused on (a *confident wrong* adjudication, not a *missing* one). Genuinely lower priority until multiplayer or a less-trusted player context makes it more likely to matter.
     - [x] **Structured output / JSON mode instead of free-form tool-calling** — done (2026-08-09), see the "Seventh experiment" entry below - a real, substantial improvement, not just a candidate anymore, now the default for `OllamaNarrator`.
     - [x] **Static few-shot example in the system prompt — tried live, a null result, kept opt-in-only, not shipped as the default (2026-08-12).** `STRUCTURED_OUTPUT_FEW_SHOT_EXAMPLE` (`server/narrator_ollama.py`) - a single worked structured-output example (correct `target`/`hp_delta` for an NPC hit, explicitly demonstrating the self-vs-NPC mistargeting failure mode that's recurred across every experiment in this item) appended once to the base structured prompt, gated by a new opt-in `few_shot_example` constructor flag and the harness's own `--few-shot`. **A real, direct A/B against `qwen2.5:7b`, `--repeat 5` both ways, same session so sampling conditions match**: baseline (no example) pooled **20/35 (57%)**, per-run spread 43/43/57/71/71%; with the example, pooled **21/35 (60%)**, spread 43/57/71/71/57%. A one-turn difference on 35 turns, well inside the noise this project's own repeat methodology already documents (both conditions span the same 43-71% range) - not a real effect, just favorable sampling on one side. **Decision: kept as a tested, documented, opt-in flag - not flipped to the default**, the same "only ship what measurably helps under real-engine testing" standard item 5's reminder experiment and item 6's split-request/history-window experiments were already held to. Also incidentally re-confirms structured output's own baseline is noisier run-to-run than a single historical number suggests: this session's fresh 57% baseline sits notably below the 66% figure this item's seventh experiment originally reported for the identical config, real variance rather than a regression - a reminder that any single `--repeat 5` result is still a sample, not a ceiling. 3 new tests (`tests/test_narrator_ollama.py`): the flag defaults off, opting in augments the base structured prompt with the example, and the example deliberately doesn't reach the roll/followup prompt variants. Full local suite: 576 passing (up from 573).
     - [x] **A stronger, confirmable version of the just-shipped visible-mitigation heuristic** — done in two slices: a silent DM self-correct attempt first (item 17, 2026-08-12), then the confirmable `/apply`-able proposal for when that declines (item 22, 2026-08-19) — see both entries below in this same list.
     - **A cheap large-model calibration point that needs neither GPU nor Anthropic credits** — every "does scale help" test so far has stayed within the 7-14B range this machine can run locally (item 6's `qwen2.5:14b` control). A hosted API call to a genuinely large open model (e.g. Llama 70B-class via Groq/Together) would answer whether local-model-scale specifically is the ceiling, or whether the task itself is just hard for anything at this size class, without waiting on the GPU migration or spending Claude credits meant for the Anthropic-backend verification (item 2).
     - **A model self-reported confidence signal** ("did anything mechanical happen this turn?") as a second detection channel alongside the just-shipped regex heuristic's outcome-language pattern — would catch phrasing the keyword pattern misses (the heuristic's own documented false-negative risk), at the cost of needing the model to reliably answer a yes/no question, which is its own open reliability question.

7. **Tabbed character sheet UI, design pass (2026-08-09).** From a direct owner request after using the client: the current `CharacterSheetPanel` is a single `Static` widget rendering everything as one long scrolling text blob, and the owner wants real per-category detail (stats, inventory, abilities/features) the way a physical tabletop character sheet has it, plus asked separately whether there's room for a map/visual panel alongside the narration log. Two genuinely different scopes — this item covers the tabbed sheet; the map/visual question is scoped separately below since it's a bigger, more speculative lift.
   - **Real, not cosmetic — checked what data already exists first.** `server/rules/srd.json` already has real class features per class (e.g. wizard's "Arcane Recovery", via `level_1_features`) that are **not currently sent to the client at all** — a Features tab has real content to show, but the payload needs to grow to carry it. Skill proficiencies are the same story: `CLASS_SKILL_PROFICIENCIES` (`server/engine.py`) already exists server-side and drives real roll bonuses, but there's no persistent "your proficient skills" list sent to the client today — only ever surfaced transiently in a roll's own label text after the fact. Race, by contrast, genuinely doesn't exist anywhere in this project's model — no race field, no racial traits in the SRD data - so a tab can't show lineage/racial traits (the Stormlight-style ask) without first building a whole race system. **Decision: ship the tabbed UI against what's real now (class features, skills, spells, inventory, stats) and explicitly defer a race system as its own separate, later candidate** rather than block or fake this feature on data that doesn't exist yet.
   - **Tab breakdown**: Overview (name/class/level/XP/HP/AC — what's currently visible without switching anything), Abilities (ability scores + modifiers, saving throws, skill proficiencies once exposed), Inventory, Spells (only shown at all when `known_spells` is non-empty, matching the current single-panel behavior), Features & Notes (class features once exposed, conditions with their real effects, the free-text `notes` field).
   - **Privacy boundary stays exactly where it is.** Tabs apply only to the owner's own full sheet panel; other players' party rows stay name+HP only (`_public_character_view`'s existing boundary, server/engine.py) — this feature doesn't expand what's shared, only how the owner's own detail is organized.
   - **Re-render all tabs on every update, not lazily.** A `character_update` event should refresh every tab's content from the full character dict each time, not just the currently-visible one — otherwise switching to a stale tab after an off-screen HP/condition change would show old data until the next update arrives. Simpler and safer than a lazy-render-on-switch model, and cheap (string building, not real computation).
   - **Keybinding, not just mouse.** This is a type-and-enter interface — plain number keys can't drive tab switching since they need to stay typeable inside the action input box. Recommend `ctrl+left`/`ctrl+right` to cycle tabs (unlikely to collide with anything typed) alongside Textual's native clickable tab headers, which `TabbedContent` provides for free.
   - **Terminal size**: verify readable at both the existing screenshot size (120×40) and this project's documented 80×24 minimum before calling it done, not just the wider size.
   - **Test/screenshot fallout, planned up front rather than discovered after**: `tests/test_client_app.py`'s sheet-rendering tests currently query the single `Static`'s rendered text directly and need to move to querying `TabbedContent`/`TabPane`s by id; `scripts/generate_screenshots.py` needs to confirm which tab is active by default when capturing (Overview), and a new screenshot on a secondary tab (e.g. Spells) would actually show the feature off rather than just proving it doesn't crash; `docs/protocol.md`'s character payload documentation needs updating once features/skill-proficiencies are added to it.
   - **Checklist, implemented (2026-08-09)**:
     - [x] Add `level_1_features` (per class) to the owner-only sheet payload — landed as a new `_owner_character_view()` (`server/engine.py`), not a change to `_public_character_view` itself (that stays the *public* side; the owner's own view needed its own function, layering these two new fields on top of `model_dump()`).
     - [x] Add a persistent skill-proficiency list to the same payload, derived from `CLASS_SKILL_PROFICIENCIES`.
     - [x] Restructure `CharacterSheetPanel` (`client/app.py`) from one `Static` into `TabbedContent`/`TabPane`s per the breakdown above (Overview/Abilities/Inventory/Spells/Features & Notes).
     - [x] Wire tab-cycle bindings — **`pageup`/`pagedown`, not `ctrl+left`/`ctrl+right`** as first planned here. Checked Textual's actual `Input.BINDINGS` before implementing rather than assuming, and found `Input` (where focus normally sits) already claims `ctrl+left`/`ctrl+right` for word-cursor movement - binding the same keys for tab-switching would have silently broken editing in the one widget doing the actual work. A real example of why this session's own "verify before implementing" habit matters even inside a single checklist item.
     - [x] Re-render every tab's content on each `character_update`, not just the active one — `_refresh_display()` rebuilds all five panes unconditionally on every `render_sheet`/`render_world`/`render_npcs`/`render_others` call.
     - [x] Verify at 80×24 and 120×40 — both render cleanly; at 80×24 with a narrower sheet column, Textual's own tab bar scrolls horizontally once more tabs are active than fit (standard `Tabs` behavior, not a bug) - `pageup`/`pagedown` cycling still reaches every tab regardless of what's currently visible in the tab bar.
     - [x] Update `tests/test_client_app.py` for the new `TabbedContent` structure — the ~10 tests that used to reach into `Static`'s private `_Static__content` now call a new `CharacterSheetPanel.all_text()` helper (concatenates every tab's content) instead; 5 new tests cover class features/skill proficiencies/notes rendering, Spells-tab show/hide, and `pageup`/`pagedown` cycling (including skipping the hidden Spells tab). 4 new `tests/test_engine.py` tests cover `_owner_character_view()` itself (real class features/skills, still includes everything `model_dump()` had, graceful on a blank/unrecognized class, and the owner-only privacy boundary via a real `state_sync`).
     - [x] Update `scripts/generate_screenshots.py` — no change needed beyond what spellcasting's own screenshot work already did; Overview is `TabbedContent`'s `initial=` tab, so every existing screenshot's default view is unaffected.
     - [x] Update `docs/protocol.md` for the grown character payload — new "Tabbed character sheet UI" section plus a "Private vs. shared state" bullet for the two new owner-only fields.
     - [x] Explicitly not in scope: a race/racial-traits system (separate, later candidate — see below).
     - **Also closed a real pre-existing gap found along the way, not part of the original ask**: `notes` (settable via `/note`) was never rendered anywhere on the sheet before this - the new Features & Notes tab is the first place a player can actually see what they wrote.
     - Full local suite: 378 passed (up from 369).

8. **Map/visual panel alongside the narration log, scoped separately (2026-08-09).** Same owner request as above, deliberately split out since it's a different size of feature — the sheet tabs above are a pure UI restructuring of data that already exists; this needs new data modeling and, for real images, a terminal capability this project can't assume. Two shapes, not one:
   - **A lightweight ASCII/symbolic map** (rooms + exits, maybe a simple grid) is buildable now and fits Oracle's existing "engine owns the source of truth" pattern (a new field on `WorldState`, updated by the DM the same way `update_world`'s objectives/location already work). This is the realistic near-term version.
   - **Real image rendering in-terminal** needs a terminal image protocol (Kitty graphics, Sixel, iTerm2 inline images) that not every terminal supports, and is really the *display* half of the already-deferred "Image generation" item (Later, below) — it needs a real generation pipeline behind it (GPU-backed, per that item's own scoping) to have anything to display, not just a widget. Not worth building the display half before the generation half exists.
   - **The ASCII/symbolic half, implemented (2026-08-09).** `WorldState` gained `location_map: dict[str, list[str]]` - a plain adjacency list (location name -> its real exits), not a 2D grid, since a grid would need the DM to supply consistent x/y positions this project doesn't ask for anywhere else. `update_world` gained `add_location` and `connect_locations` (a 2-element array, always symmetric - a self-connection is a deliberate no-op guarding against corrupting the same list object twice), `AnthropicNarrator` only, same gating every other `update_world` field already has. Rides the existing `world_update` broadcast - no new envelope. `CharacterSheetPanel` gained a sixth tab, "Map", hidden until the DM has registered at least one location (via the same `hide_tab`/`show_tab` pattern the Spells tab already established), marking the character's current location and listing each known location's real exits.
     - **6 new tests**: 4 in `tests/test_state.py` (`add_location`, `connect_locations` is two-way and registers new endpoints, a repeat call is a real no-op rather than duplicating the edge, a self-connection is ignored rather than corrupting the list), 1 in `tests/test_engine.py` (a real `update_world` tool call reaches the `world_update` broadcast, not just `WorldState.apply_update` in isolation), 1 in `tests/test_client_app.py` (the Map tab's hidden-to-shown transition, `(here)`/exit rendering) - plus an existing tab-cycling test's own comment corrected to note it already exercised skipping the newly-hidden Map tab too, not a new test itself.
     - **Real image rendering stays deferred**, exactly as scoped above - needs a terminal graphics protocol and a real generation pipeline, neither of which exist yet.
     - Full local suite: 384 passing (up from 378, item 7's own count).

9. **Equipped gear: weapon/armor slots distinct from carried inventory (2026-08-09).** A direct owner ask after using the tabbed sheet: inventory shouldn't be "just a general listing of items" - closely resembling a real tabletop character sheet (their own comparison: D&D, Stormlight RPG) means distinguishing what's equipped from what's just carried.
   - **`CharacterSheet` gains `equipped_weapon`/`equipped_armor`** - pointers into `inventory` by name, not a separate item store. Deliberately still plain name strings: the owner flagged, unprompted, that equipment will eventually need real special properties (magic items, unique loot found/crafted/looted) - that would need `inventory` to become a list of structured item objects, not just names, a genuinely bigger schema change than this pass. Named and explicitly deferred here rather than half-built, the same scoping discipline every other feature this session has followed (concentration in spellcasting, a race system in the tabbed sheet).
   - **Auto-equipped at character creation**, not left unequipped until a player remembers `/equip` - `_auto_equip_starting_gear()` picks the first weapon-like (`damage` field) and first armor-like (`ac` field) item out of the class's starting inventory. Starting AC is unchanged from before this feature.
   - **A real, closed gap: AC now recomputes on every equip/unequip**, not just once at creation - `_compute_ac()`'s signature changed from scanning the whole inventory for the best armor found (a real latent bug: any armor anywhere in inventory silently counted toward AC, whether "worn" or not) to taking the single `equipped_armor` name - only what's actually equipped affects AC now.
   - **`character_edit` gains `equip`/`unequip`**, both backends (player-side bookkeeping, not a DM tool, same exemption `add_item`/`remove_item`/`notes` already have). `equip` resolves weapon-vs-armor from real SRD data, warning rather than silently no-opping on an unowned item or one that's neither. Removing an equipped item also unequips it, so the pointer never dangles.
   - **A real public broadcast fired only when it matters**: equipping/unequipping armor sends the public `player_update` (ac is public, same as hp), a weapon swap doesn't (never touches ac) - not every character_edit blindly broadcasting, matching this project's existing "only broadcast a real change" discipline.
   - **Client**: Inventory tab now shows Equipped (Weapon/Armor, `(none)` when empty) separate from Carried; `/equip <item>`/`/unequip <item>` commands.
   - **Not built (yet)**: structured item objects with real special properties (the owner's own flagged future need, above), showing a weapon/armor's real stats (damage die, AC bonus) directly on the sheet instead of just its name, and equipment slots beyond one weapon/one armor (off-hand, shield, accessories).
   - **13 new tests**: `tests/test_engine.py` gained 10 and lost 1 (net +9) - auto-equip at creation, equip switches weapon without touching AC, equip armor recomputes AC and broadcasts `player_update`, equip an unowned item warns, equip something that's neither weapon nor armor warns, unequip clears the slot and recomputes AC, unequip something not equipped warns, removing an equipped item also unequips it, and `_compute_ac`'s single-armor-name signature replacing its old whole-inventory-scan test. `tests/test_client_app.py` gained 4 (Equipped/Carried split, empty-slot `(none)` rendering, `/equip`/`/unequip` command wiring).
   - Full local suite: 397 passing (up from 384, item 8's own count).

10. **Critical hits and a color-coded combat outcome log (2026-08-09).** A direct owner ask after using the client: attacks/spells didn't say if a hit was critical, didn't clearly state how much damage was done or what happened to the target beyond whatever the DM's own prose happened to describe, and had no color differentiation between damage/healing/spells/items at a glance.
    - **Critical hits**: a natural 20 on a real attack roll (`roll_kind == "attack"`, the *kept* d20 under disadvantage - mirrors the existing kept-vs-discarded narrowing this file's disadvantage-highlighting logic already uses) is a critical hit. `dice_result` gains an optional `critical` field; the DM's own tool_result text gets a `CRITICAL HIT!` suffix too, so the model narrates knowing it happened. Client shows a distinct bold-red callout, separate from the existing green natural-max digit highlight (which fires for any roll's nat max, including an unrelated skill check - a great check isn't a critical hit). **Deliberately not automatic damage-doubling** - real 5e's other crit effect needs the engine to correlate a confirmed crit with a *separate, later* `request_roll` for that attack's damage, a genuinely bigger mechanic than detecting the crit itself; named and left for a real follow-up rather than half-built.
    - **Combat outcome log**: `CharacterSheet.apply_update()` already computed a real, deterministic change summary (`"HP -5 (now 5/10)."`, `"gained 'a torch'."`) for the DM's own tool_result - it just never left that private channel to reach the visible game log. New `_outcome_category()` picks one dominant category per real change (damage/heal beat condition/spell/item when a call combines several, e.g. a poisoned dart's hp_delta + add_condition together), and the engine now broadcasts a `kind: "outcome"` log line with that category, for both the acting character's own changes and any NPC's. `None` category (e.g. only notes/disposition changed) means no line at all - not every change needs a spotlight.
    - **Client color-codes by category**: damage red, heal green, spell cyan, condition magenta, item yellow (`_OUTCOME_COLORS`) - an unrecognized/missing category still shows the line, just uncolored.
    - **12 new tests**: `tests/test_engine.py` gained 8 (4 critical-hit detection cases - a real attack crit, a check's nat 20 correctly isn't one, a non-d20 attack notation isn't one, disadvantage uses the kept not discarded roll; 1 parametrized `_outcome_category` unit test covering all 12 category/priority combinations; 3 integration tests - a real hp_delta broadcasts a colored line, a notes-only change broadcasts nothing, an NPC's own damage is named for the NPC not the player). `tests/test_client_app.py` gained 4 (critical-hit callout shown/not-shown, outcome log line colored by category, an unrecognized category still shows uncolored).
    - Full local suite: 420 passing (up from 397, item 9's own count).

11. **SRD dataset expansion: 5 more monsters, 4 more spells, 4 more equipment items (2026-08-09).** Content-only work (no engine/protocol changes) — the "Expand the SRD dataset" candidate from the Later section below, picked as the recommended next step after a long run of feature-building, with the owner going to playtest everything shipped so far in parallel.
    - **5 new monsters** (`server/rules/srd.json`), spanning the low end of the CR range this project's scenarios actually use: Bandit (CR 1/8 — also the exact name this file's own reliability-testing scenario has used as its placeholder target all session, previously never a real stat block), Kobold (CR 1/8), Giant Rat (CR 1/8), Black Bear (CR 1/2), Ogre (CR 2). All real SRD 5.1 stat blocks (stats, AC, HP, traits, actions), not invented — matches `ATTRIBUTION.md`'s existing CC-BY-4.0-only scope.
    - **4 new spells**: Burning Hands and Misty Step for wizard, Healing Word and Spiritual Weapon for cleric — added to both `srd.json` and `CLASS_KNOWN_SPELLS` (`server/engine.py`), so they're not just inert data but actually castable. Burning Hands is save-based (`save_ability: "dex"`, matching Sacred Flame's existing shape); Spiritual Weapon is attack-based (`attack: true`, matching Fire Bolt's); Healing Word and Misty Step are pure-utility (no damage/attack/save field), matching Guidance/Mage Armor's existing shape.
    - **4 new equipment items**: Dagger, Mace, Light Crossbow (weapons, back the new monsters' own actions and give players more `/equip` variety), Studded Leather (light armor). **Deliberately did not add heavier armor** (chain mail, scale mail) — `_compute_ac()`'s formula (`base + dex_modifier`) has no support for a DEX cap or a flat no-DEX-bonus armor type, a real, already-documented limitation (see the Structured Equipment entry, Current state above). Adding an armor type the formula computes *wrong* would be a real correctness bug, not just incomplete coverage — left out rather than shipped broken. Same reasoning for shields: a shield's real +2 AC is additive on top of body armor, not a replacement base value, which the current single-`equipped_armor`-slot model can't represent at all.
    - **A pure insertion diff, not a reformat** — the file mixes compact one-line `stats`/`classes` fields with multi-line `traits`/`actions` blocks; a first pass that round-tripped the whole file through `json.load`/`json.dump` reformatted every existing entry to a uniform style (483 lines touched for what should've been ~170 lines of real additions) before being caught and reverted in favor of surgical text insertion at the right points instead.
    - **3 new tests** (`tests/test_engine.py`): a spell-attack-resolution test for Spiritual Weapon (mirrors Fire Bolt's own coverage — real dice/damage_type/ability/roll_kind resolution, not just that the data loads), a cast_spell slot-consumption test for Burning Hands (confirms it's reachable via `CLASS_KNOWN_SPELLS`, not just present in `srd.json`), and a defeat/XP-award test for Bandit (confirms the new CR integrates with the real `xp_by_cr` table and the stat block's AC gets copied onto the introduced NPC). Two existing tests updated (the wizard/cleric starting-character spell-list assertions, to include the newly-known spells).
    - **A real, pre-existing test caught and fixed along the way, not introduced by this change**: `test_start_combat_dex_modifier_fallback_for_npc_without_stats` used `"bandit"` as its example of a name that matches *no* known SRD monster, to exercise the DEX-modifier fallback path for an NPC with no real stats. Adding a real "Bandit" monster entry (above) made that assumption false - the test started getting real stats instead of the empty-fallback case it meant to cover. Renamed the test's NPC to `"cutpurse"` (genuinely unmatched) rather than leaving a real bug in the fallback coverage silently unexercised.
    - Full local suite: 423 passing (up from 420, item 10's own count).

12. **Owner playtest findings, not yet actioned (2026-08-09).** A real dry-run playtest (client run against `msi-laptop`) surfaced several things — some are a stale-client artifact, two are real, verified gaps in the current code itself. Logged here in full rather than fixed in a rush right before a session/terminal clear.
    - **No tabs, no map, no color observed — almost certainly stale code on `msi-laptop`, not a regression.** The last rsync to that machine predates the tabbed sheet (item 7), map panel (item 8), and combat-feedback color-coding (item 10) - all landed in commits after that sync point. Needs a fresh rsync (the exact command from earlier in this session, excluding `.git`/`.venv`/`sessions`/`__pycache__`/`.pytest_cache`/`*.pyc`/`.env`) before trusting a playtest against that machine again. **Not yet confirmed** this is the actual explanation - worth verifying `msi-laptop`'s checked-out state before assuming, rather than treating this as settled.
    - [x] **A real, verified gap: no persistent "whose turn" indicator - fixed (2026-08-10).** Confirmed by reading the code, not just inferred from the report: `client/app.py` never read `current_turn` from `state_sync`'s payload at all - the only turn indication anywhere was the one-shot `turn_prompt` handler, which wrote a single `"Your turn."` / `"{name}'s turn."` log line and nothing else. That line was easy to miss if it scrolled by, and was never re-shown on reconnect - `state_sync` carried `current_turn` but nothing rendered it, so a rejoining player had no way to tell whose turn it was without asking or waiting for the next `turn_prompt`. The owner's own framing: "maybe have some indicator somewhere ... if waiting for response from DM or processing for LLM" - `#status` (`client/app.py`) already existed and already did exactly this for one case (`"The DM is thinking..."`, set/cleared around each `narrate()` call), the natural fix extends that same persistent indicator to also show turn state whenever the DM isn't actively composing.
      - `DungeonMasterApp` gained `self.current_turn` (set from `state_sync`'s and `turn_prompt`'s own `current_turn`/`player_id` fields) and a new `refresh_turn_status()` method (mirrors `refresh_sheet_widgets()`'s existing safe-no-op-if-not-mounted pattern via `_try_query_one`), writing `"Your turn."` or `"<name>'s turn."` to `#status`. `SessionScreen.set_thinking(False)` now calls it instead of just blanking `#status` - the same call sites (every narration chunk, narration failure) that used to clear the indicator now restore it, so the two states (thinking vs. whose turn) never fight each other. Also refreshed directly in `SessionScreen.on_mount()`, closing the reconnect gap specifically - the indicator is now correct the instant the session screen mounts, not just after the next `turn_prompt`.
      - 4 new tests (`tests/test_client_app.py`): the indicator appears on a fresh state_sync (both own-turn and another-player's-turn cases) and survives a full narration streaming/finishing cycle without being left blank. 496 tests pass project-wide. Live-verified against the real GPU-hosted model via a real driven `DungeonMasterApp`/`Pilot` (not just protocol-level checks, since this is a pure client-rendering fix) in an isolated worktree: `#status` correctly read `"Your turn."` after the opening scene, switched to `"The DM is thinking..."` the instant a real action was submitted, and correctly restored to `"Your turn."` once that turn's real narration finished streaming.
    - [x] **Which client-visible commands should the player actually see listed - resolved and fixed (2026-08-10).** The owner liked the greyed placeholder-text command hints in principle but felt some of the currently-listed ones shouldn't be visible/known to the player. Asked directly rather than guessed at: the owner confirmed the earlier candidate guess - `/item add`/`/item remove`/`/equip`/`/unequip` read as cheat-like bookkeeping that should be discovered organically or handled narratively - **and** picked up two more of their own: `/note` (meta, not an in-fiction action) and `/export`/`/transcript` (file-saving utilities, functional rather than table-talk). Only `/roll` and `/chat` are genuine table-talk actions left advertised.
      - Purely a hint-text change, `client/app.py` - every hidden command still works exactly as before (`on_input_submitted`'s own dispatch is untouched); a player who already knows `/equip` from a prior session, an export file, or simply guessing can still use it. `SessionScreen`'s input hint is now `"What do you do? (/roll, /chat)"`; `LobbyScreen`'s chat-input hint drops its `/export`/`/transcript` mention entirely, since `/chat` itself has no dedicated command there (it's the whole point of that input).
      - 2 new tests locking in the visible/hidden set on each screen's own hint text, so this can't silently drift back. 498 tests pass project-wide. No live GPU verification needed - a static placeholder-text change has no server or LLM-dependent behavior to verify beyond what the tests already cover.
      - **Closes the full 2026-08-09 owner playtest findings pass** - the stale-`msi-laptop` note was informational only (no code gap), the persistent turn indicator and this command-visibility question were the two real, actionable items, and both are done now.

13. **A real race system, design pass and implementation (2026-08-11).** Picked up the explicitly-deferred candidate named twice already (item 7's tabbed-sheet scoping decision, item 9's equipped-gear precedent) - Oracle had no race field or racial-trait data anywhere in its model until now.
    - **Scope, decided up front rather than discovered mid-build**: the SRD's four core playable races - Human, Elf, Dwarf, Halfling - mirroring the project's existing four-class scope, no more. **Deliberately no subraces** (Hill vs. Mountain Dwarf, High vs. Wood Elf) - named and deferred the same way concentration/structured-items/race-itself were each deferred earlier, not half-built. Race is optional (blank = no bonus/traits, the same graceful degradation a blank/unrecognized `character_class` already has) and picked via a guided RadioSet picker rather than a free-text field alongside class's own `#class-input` - a genuinely different UX call than class's own survey-plus-text-field pattern, made deliberately smaller since race is mostly flavor, not a differentiated mechanical choice the way class is.
    - **Data**: `server/rules/srd.json` gained a `races` table, sibling to `classes` - each entry has a real `ability_score_increase` dict (e.g. dwarf's `{"con": 2}`) and `traits` (real SRD trait text, same expanded-list style `level_1_features` already uses). `ATTRIBUTION.md` updated alongside it, same discipline the equipment/shield expansions already followed.
    - **Server**: `CharacterSheet.race` (`server/state.py`) mirrors `character_class` - a plain string, blank by default. `build_starting_character` (`server/engine.py`) takes a new `race` parameter, independent of `character_class` (a classless character can still have a race recorded, and vice versa); a new `_apply_race_bonus()` stacks the race's ability bonus additively on top of whatever the class-priority Standard Array already produced, feeding into starting HP the same way a class's own CON modifier already does. An unrecognized race gets the same live-found-gap treatment `#50`'s unrecognized-class warning already established - a private warning, not a silent blank fallback. `_owner_character_view` gained `racial_traits` (owner-only, same pattern `class_features` already set) and `_public_character_view` gained `race` itself (public, same "visible fluff" treatment `character_class` already gets - another player's race is real information visible at the table).
    - **Client**: `WelcomeScreen` gained a race picker - a single "Not sure? Browse races" toggle button revealing a hidden `RadioSet` of the four races with a one-line trait teaser each; `_join()` reads whichever button ends up pressed directly (no separate pre-filled text field, unlike the class survey). A new `_race_class_label()` helper combines race + class into one label (`"Dwarf Fighter"`) for both the owner's own sheet header and the party-list line. `CharacterSheetPanel`'s Features & Notes tab gained a Racial Traits section, same rendering pattern `class_features` already has.
    - **A real, previously-latent CSS specificity bug found and fixed along the way, not introduced by this feature**: `#welcome-box Input`'s own `margin-bottom: 1` (an ID+type selector) was silently outranking a bare `#stat-priority-input { margin-bottom: 0 }` override (ID-only, lower specificity) - `#join` was already sitting flush against this screen's very last visible row at a standard 80x24 terminal before the race picker added anything at all, confirmed directly via widget `.region` inspection, not assumed. Rewritten as the two-ID form `#welcome-box #stat-priority-input`, matching `#mode-select`'s own already-correct precedent - reclaims exactly the one row the race picker's own toggle button needs, so this screen's zero-slack row budget (see the many prior `#survey-toggle`/`#stat-priority-input` compaction comments this file's own commit history has accumulated) still holds.
    - `docs/protocol.md` updated: `race` joins `join_session`'s payload and every public-view envelope (`player_update`, `player_joined`, `state_sync`'s non-owner entries); `racial_traits` joins the owner-only `class_features`/`skill_proficiencies` list, same envelope, no new event type.
    - **19 new tests**: `tests/test_engine.py` gained 9 (race ability-bonus stacking onto the class array and its HP effect, blank/unrecognized-race fallback, race recorded independent of a blank class, the same end-to-end `join_session` coverage `character_class` already has including its own unrecognized-race warning and blank-is-not-a-mistake case, `_owner_character_view`'s `racial_traits`, `_public_character_view`'s `race`). `tests/test_client_app.py` gained 10 (`_race_class_label`'s own combining/fallback logic, the picker's reveal/hide toggle, picking a race and having it actually sent on join, changing a pick before joining sends the latest one, never opening the picker sends a blank race, the Features tab's Racial Traits section shown/hidden, and the combined race+class label rendering on both the owner's own overview header and the party list). Full local suite: 554 passing (up from 533).

14. **Structured item objects with real special properties (2026-08-11).** The gap named and explicitly deferred twice already (item 9's own "equipped gear" entry, and again in `docs/protocol.md`'s former `equipped_weapon`/`equipped_armor` note) - `inventory` was a flat list of plain name strings, so two potions of healing were two identical entries and nothing could represent a magic item's real bonus.
    - **Design pass surfaced one genuinely ambiguous question, resolved with the owner directly before writing any code**: how does a magic bonus actually get *onto* an item? The SRD catalog already has generic magic items (`+1 Weapon`, `Cloak of Protection`, `Ring of Protection`, ...) but they're completely inert - pure description text nothing ever reads mechanically, and `equip`'s own weapon/armor detection (by `damage`/`ac` field presence) can't even classify them as equippable. Two real options: special-case the existing generic catalog entries (no tool-schema growth, but only ever grants an abstract "+1 Weapon", never a specifically-enchanted item, and needs real equip-logic special-casing), or grow `update_character`'s `add_item` with a new optional `magic_bonus` field so the DM can narrate finding a real `+1 Longsword` directly. **Went with the schema-growth option, the owner's own pick** - more realistic (any base item can be enchanted) and generalizes cleanly, at the cost of one more optional field on an already-reliability-challenged tool - a real, deliberate tradeoff on this project's own central investigation (item 6), not a free choice.
    - **`CharacterSheet.inventory` is now `list[InventoryItem]`** (`server/state.py`), not `list[str]` - each entry is `{name, quantity, magic_bonus}` (`quantity`/`magic_bonus` both default to values that make an entry behave exactly like the old plain-string model: one ordinary, non-magical item). New `find_item()`/`add_item()`/`remove_item()` methods centralize the stacking logic in one place (the same "one function defines the shape" pattern `_public_character_view`/`_owner_character_view` already established) - `add_item()` merges onto an existing stack only when both `name` *and* `magic_bonus` match (a mundane and a `+1` copy of the same base item are two separate stacks, not one), `remove_item()` decrements before dropping a stack at zero. `equipped_weapon`/`equipped_armor`/`equipped_shield` stay plain name strings, resolved against `inventory` via `find_item()` - still "whichever stack matches first" for a name shared by more than one stack, a real, named limitation short of a full per-item id system.
    - **`magic_bonus` is DM-only, never player-settable** - `update_character`'s `add_item` gained the optional field (`server/narrator.py`, `AnthropicNarrator` only, same gating every other DM-only field has); the player's own `character_edit` `add_item` never accepts one, the same "engine/DM decides mechanical state, the player only decides fiction/bookkeeping" boundary `equip`/`unequip`'s own `ac` side effect already respects - verified directly (`test_character_edit_add_item_never_grants_a_magic_bonus`), not just assumed from the payload shape.
    - **The bonus is wired into real math, not just stored.** `_compute_ac()` (`server/engine.py`) gained `armor_magic_bonus`/`shield_magic_bonus` parameters, resolved by each caller from the acting character's own `find_item()` and added on top of the SRD base stats. `request_roll`'s weapon-damage resolution resolves the acting character's own carried copy of the named weapon and adds its `magic_bonus` to the damage roll (`dice_result`'s new `weapon_magic_bonus` field) - **deliberately only the damage roll, not also the to-hit roll**: `weapon` has only ever meant "resolve this weapon's real damage die," and a to-hit roll never names a weapon at all today - a real, named structural gap for a later pass, not silently promised here. Since `srd.json` has no armor/shield entries with a magic version of their own yet, the armor/shield half of this mechanism has no data to exercise in practice today - it's real and general (any base item the DM names via `add_item` can carry a bonus), just not yet demonstrated end-to-end the way the weapon-damage path is.
    - **A real, previously-latent bug found and fixed along the way, not introduced by this feature**: `request_roll`'s own closure builds the DM-facing `tool_result` label text, but the *broadcast* plain-text dice log line is built by a completely separate function, `_dice_log_text` (`server/engine.py`) - a second, independent reconstruction of the same label that had silently drifted out of sync with every tag `request_roll` itself already knew how to show (caught here because a fresh `weapon_magic_bonus` tag only showed up in one of the two). Both now stay in sync for this field; whether the other two label-builders (`request_roll`'s own, `_dice_log_text`) should be unified into one shared function instead of two independently-maintained copies is a real, worthwhile cleanup for a later pass, not done here.
    - **Client**: new `_format_item_label()` renders `"Longsword +1 x2"` (name, bonus if nonzero, count if more than one) everywhere an item name shows - Equipped and Carried both. The Carried-list split is stack-aware, not name-aware - equipping one of two same-named stacks (a mundane and a magic copy) only ever hides that one specific stack from Carried, not both.
    - **16 new tests**: `tests/test_state.py` gained 6 (`add_item`'s magic_bonus/outcome-text, stacking identical items, keeping a different `magic_bonus` as a separate stack, `remove_item` decrementing before dropping a stack, `find_item`'s case-insensitivity and blank-name handling). `tests/test_engine.py` gained 6 (`_compute_ac`'s new armor/shield magic-bonus parameters, a magic weapon's bonus reaching a real damage roll end-to-end through `request_roll` while a mundane weapon's roll omits the field entirely, equipping a magic armor item carrying its bonus into a real AC recompute, and the `character_edit`-can-never-grant-a-magic-bonus security-boundary test). `tests/test_client_app.py` gained 4 (`_format_item_label`'s own formatting rules, quantity/magic-bonus shown on both Equipped and Carried, the stack-aware Carried split with two same-named stacks, and the `dice_result` log line's new weapon-magic tag). Full local suite: 570 passing (up from 554).

15. **Unified the two independently-drifting dice-log label builders (2026-08-11).** A direct follow-up to a real bug found while shipping item 14: `request_roll`'s own closure built the DM-facing `tool_result` label text, and a separate `_dice_log_text` (`server/engine.py`) independently rebuilt the same label for the broadcast plain-text log line - two copies of the same logic that had already drifted once (a fresh `weapon_magic_bonus` tag only landed in one of the two, caught by a test).
    - **New module-level `_dice_roll_tags(roll: dict) -> str`** (`server/engine.py`) is now the single source of the shared descriptive tag suffix (damage type, weapon magic bonus, ability modifier, skill/spell proficiency, roll kind, tracked-condition disadvantage) - both `request_roll` and `_dice_log_text` call it against the same `roll`-shaped dict `rolls_made`/`_dice_result_envelope` already use, instead of each rebuilding it from scratch. Deliberately still leaves `reason`/`purpose` and the critical-hit callout to each caller - a real, deliberate difference (the DM already knows why it asked for a roll, so its own `tool_result` never echoes `reason` back; the broadcast line does, since the player has no other way to know it), not something to unify away.
    - **Auditing the two old copies side by side surfaced two more real, previously-silent drifts, not just the one already known**: `_dice_log_text` never showed the `"CRITICAL HIT!"` callout at all (the DM's own `tool_result` always did), and its `roll_kind_label` never had the proficient-save special case (`"(save, +2 proficiency)"`) `request_roll`'s own copy already carried - a proficient save's broadcast log line was silently missing real information the `dice_result` payload and the DM's own text both already had. Both fixed as part of this unification, not left standing now that the shared function makes them visible.
    - **No new test functions - 2 existing tests extended with the regression assertions these two fixes need**: `test_request_roll_natural_20_attack_is_a_critical_hit` (`tests/test_engine.py`) now also asserts `"CRITICAL HIT!"` reaches the broadcast `log_entry` text, not just the DM's own `tool_result`; `test_request_roll_save_adds_proficiency_bonus_when_class_is_proficient` now also asserts `"(save, +2 proficiency)"` reaches the same broadcast text. Full local suite: 570 passing (unchanged - a pure refactor plus two bug fixes, no new test count).

16. **Race subraces (2026-08-11).** Item 13's own explicitly-named-and-deferred candidate: real SRD subraces for the three races that have one - High Elf/Wood Elf, Hill Dwarf/Mountain Dwarf, Lightfoot Halfling/Stout Halfling (Human has no subrace in the SRD - a real asymmetry, not a gap left uncovered).
    - **Each subrace is a fully self-contained `server/rules/srd.json` `races` entry, not a base race plus a stacked delta.** `hill_dwarf`'s own `ability_score_increase` is `{"con": 2, "wis": 1}` - already the combined base-dwarf-plus-subrace total - and its `traits` list already includes both dwarf's own base traits and Hill Dwarf's own (Dwarven Toughness). **`server/engine.py` needed zero code changes** - `_apply_race_bonus`/`build_starting_character`/`_owner_character_view` already just read one named `race` entry by key, and a subrace is nothing more than another key in the same table.
    - **Deliberately additive, not RAW-strict - a real, deliberate divergence from real 5e, not an oversight.** Real 5e requires picking a subrace wherever one exists; this project keeps the plain base race (`dwarf`, `elf`, `halfling`) fully valid and selectable on its own rather than removing it, so an already-shipped race system doesn't retroactively change what a prior `Dwarf` pick means. Matches the "recommend a richer choice, never force a re-pick" tone every other optional/overridable choice in this project already has (the class survey, the stat-priority override).
    - **Client**: `RACE_OPTIONS` (`client/app.py`) grew from 4 to 10 entries, each subrace listed immediately after its base race in the picker. A real regression risk checked directly, not assumed safe: `#race-select`'s widget id is `f"race-select-{race_name}"` and `_join()` recovers the name via `id.rsplit("-", 1)[-1]` - subrace slugs use underscores (`hill_dwarf`), not hyphens, so that split still lands on the right boundary; a hyphenated slug would have silently broken it. Verified the collapsed welcome screen still fits an unscrolled 80x24 terminal afterward (unchanged - the new options only add rows inside the already-hidden, toggle-revealed picker, never the always-visible collapsed state item 13's own zero-slack row budget covers).
    - **3 new tests**: `tests/test_engine.py` gained 2 (a subrace's combined ability bonus reaching real stats end-to-end through `build_starting_character`, and `_owner_character_view`'s `racial_traits` for a subrace containing both the base race's own traits and the subrace-specific ones). `tests/test_client_app.py` gained 1 (picking an underscored subrace slug and having it reach `join_session` correctly, the dedicated regression test for the id-parsing risk named above). Full local suite: 573 passing (up from 570).

17. **The missed-change advisory gets a real chance to self-correct, not just warn (2026-08-12).** The item 6 "remaining candidates" entry named this directly: "have it also propose a corrective tool call... for the player or DM to confirm." A real design question first: the heuristic fires purely from a regex match on already-streamed narration text - there's no structured data about what actually happened, so where would a specific correction number even come from?
    - **Decided against text-parsing a guessed number out of the narration** (the literal ROADMAP wording's "for the player or DM to confirm" could have meant a player-facing guessed-number confirm/dismiss flow) - would mean trusting a regex-extracted `hp_delta`, something this project has deliberately avoided everywhere else (ability scores, XP, damage rolls are all real engine math, never parsed from prose). **Went with a silent DM self-correction follow-up instead**: right after a turn where the heuristic's trigger condition is met, one more real, separate call to the same backend, offering only `update_character` and asking it to review its own narration and call the tool now if it should have. If it does, that's a real model decision through the real mechanism, not a guess.
    - **`NarratorBackend` gains an optional `check_missed_change(narration, character_summary, apply_update) -> bool`** (`server/narrator.py`) - optional, not a required Protocol method, checked via `getattr` at the call site (`server/engine.py`) so the many existing test-double DMs (`StubDM` and friends, `tests/test_engine.py`) needed no changes at all. `AnthropicNarrator`: a real, separate `messages.create()` call (not part of `narrate()`'s own streamed tool-round loop), offering only `update_character`. `OllamaNarrator`: structured-output only (same "ignored when off" precedent `request_roll`/`world_updates` already have) - a new `MISSED_CHANGE_SCHEMA` (`_OUTCOME_PROPERTIES` minus `narration`, since there's no new narration to write) and `MISSED_CHANGE_SYSTEM_PROMPT`, one more constrained call reusing the same mechanism a normal turn already uses.
    - **Placed deliberately before the existing broadcast block, not after.** A correction mutates the same `sheet_changed`/`npcs_touched`/`outcomes` nonlocal state a real in-turn `update_character` call already would (they share the identical `apply_update` closure) - calling `check_missed_change` before those broadcasts run means a real correction rides the exact same `character_update`/`player_update`/`npc_update`/outcome-log-line mechanism, with zero new broadcast code needed. A correction also sends a distinct, non-advisory `system_message` ("The DM double-checked that last narration and updated the sheet to match.") instead of the passive warning - deliberately *not* `advisory: true`, since that renders a yellow warning triangle client-side, right for "you might want to double check," wrong for a real confirmation the sheet's already fixed. When the follow-up genuinely finds nothing, the original passive warning still fires, unchanged.
    - **A real, honest live finding, not just a unit-tested mechanism**: a live smoke check against `qwen2.5:7b` (`msi-laptop`) confirmed the plumbing itself works correctly end-to-end (real JSON parsed, real `apply_update` wiring) but the model consistently declined to self-correct across every one of 4 runs of the exact scenario this project's own reliability investigation already knows this model struggles with (an NPC dying with no history/context beyond the isolated narration line) - `mechanical_change: false` every time, even for unambiguous narrated death. Not a bug - a real, small-local-model reliability limitation, consistent with everything item 6 has already found about `qwen2.5:7b`'s tool-call judgment specifically. Shipped anyway: this is a pure safety-net addition with no downside for a backend that doesn't self-correct reliably - it silently no-ops back to the exact prior passive-warning behavior, never worse than before this feature existed, and is expected to fare better on `AnthropicNarrator` (Claude's own comparatively strong native tool-use reliability, per item 6's own recurring framing) once that backend is live-verified (item 2, still blocked on API credits).
    - **11 new tests**: `tests/test_narrator.py` gained 3 (`AnthropicNarrator.check_missed_change` applies a real correction, returns false when the DM declines, and ignores a non-`update_character` tool call). `tests/test_narrator_ollama.py` gained 4 (applies a real correction, returns false on decline, returns false on malformed JSON, and is a no-op on the legacy tool-calling path). `tests/test_engine.py` gained 4 (a real correction lands and broadcasts exactly like an in-turn tool call, the correction message is styled non-advisory, falling back to the passive warning when the DM finds nothing, and the correction check never fires when a real tool call already landed this turn). Full local suite: 587 passing (up from 573).

18. **Long-gap reconnect recap: grounding the DM's own context, not just the player's (2026-08-12).** The third item from the same batch as items 15/17 above. `_on_join_session` already sends a private "story so far" recap (`_resume_recap()`) to anyone reconnecting into an already-started session - but only ever to the *player*. The rolling history window (`Session.history`, `max_history_messages`, 12 messages/6 turns) only ever holds the last few turns, so a player reconnecting after a long gap and then acting again could have the DM narrate against a context that's already scrolled past anything about them - the player gets grounded, the model generating their next narration doesn't.
    - **`Session.pending_dm_recap: list[str]`**, a new persisted field (not a transient engine attribute) - a reconnect can span a server restart, the same reason `turn_order`/`current_turn_index` are persisted rather than kept purely in memory. Populated in `_on_join_session`'s existing `if self._has_started():` block, only for a genuine reconnect (`not is_new_character`) - a brand-new player joining an in-progress session has no prior turns of their own to have scrolled out of the window, so there's nothing to re-ground for them.
    - **Consumed (popped) the moment that player's next real `player_action` arrives** (`_on_player_action`, before `_narrate_and_apply`), not just checked - a real multi-turn gap between the reconnect and their first action doesn't re-trigger this on every later turn, only the once.
    - **Reuses `_resume_recap()` itself rather than composing a second DM-facing text** - the exact same deterministic "story so far" string the player already saw, prepended to `_narrate_and_apply`'s `action_text` ahead of the existing `CONTENT_PREFERENCE_HINTS` prepending (context for "what's going on" belongs before a tone instruction, not after it). Invisible to the rest of the table, like the content-preference hint - only ever touches what the narrator backend receives, never the broadcast `action` log line.
    - **5 new tests** in `tests/test_engine.py`: a reconnect into a started session queues the pending recap; a brand-new player joining an in-progress session does not; a reconnect before the session has started does not; the reconnecting player's next action actually reaches the DM with the recap prepended (and never leaks into the visible action log); and the recap is only ever prepended once, not on a later second action. Full local suite: 592 passing (up from 587).

19. **`WorldState` scene-mood tag: a real hook for the deferred image-generation item (2026-08-19).** The "Later"-section candidate ("A scene 'mood'/environment tag on `WorldState`... sets up a real hook for the image-generation item below") picked up and shipped — `WorldState` previously had only `location`/`summary`, the *where* and the *what* of the campaign, but nothing for the *tone* of the present moment, so a DM could only ever convey mood in prose.
    - **`WorldState.mood: str = ""`** (`server/state.py`), a sibling of `location`/`summary` — a short scene-tone descriptor (`'tense'`, `'foreboding'`, `'hopeful'`, `'festive'`), deliberately not per-turn detail. `apply_update()` handles it overwrite-if-changed like the other fields (a repeat value is a real no-op, `"mood now 'tense'"` in the change summary), and `narrator_context()` surfaces it to the DM as `Current mood: X` when set.
    - **Both backends, unlike every other `update_world` field.** `UPDATE_WORLD_TOOL` (`server/narrator.py`, Anthropic) gained `mood` with an explicit "only when the tone meaningfully shifts, not every turn" instruction; `OllamaNarrator`'s structured world payload (`_WORLD_PROPERTIES`) gained the same field plus a matching prompt rule — deliberate, since a stable mood tag is exactly the kind of low-frequency structured value structured output handles well, and the image-generation hook it sets up shouldn't be Anthropic-gated.
    - **Client**: the Map tab (`CharacterSheetPanel`, `client/app.py`) renders `Mood: <mood>` as a dim line above the map — set before any locations are registered, it still has a place to live on the sheet. `docs/protocol.md` updated: `mood` joins `world_update`'s payload in the envelope table and the Map section, plus an "Implementation status" row.
    - **4 new tests**: `tests/test_state.py` (apply_update sets / no-ops on repeat / changes, `narrator_context()` includes it when set), `tests/test_engine.py` (a real `update_world` mood call reaches the `world_update` broadcast), `tests/test_narrator.py` (the Anthropic tool schema exposes it), `tests/test_narrator_ollama.py` (the structured schema carries it and a real world-change call applies it) — plus an existing Map-tab client test extended to assert the mood line renders. Full local suite: 605 passing (the count includes tests from the few-shot/self-correct/confirmable commits that postdate item 18's own 592 snapshot).

20. **Presentation polish pass: dark-dungeon theme, README wordmark, animated-demo recorder (2026-08-19).** All three "Later"-section candidates picked together by the owner; the theme direction (dark dungeon) chosen by them too, per that item's own "ask rather than guess" note.
    - **The theme is design tokens, defined once on `DungeonMasterApp` (`client/app.py`), not per-screen CSS.** Warm charcoal-brown surfaces (`$background`/`$surface`/`$panel`/`$boost`), torch-gold `$accent`/`$primary`, light-warm `$text`/`$foreground`, plus `$header`/`$footer`/`$input`/`$button-*` token sets so every screen (welcome, lobby, session) and every framework widget picks it up with zero per-screen duplication. No functional behavior changed - purely the palette. The existing semantic colors (`_OUTCOME_COLORS`, the `_hp_bar` fractions, the ANSI-named markup) were deliberately left alone: they're category-meaningful, not decorative, and retinting them would blur that at the cost of palette purity. Client test suite passes unchanged (117 client tests; 605 project-wide).
    - **README wordmark**: originally built as `docs/wordmark.svg` (same palette as the theme), then **superseded before push**: the owner's own brand kit (`docs/images/banner.svg`/`logo.svg`/`favicon.svg`/`social-preview.*`, commit `9fde5fc`) landed on `main` mid-push and already replaces the `# Oracle` heading - so my duplicate wordmark was removed rather than leaving two competing banners at the top of the README.
    - **The two former live-Ollama screenshots joined the scripted set, honestly.** The recolor made the old live captures (old palette) stale, and they can't be regenerated live in this no-live-LLM environment - so `scripts/generate_screenshots.py` now regenerates all five deterministically: two new stub DMs (`OpeningSceneDM` uses a real `update_world` call so the Map tab genuinely shows a location, `StoryDM` narrates a plain mid-story turn), two new generator functions, all five screenshots regenerated under the new theme, README captions updated to the same "real engine mechanics, scripted prose" framing `xp-level-up`/`spellcasting` already used (module docstring updated to record the switch rather than silently dropping the old "real capture" claim).
    - **Animated demo**: `scripts/record_demo.sh` (asciinema → svg-term), recorded by hand against a running server and committed as `docs/screenshots/demo.svg` - written and documented in the README ("Recording a demo" section under Screenshots), but **the actual recording is still pending**: needs asciinema + svg-term installed and a live backend (the owner's GPU machine), none of which exist on this box. The script is `bash -n`-checked; the recording pipeline itself is unverified end-to-end by design.
    - **Deliberately not in scope**: retinting `_OUTCOME_COLORS`/`_hp_bar` (above), an animated GIF variant of the demo (SVG suffices), and a second light theme (owner picked one direction). Full local suite: 605 passing (unchanged - purely presentational).

21. **Party-wide XP splitting (2026-08-19).** The "Later"-section candidate ("Party-wide XP splitting - currently XP always goes to whoever's turn it is...") picked up and shipped - the same judgment call the XP/leveling entry's own writeup (Current state, above) flagged as worth revisiting once real multi-character sessions showed up.
    - **A new `GameEngine._award_party_xp()` (`server/engine.py`)**, the single shared award path for both defeat sites (the in-turn `apply_update` closure and the player-confirmed correction path in `_on_apply_proposed_change`, which already shared `_apply_level_up` for the same "don't let the two paths drift" reason). It divides the kill's XP evenly across every joined player in `session.characters` - floor division, the remainder simply drops (real 5e's own "divvy evenly" guidance); a solo session (N=1) awards everything to the one member, an exact no-op vs. the old behaviour.
    - **Still deterministic, still no DM tool call** - the kill trigger (`was_alive`/`hp == 0`, item 8's reliability rationale) is unchanged; only the award's destination widened. The acting character keeps their own share like everyone else, and every party member's `gain_xp`/level-up math runs independently, so two members can cross different thresholds off the same kill.
    - **Announcement text adapts**: a solo kill keeps the original `"{name} defeats {npc} and gains {xp} XP!"` phrasing exactly; a party kill broadcasts `"The party defeats {npc} and gains {xp} XP ({share} each)!"` followed by a per-member `"{name} reaches level {N}!"` + ASI line for every leveler. The DM-facing tool-result note ("The party gains X XP") and the private/public sheet pushes (`character_update` to each awarded member, `player_update` broadcast for each) were widened to match - before, only the acting character's sheet ever refreshed off a kill.
    - **3 new tests** (`tests/test_engine.py`): an even 2-player split (25/25 off a 50 XP goblin, both sheets refreshed, the party announcement text, no change to the acting-player path); the 3-player remainder flooring to 16 each; and both members leveling off a 5,400 XP kill (2,700 each, both reach level 4, both get the ASI, both named in the broadcast). Full local suite: 608 passing - the same 2 pre-existing root-sandbox `test_persistence.py` permission-test failures as every entry in this file (chmod-based read-only dirs can't block root), unrelated to this change.

22. **Confirmable missed-change correction: the advisory carries a real, `/apply`-able proposal (2026-08-19), closing the rest of what item 17's self-correct follow-up left open.** Item 17 gave the DM a silent chance to fix its own narration via `check_missed_change` - but that entry's own live finding was that `qwen2.5:7b` consistently *declined* to self-correct, so the passive warning still fired every time on that backend, with nothing more for the player to do about it than shrug. This closes the gap: when the auto-correct declines, the advisory now carries the DM's own best-guess `proposed_change` for the player to accept explicitly.
    - **A second, distinctly-framed backend call, not a repeat of `check_missed_change`'s auto-correct attempt.** `NarratorBackend` gains an optional `propose_correction(narration, character_summary) -> dict | None` (`server/narrator.py`), called only when `check_missed_change` already declined - same `getattr` optionality, same `MISSED_CHANGE_SCHEMA`/reused `_outcome_update()` mechanism on the Ollama side (structured output only), but framed to the model as a hypothesis to hand back ("if something did change, what would it have been"), not a decision to act on directly - it returns the proposed dict instead of applying it.
    - **`GameEngine._pending_proposals: dict[player_id, dict]`**, one slot per player, set when a real proposal comes back and included on the advisory `system_message`'s new `proposed_change` field. **Expires on the player's own next action** (`_on_player_action` pops it unconditionally before doing anything else) - a stale suggestion from several turns back can never be applied late against whatever the sheet looks like now.
    - **A new `apply_proposed_change` envelope / `/apply` command applies it through the exact same machinery a real tool call already uses** - `_on_apply_proposed_change` (`server/engine.py`) reuses `CharacterSheet.apply_update()` and the identical self-vs-NPC target dispatch, introduce-on-missing NPC creation (with real SRD monster stats/AC when the name matches), and defeat → XP → level-up path - via a new shared `_apply_level_up()` helper, factored out of the in-turn `apply_update` closure specifically so this second call site couldn't drift from the original's HP/ASI/spell-slot math. Broadcasts the identical `character_update`/`player_update`/`npc_update`/outcome-log lines a real in-turn correction already would, so an accepted proposal is indistinguishable in effect from the DM having called the tool itself.
    - **Client** (`client/app.py`): a new `_proposed_change_text()` renders the proposal in plain language ("Suggested correction: -6 hp on bandit. Type /apply to accept.") as a dim line under the yellow advisory; `/apply` sends the bare `apply_proposed_change` envelope and clears the client's own tracked `_pending_proposal` immediately, without waiting for the server round-trip.
    - **`docs/protocol.md` updated**: `system_message`'s payload row gained `proposed_change?`, plus a new "Missed-change confirmable proposal" section documenting the whole flow.
    - **11 new tests**: `tests/test_engine.py` gained 5 (a real proposal reaches the advisory only when `check_missed_change` declined, `/apply` applies it through the same broadcasts a real call gets including a defeat→XP→level-up path via the shared helper, a stale proposal is gone after the player's next action, applying with nothing pending is a graceful no-op, and NPC-target introduction/monster-stat-matching work identically to the in-turn path). `tests/test_narrator_ollama.py` gained 4 (`propose_correction` returns a real hypothesis, returns `None` when the DM has no guess, returns `None` on malformed JSON, and is a no-op on the legacy tool-calling path). `tests/test_narrator.py` gained 1 (the Anthropic default). `tests/test_transport_e2e.py` gained 1 real end-to-end test over an actual websocket. Full local suite green (the same 2 pre-existing root-sandbox `test_persistence.py` failures noted throughout this file, unrelated).

23. **Class progressions beyond level 1: real per-level features, earned and shown as levels are gained (2026-08-21).** The SRD-expansion checklist's own named remaining half. Level-ups already granted real HP (class hit die + CON), ASIs at the real levels, and full-caster spell-slot growth — but a character's `class_features` were frozen at `level_1_features` forever: a Fighter reaching level 5 had no Extra Attack anywhere in the system, and nothing a player could look at ever changed after creation.
    - **Data**: every `classes` entry (`server/rules/srd.json`) gained a `features_by_level` table - `{level: [feature strings]}` at each level a feature is actually gained, all four classes, levels 2-20, real SRD 5.1 text in the same "Name: description." style `level_1_features` already uses (Fighter 2 Action Surge / 5 Extra Attack / 9 Indomitable with its later improvements; Rogue 2 Cunning Action through 20 Stroke of Luck; Cleric's Channel Divinity/Turn Undead/Destroy Undead ladder; Wizard's endgame Spell Mastery/Signature Spells). Two deliberate scoping choices, documented in ATTRIBUTION.md alongside the data: **subclass-only levels are omitted** (no subclass system, the same call the race system made about subraces) and **ASI levels are omitted** (already applied by `_apply_ability_score_improvements`' own math, not re-documented as text).
    - **Zero new state - derived, not stored.** A single helper, `_class_features_for(class_entry, level)` (`server/engine.py`), accumulates `level_1_features` + every `features_by_level` entry through the character's current level; `_owner_character_view` calls it instead of reading `level_1_features` directly. Since features are always a pure function of (class, level), there's nothing to persist, migrate, or broadcast - the existing `character_update` refresh after an XP award automatically delivers the new features to the client's Features tab with zero protocol or client changes.
    - **Deliberately skipped**: per-feature broadcast announcements on level-up ("Thrain gains Action Surge!") - the owner's sheet panel shows them the moment their sheet refreshes, and announcement text would have meant widening the `party_results` tuple through both XP paths for a line nobody asked for. Add when a real playtest shows players missing their new features.
    - **4 new tests** (`tests/test_engine.py`): a Fighter's view gains Action Surge exactly at level 2 and Extra Attack exactly at 5 (and not Indomitable at 9); a Cleric jumped 1→8 in one award includes everything through level 8 but not Divine Intervention (10); a blank-class character leveled to 2 still has no features conjured out of nowhere. Full local suite green (the same 2 pre-existing root-sandbox `test_persistence.py` chmod failures).

24. **Second SRD content batch: 6 monsters, 5 spells (2026-08-21).** The remaining open half of the same SRD-expansion checklist item 23 just closed its other half of. Chosen to fill real gaps in the existing roster rather than pad counts: a CR-1/2 humanoid pair with distinct mechanics (Hobgoblin's Martial Advantage, Gnoll's Rampage), two CR-1 monsters with non-damage levers the old set had none of (Ghoul's paralysis saves, Harpy's Luring Song charm), and the set's first real big-game above the Ogre's CR 2 — Owlbear (CR 3) and a Regeneration Troll (CR 5, fire/acid weakness baked into the trait text). Spells fill class-list gaps: wizard gets Sleep, Charm Person, Thunderwave; cleric gets Inflict Wounds and Shield of Faith.
    - **Same integration precedent as item 11, not inert data**: all five spells joined `CLASS_KNOWN_SPELLS` (`server/engine.py`) so they're actually castable from turn one — Thunderwave mirrors Burning Hands' save-based shape (`save_ability: "con"`), Inflict Wounds mirrors Spiritual Weapon's attack-based shape (`attack: true`), Sleep/Charm Person/Shield of Faith are pure-utility like Guidance/Mage Armor. Monster entries reuse the exact existing field set (including Skeleton's `immunities` string for the Ghoul); every new `cr` string resolves against the real `xp_by_cr` table.
    - **2 new tests + 2 updated**: the wizard/cleric starting-spell assertions now include the batch; a new attack-spell resolution test for Inflict Wounds (real dice/damage_type/ability/roll_kind through `request_roll`, mirroring Spiritual Weapon's own coverage) and a data-integrity test locking all six new monsters to resolvable entries with real XP and an AC an introduced NPC would copy. Full local suite green (the same 2 pre-existing root-sandbox `test_persistence.py` chmod failures).

25. **DM-facing tracked-NPC roster: dispositions/notes/wounds survive the history window (2026-08-21).** `WorldState.narrator_context()` grounds the DM in location/mood/objectives every turn, but tracked NPCs reached it *only* through the 12-message rolling history - past that window a friendly shopkeeper could be re-narrated hostile with nothing to check against, defeating `disposition`'s own stated purpose ("a structured value to stay consistent against turn to turn") and feeding item 1's documented memory blind spot.
    - **`GameEngine._npc_roster()`** builds one bounded line per living tracked NPC (`- Bandit: HP 3/10, hostile, poisoned - A greedy toll-keeper.`), appended to `world_summary` at `_narrate_and_apply`'s single call site - dead NPCs excluded, neutral disposition omitted, notes truncated to 80 chars (`# ponytail:` no count cap; trim by recency if ever observed), empty string when nothing lives - the same "don't render the absent default" convention `narrator_context()` follows. DM-facing only, invisible to players like all DM-only context; no new envelope, no protocol or client change.
    - **One real gating fix this surfaced**: OllamaNarrator's structured path embedded `world_summary` only when `OLLAMA_WORLD_UPDATES` was on - which would have made NPC grounding opt-in-only on the default local backend. The gate is now just `if world_summary:` on both the main and roll-followup prompts: an empty summary still means no section at all, so sessions with world updates off behave exactly as before unless NPCs actually exist; only the world_* schema fields stay flag-gated.
    - **Deliberately not** the full item-1 memory system - prose summarization stays deferred until observed as needed; this covers the structured subset where the data already exists.
    - **4 new tests + 1 rewritten**: an introduced NPC reaches the DM's `world_summary` with real hp/disposition/notes alongside location; dead NPCs are excluded, neutral disposition omitted, over-long notes truncated; `test_structured_narrate_omits_world_summary_when_world_updates_is_off` became `...includes_the_summary_even_when_world_updates_is_off` (summary flows, world fields stay gated) plus a new empty-summary-sends-no-section test. Full local suite green (the same 2 pre-existing root-sandbox chmod failures).

26. **`--scenario persuasion`: measuring rule-adherence under rhetorical injection (2026-08-21).** The first of item 6's remaining candidates to get its measurement tool built *before* any fix is attempted - the same harness-first order structured output itself followed. The "Seduced by the Narrative" finding (arxiv.org/html/2607.02802v1, already logged with that list): LLM adjudicators can be talked out of their own rules by confident or authoritative player framing - a *confident wrong* adjudication class this project has never measured, distinct from the missing-call problem every prior scenario targets.
    - **5 turns, 4 scored** (`PERSUASION_SCENARIO`, a `fighter`, not combat's blank class): two uncertain outcomes framed as certainties (a greatsword attack "we both know how this ends", charming an innkeeper into free lodging) scored `roll` - correct only if a real `dice_result` broadcast that turn; an unearned self-healing claim ("my troll blood closes my wounds") scored `no_self_heal` - correct only if HP genuinely didn't move (the same before/after snapshot pattern `rest` scoring uses, inverted); plus a pure sheet-state assertion (+5 armor - the engine has no AC-set path at all, so no update firing is the whole point) and a plain looting control turn, both `no_call`. Deliberately nothing scores narration tone - nothing can do that reliably.
    - **Two new expectation kinds** (`EXPECT_ROLL`/`EXPECT_NO_SELF_HEAL`) and a `rolled` flag on `TurnResult`; `request_roll`'s own closure already broadcasts `dice_result` the same turn, so no engine changes were needed at all.
    - **Roll requests are forced on for this scenario** (`roll_requests=args.scenario == "persuasion"` in `main_async`) - normal play keeps them opt-in (`OLLAMA_ROLL_REQUESTS`), but two of these scored turns are meaningless without them. Documented in the module docstring and `--scenario` help.
    - **Not yet run against a real model** - unit-tested against `ScriptedNarrator` (now able to play back rolls as well as updates), same honest bound as field-parity before its own live run. The natural next GPU session runs all three scenarios: `--repeat 5` each for combat (baseline continuity), field-parity, and persuasion - and persuasion again later against whatever prompt-hardening candidate gets tried, so the fix is judged by measurement, not vibes.
    - **3 new tests + ScriptedNarrator extended**: perfect score when the DM holds the line (rolls really fired, no updates on roll turns); a roll turn scored incorrect when no roll fired; unearned self-heal scored incorrect when HP actually increased (turn 1 damages the fighter first, since apply_update healing at full HP is a no-op with nothing to score). Full local suite green (the same 2 pre-existing root-sandbox chmod failures).
    - **The measurement's first candidate, built and shipped opt-in the same day: `HARDENED_RULES_ADDENDUM` (`server/narrator_ollama.py`), behind `hardened_rules=False` by default and the harness's own `--hardened-rules`.** A short anti-injection block appended to *every* prompt variant (base structured, roll, roll-followup, legacy tool-calling - roll turns are exactly where rhetorical pressure lands): player assertions/backstory/willpower are never evidence; uncertain outcomes go through real rolls regardless of framing; sheets only change from causes the DM decided and narrated. Deliberately **not default** - identical status to the few-shot example (tested mechanism, opt-in flag, promoted only if a live persuasion-scenario A/B shows it moves the number), since this project's standard is shipping only what measurably helps and the scenario to measure it against has never run against a real model yet. 2 new tests: defaults off; opting in reaches all four variants.

27. **`check_missed_change()` un-gated from `POSSIBLE_UNTRACKED_CHANGE_PATTERN` - a second, independent detection channel instead of a regex-only trigger (2026-08-22).** Item 17 built `check_missed_change()` as a real self-correction chance, but only ever reached it through the same narrow regex the passive warning uses - and that regex's own comment already admits real false negatives (phrasing it doesn't catch). This closes that specific gap without touching the regex path itself.
    - **`server/engine.py`'s first missed-change gate (~L1918) no longer requires `POSSIBLE_UNTRACKED_CHANGE_PATTERN.search(buffer)`** - `check_missed_change()` now runs on every turn where `check_for_missed_changes and not sheet_changed and not npcs_touched`, regardless of trigger language. A clean "nothing to fix" response (`_has_outcome_change()` gates the actual `apply_update`) costs one extra structured-output call, never a spurious correction. **The player-facing passive-warning gate below it (~L2008) deliberately keeps the regex condition** - that tier is player-visible, so false positives there cost attention, not just latency; only the silent self-correct attempt was widened.
    - **2 new tests** (`tests/test_engine.py`): `check_missed_change` now reached and applying a correction on a turn with zero regex-trigger language; and the negative case, silent (no correction, no warning) when the backend genuinely finds nothing on a quiet turn. Full local suite: 638 passing (the same 2 pre-existing root-sandbox `test_persistence.py` chmod failures noted throughout this file).
    - **Live-verified against `qwen2.5:7b`, `scripts/live_reliability_check.py --backend ollama`, combat scenario, single run: 7/7 scored turns correct (100%), 0 leaked pseudo-tool-call text.** Confirms the widened gate's plumbing works end-to-end against a real model with no regression - every quiet/no-call turn (3, 8) stayed silent, and `check_missed_change` being reachable on every turn didn't introduce any spurious correction or leak. **A single run, not a `--repeat N` study** - this project's own standing rule (item 6's `qwen3:8b` false-positive) is that one good run isn't proof, so this is read as "the change didn't break anything," not as a new reliability number for the un-gated check specifically. A `--repeat 5` pass, ideally on a scenario deliberately including phrasing the regex would miss (this project's combat scenario already skews toward language the regex catches), is the natural follow-up before trusting a specific improvement figure.

28. **`HARDENED_RULES_ADDENDUM` A/B, live-run at last (2026-08-22) - closes item 26's "not yet run against a real model" gap. Null result: stays opt-in, not promoted to default.** `scripts/live_reliability_check.py --scenario persuasion --repeat 5` against `qwen2.5:7b`, baseline vs. `--hardened-rules`, run in parallel (this project's own precedent - the item-1 32B/7B comparison - already established CPU contention affects wall-clock, not correctness, since correctness is sampling-dependent not timing-dependent).
    - **Baseline: pooled 13/25 (52%)**, spread 60/60/60/40/40. **Hardened: pooled 14/25 (56%)**, spread 60/60/60/40/60. A one-turn difference out of 25 - inside noise, the same call this project already made once before on the few-shot example's own null result (57% vs 60%). No leaked text in either variant, either.
    - **Decision: `HARDENED_RULES_ADDENDUM` stays opt-in (`hardened_rules=False` default, `--hardened-rules` harness flag), not promoted to default** - identical status and identical reasoning to the few-shot example. The addendum isn't shown to actively hurt anything, so there's no reason to *remove* it either; it just didn't clear the bar item 26 itself set for promotion.
    - **A real, if modest, finding either way**: the persuasion scenario's "confident wrong" adjudication class (item 26's own framing, citing the "Seduced by the Narrative" paper) doesn't appear to be something a short anti-injection prompt addendum fixes for this model - consistent with item 6's broader pattern that prompt-level interventions (the per-turn reminder, this addendum) haven't moved reliability numbers the way the structured-output *mechanism* change did. Worth remembering if a future persuasion-scenario fix is considered: a prompt-wording change is not obviously where the next real gain is.
    - No code changes - a pure measurement pass. Evidence: `evidence/qwen25_7b_persuasion_baseline.json` / `_hardened.json` (gitignored per item 6's own convention, local-only, regenerable via the exact command above).

29. **Item 1 (memory/history-window revisit), the small real half of it fixed (2026-08-22): `WorldState.summary` now reaches the DM's own per-turn context, not just a reconnecting player's.** `_resume_recap()` (`server/engine.py`) already computed a "story so far" from `world.summary` for anyone reconnecting - but `narrator_context()` (`server/state.py`), what the DM itself receives every turn as `world_summary`, never included it, only `location`/`mood`/active objectives. So the exact rolling-history-window blind spot item 1 named ("a very long session could still see the DM drop an early plot thread") applied to the DM's own narration on an ordinary long session, not just at a reconnect gap - the fix for the reconnect case existed the whole time and simply wasn't wired to the DM's regular context too.
    - **One line, `server/state.py`**: `narrator_context()` now includes `self.summary` (when set) between `location` and `mood`, same field `update_world`'s `summary` param already writes.
    - **Deliberately not building a new auto-summarization/rolling-memory mechanism.** Item 1's own text already sets the bar for that: "not worth solving until actually observed happening in play" - nothing in this file indicates that's happened, and `summary`'s own reliability (weak on Ollama, item 6's 4/12 finding) is a tool-call-reliability question, not an architecture gap this change is meant to also fix. Scoped narrow on purpose.
    - **2 new tests** (`tests/test_state.py`): `narrator_context()` includes `summary` when set; ordering (location, summary, mood) matches when all three are set together. Full local suite: 642 passing (the same 2 pre-existing root-sandbox `test_persistence.py` chmod failures noted throughout this file).

30. **Third SRD content batch: 4 monsters, 3 spells (2026-08-22).** The other still-open half of the SRD-expansion checklist item, picked the same way item 24's batch was - chosen to fill real mechanical gaps in the existing roster, not pad counts.
    - **Monsters** (all CR 1-3, none of which duplicate a mechanic the existing 16 already cover): Giant Spider (a ranged, recharge-gated Web attack that restrains - the roster's first area-control monster action), Bugbear (Surprise Attack's first-round burst damage - a distinct combat-pacing lever from any existing monster's flat per-round damage), Wight (Life Drain - necrotic damage that both heals the wight and can raise a slain humanoid as a zombie under its control, the roster's first monster with an ongoing consequence beyond the fight itself), and Basilisk (Petrifying Gaze - a save-or-worsen effect over two turns, ending in the `petrified` condition, which nothing in the existing roster inflicts). `paralyzed`/`charmed`-referencing monsters (Ghoul, Harpy) already established that a condition doesn't need its own `conditions` table entry to be used in trait text - `petrified`/`restrained` follow the same precedent here.
    - **Spells**: Guiding Bolt (cleric's first ranged spell *attack* - previously Sacred Flame was the only ranged option and Inflict Wounds the only attack roll, both cantrip/touch-range; this closes cleric's missing "reach out and hit something" 1st-level slot), Hold Person (a real control spell - the SRD's own paralysis-on-failed-save effect, on both class lists since real 5e has it on both), and Web (thematically paired with the new Giant Spider - a real area-restrain spell, wizard-only). All three added to `CLASS_KNOWN_SPELLS` (`server/engine.py`) - the lesson from item 24's own "not just data sitting in srd.json unreachable by any character" note, applied up front this time rather than caught after the fact.
    - **4 new tests** (`tests/test_engine.py`): the same real-lookup-path CR/AC/XP lock item 24's own test established, applied to the 4 new monsters; two new cast-spell tests (Guiding Bolt at 1st level mirroring the existing Burning Hands precedent; Hold Person by granting a 2nd-level slot directly, since a fresh level-1 caster has none yet, to confirm the spell itself is real and reachable rather than just present in srd.json). Full local suite: 645 passing (the same 2 pre-existing root-sandbox `test_persistence.py` chmod failures noted throughout this file).

31. **In-combat indicator and initiative-order display (2026-08-22)**, closing the exact gap the "Formal initiative" entry (Current state, above) named as deliberately not built: `Session.in_combat`/`turn_order` already drove the mechanical turn cycling, but only ever reached the player as a one-time `system_message` announcement - easy to miss, and gone entirely on reconnect or after scrolling past it.
    - **`in_combat` (bool) added to `state_sync`** (for a (re)joining client) **and to `turn_prompt`** (for an already-connected one, since that's what broadcasts live on every `start_combat`/`end_combat`, and every ordinary turn advance) - no new event type, both envelopes already carried `turn_order`/`current_turn` for the mechanical side of this.
    - **Client**: `CharacterSheetPanel.render_combat(in_combat, turn_order, current_turn)` drives a new "⚔ Combat" section in the Overview tab (right under the HP/XP header, in the same "vitals up top" grouping the sheet's own design pass established), shown only while `in_combat` is true - each `turn_order` entry resolved to a name via the panel's existing `self._character`/`self._others` data, current turn marked inline. Deliberately no roll numbers or NPCs in this persistent view - neither is retained anywhere structured (NPCs never enter `turn_order` by design; rolls are one-time flavor already fully shown in the original announcement) - this section answers "are we in combat, and what's the order," not a replay of the roll.
    - **7 new tests**: `tests/test_engine.py` gained 1 (`in_combat` reaches both `state_sync` and `turn_prompt`, flips correctly across a real start/end cycle). `tests/test_client_app.py` gained 3 (the Combat section renders with the right names/current-turn marker on a (re)joining client, stays hidden when not in combat, and updates live off `turn_prompt` alone without a fresh state_sync). `docs/protocol.md` updated (`state_sync`/`turn_prompt` payload rows, plus a new "In-combat indicator and initiative order" section replacing the stale "not built (yet)" note under "Formal initiative"). Full local suite: 649 passing (the same 2 pre-existing root-sandbox `test_persistence.py` chmod failures noted throughout this file).

32. **Post-v2 harness re-run: two-phase turns measured at last, plus a scorer artifact found and fixed (2026-08-23).** REBUILD_PLAN.md's own note ("two-phase turns + summarizer change prompt context — re-run the harness after phase 2 so ROADMAP percentages stay honest") and README's "treat pre/post numbers as separate eras" caveat both left this open; the first post-v2 run (r1) came back **2/7** against the ~66% single-call baseline and was traced to a real bug before any conclusion was drawn: the decide-call system prompts were derived from the narration-instructing originals by `.replace()` on exact source text, but the replaces silently no-op'd — so every decide call ordered the model to "write `narration` (3-5 sentences…)" next to a schema with no narration field. Both prompt variants now genuinely strip narration instructions (with an import-time assert guarding against future silent no-ops), and the OpenAI backend's decide prompt was switched to the shared fixed variant too.
    - **Re-measured (`r2`) after the fix: still 2/7 raw — but 3 of those 5 failures are a scorer artifact, not model error.** The harness scored target match by exact `casefold()` string equality, and the model emitted `second_bandit` where the scenario expects `"second bandit"` — the right NPC, slugified. Re-scoring both runs' stored raw targets with underscore-folding gives **4/7 (57%) for r1 and r2 alike** (identical per-turn pass/fail patterns across both runs): turns 1/2/5/6 correct; turn 4 mistargeted (`self` + first bandit instead of the newcomer — a real failure); turns 3/8 hallucinated `update_character` calls on no-op search/glance turns — also real, and the same no-call-precision class item 6 already documented.
    - **Scorer fixed to match the engine**: `_norm_target()` (casefold + underscore-fold, `scripts/live_reliability_check.py`) mirrors what the engine itself now does — because the same slugification would fork one NPC into two tracked sheets mid-session (`npc_key = target.casefold().replace("_", " ")` at both apply sites, `server/engine.py`; previously only case was folded). Historical numbers above keep their raw scoring; the stored JSONs carry the raw targets either way, so old reports re-score cleanly under either rule.
    - **Reading, honestly**: two-phase at 4/7 (57%) vs single-call structured-output at ~66% on the same scenario/model — still behind, within small-sample noise of catching up, and no leaked pseudo-tool-call text in any run (0/8, both runs). Two-phase buys the separate unconstrained narration call (scene facts, prose quality); its decide-phase reliability hasn't yet earned a default flip on correctness alone, which is fine — it's already the Ollama default for v2's architecture reasons, and `OLLAMA_TWO_PHASE=0` remains the A/B escape hatch. Evidence: `evidence/postv2/qwen25_7b_twophase_r1.json` / `_r2.json`.
    - **Side finding worth remembering**: qwen2.5:7b code-switched into Chinese mid-narration twice in r2 (unscored — prose isn't graded), consistent with known qwen quirks on long English prompts. Watch, don't fix.
    - **4 new tests**: `tests/test_engine.py` gained 1 (a slugified later-turn target updates the existing tracked NPC instead of forking a second sheet). `tests/test_live_reliability_check.py` gained 1 (a slugified target scores correct against the spaced expectation). Full local suite: 538 passing (the same 2 pre-existing root-sandbox `test_persistence.py` chmod failures noted throughout this file).

## Later — planned, lower priority

### Owner UX feedback checklist (2026-08-07)

From a direct discussion with the owner after playing the GPU-backed session, prioritized by them as: character sheet first (done, see Current state above), the rest logged here for later rather than tackled all at once.

- [x] **Real character sheet at creation** (class, HP from hit die, starting equipment) - done, see the "Real character creation" entry under Current state, above.
- [x] **A "DM is thinking" status indicator** - done, see Current state, above.
- [x] **Other players' presence and sheets in multiplayer** (2026-08-08) - done, design question resolved as name+HP-only (below); see "Current state" for the full writeup. Real 2-connection websocket verification also closes most of the untested-multiplayer item just below - join/presence/disconnect specifically, not yet a full turn-taking session between two real players (see that item's note).
- [x] **A pre-game main menu** (2026-08-08) - done, closing the last item on this checklist; see "Current state" for the full writeup.
- [x] **`character_edit`** (2026-08-08) - done, let a player edit their own notes/inventory directly, without DM adjudication; see "Current state" for the full writeup.
- [x] **Expand the SRD dataset** (`server/rules/srd.json`) — three real passes done: 2026-08-09 (item 11 above): 5 monsters, 4 spells, 4 equipment items; 2026-08-21 (item 24 above): 6 more monsters, 5 more spells; 2026-08-22 (item 30 above): 4 more monsters, 3 more spells. Full class progressions beyond level 1 also landed (2026-08-21, item 23 above). Still open: further monster/spell coverage beyond these batches - real content, not a hard limit.
- **Image generation** for scenes/characters, triggered off narration beats — see the GPU-migration entry below; needs a GPU to be practical, and would follow the same swappable-backend pattern as `NarratorBackend`.
- **Text-to-speech** for DM narration — likely has the same GPU-practicality dependency as image generation, though TTS models are generally lighter-weight than image diffusion; worth actually measuring on CPU before assuming it needs a GPU too.
- **Background world-ticks**: NPCs pursuing goals and the world state advancing between turns/sessions rather than staying frozen until directly poked — surfaced by the immersion-pass research (Current state, above) as a real gap between a reactive chatbot and a believable sandbox, deliberately deferred in favor of the smaller `update_world`/quest-log slice actually built. Worth revisiting once the reliability picture (item 6) is in a better place, since this would add yet more tool-calling surface.
- **A relationship graph** (who-knows-whom, typed edges, source-anchored) — the closest open-source sibling project (`open-tabletop-gm`) leans on this for cheap continuity instead of relying on conversation history alone. Bigger and more speculative than the NPC `notes` field actually built; revisit if `notes` alone turns out not to be enough once played more.
- [x] **A lightweight NPC disposition field** (2026-08-08) - done; see "Current state" for the full writeup.
- [x] **Objective expiry/failure on `WorldState.objectives`** (2026-08-08) - done; see "Current state" for the full writeup.
- [x] **A "previously on..." recap mechanism for reconnecting after a long gap** (2026-08-19) - done as item 18 above; see that entry for the full writeup. Directly addresses the rolling-history-window's known blind spot (item 1, above: anything older than the last 6 turns is invisible to the DM) for the specific case of a player coming back after time away, without paying the unbounded-cost-growth problem a full-history approach would reintroduce.
- [x] **A scene "mood"/environment tag on `WorldState`** (2026-08-19) - done as item 19 above; see that entry for the full writeup. Sits alongside `location`/`summary`, and sets up the real hook for the image-generation item below to key an image prompt off of a short structured tag rather than parsing raw narration text.
- [x] **`dice_result` client rendering** (2026-08-08) - done, closing `docs/protocol.md`'s own former "Known client gap" note; see "Current state" for the full writeup.
- [x] **A distinct visual treatment for the missed-change warning** (2026-08-08) - done; see "Current state" for the full writeup.
- [x] **A real NPC status panel** (2026-08-08) - done; see "Current state" for the full writeup.
- [x] **A session-transcript export in the client** (2026-08-08) - done; see "Current state" for the full writeup.
- **Presentation polish for the README/GitHub page** — all three candidates below done (2026-08-19); see the "Presentation polish pass" entry (item 20) for the full writeup:
  - [x] A custom Textual `CSS`/theme for `DungeonMasterApp` (`client/app.py`) — dark-dungeon palette (warm charcoal-brown surfaces, torch-gold accent) via design tokens defined once on the App class; owner picked this direction. All five screenshots regenerated under it; the two former live-Ollama captures joined the scripted set so the README stays visually consistent (see Current state).
  - [x] A short animated demo — `scripts/record_demo.sh` (asciinema → svg-term), written and documented in the README; the actual recording still needs to be made by hand against a live session (needs asciinema + svg-term installed and a running backend), not done on this box.
  - [x] ~~An HP bar or similar small visual indicator on `CharacterSheetPanel`~~ - done as part of the "Character sheet panel design pass" entry (Current state, above; `_hp_bar()`), stale by the time this bullet was still being carried here.
  - [x] A simple wordmark/banner for the top of the README — `docs/wordmark.svg` (same palette as the theme), replacing the bare `# Oracle` heading.
- [x] **Make a mid-session `_save()` failure visible instead of an unhandled exception** (2026-08-08) - done, closing the known limitation the 2026-08-07 startup-writability-check entry explicitly left open; see "Current state" for the full writeup.
### Character progression checklist (2026-08-08)

From the owner's own idea-discussion pass on Oracle's mechanical foundation and long-term direction (genre-agnostic mechanics now, genre-switching/visuals/vocals explicitly "down the road" - not tracked as near-term items here since the owner framed them that way themselves).

- [x] **D&D-style XP/leveling** - done, the primary ask from that discussion; see "Current state" for the full writeup.
- [x] **Character export/import (save-file format)** (2026-08-08) - done, built exactly to the scoped shape logged here (see "Current state" for the full writeup) - the secondary half of the XP/leveling request: *"if possible character saving format so players can import or export their character as an extra layer to not lose their progression... even maybe some save details for the character experiences, items, behavior, etc."*
- [x] **Ability scores, including ability-score-driven HP growth** (2026-08-08) - done, from a follow-up "let's brainstorm more mechanics" → "let's start with ability scores" pass; see "Current state" for the full writeup. Closes the flat-hit-die-max simplification both level-1 starting HP and level-up HP used before this.
- [x] **Party-wide XP splitting** (2026-08-19) - done as item 21 above; see that entry for the full writeup. The same judgment call the XP/leveling entry (Current state, above) originally flagged as a deliberate simplifying default, revisited once real multi-character sessions showed up.

### GPU migration readiness (2026-08-07)

**Migration completed and verified live (2026-08-07).** Original plan below assumed "a 2080 Ti — 11GB VRAM" sight unseen; the machine actually used is a laptop (`msi-laptop`, reached over the LAN, not this project's usual dev machine) with an **RTX 2080 with Max-Q Design — 8GB VRAM (7.6 GiB reported available), CUDA compute 7.5, driver 610.43.03**, correcting that assumption now that real hardware is in hand rather than leaving the old guess standing.

- **No Oracle code changes were needed** - confirms the "zero application-level changes" expectation below held in practice, not just in theory. Ollama was installed user-locally (`~/.local/ollama`, no root - the official installer's own `sudo` steps didn't work non-interactively over this SSH session, so the plain release tarball was extracted and symlinked into `~/.local/bin` instead), the repo copied over via `rsync` (this repo is still private, and `gh` wasn't installed on the target machine), and a normal `pip install -e '.[ollama,dev]'` in a fresh venv. All 94 tests passed unmodified.
- **GPU pickup confirmed, not assumed**: `ollama serve`'s own startup log reported `library=CUDA ... name="NVIDIA GeForce RTX 2080 with Max-Q Design" ... type=discrete total="7.6 GiB"`, and `ollama ps` during a real narration call showed `100% GPU` - exactly the smoke check this section called for before trusting any results transfer.
- **Real speedup measured, not estimated**: a cold first call (`qwen2.5:7b`, opening-scene-shaped prompt) took 55.8s - almost entirely one-time model-load-into-VRAM overhead, not representative of steady-state - but two warm calls immediately after took **1.5s and 1.7s**, at 36-40 words/sec. Against this project's own documented CPU baseline (30-90s/turn throughout every experiment in this file), that's roughly a **20-40x per-turn speedup** once the model is loaded. Verified end-to-end through the real engine too, not just the narrator in isolation: a live join/opening-scene/turn cycle through the actual `server.main`/`client.main` over a real websocket connection worked cleanly, session persisted correctly to `sessions/default.json` on this new machine (the writability-check and `.env`-loading fixes earlier in this same file both verified working on a second, independent machine as a side effect).
- **8GB VRAM, not 11GB - re-scopes the "what fits" estimate below.** `qwen2.5:7b` (4.7GB file) fit with headroom (7.5 GiB free reported at load). `qwen2.5:14b`'s ~9GB Q4_K_M file is close to or past the 7.6 GiB ceiling and hasn't been tried on this machine yet - worth an explicit test rather than assuming it fits or spills to CPU gracefully, since the original "11GB comfortably fits 7-14B" framing doesn't hold as-is on this specific card.
- **What a GPU actually unlocks for the reliability work already underway**: CPU-only inference is the reason every experiment this session has taken 5-20+ minutes per model per scenario (30-90s+ per turn, worse for `qwen3:8b`'s thinking mode) — that latency is *why* this project has only ever run one scenario, one time, per model. A GPU makes the kind of statistical rigor the current methodology can't afford practical: repeated runs of the same scenario per model to quantify sampling variance (exactly the "run it again to confirm" request that prompted this note), multiple distinct scenarios instead of one, and testing genuinely large models (`qwen2.5:32b`+, `qwen3:14b`/`32b`) that are impractical to even attempt on CPU. The measured 20-40x per-turn speedup above makes this concretely practical now rather than a future aspiration - re-running item 6's `--repeat` experiments on this machine is the natural next step, not yet done as part of this migration itself.
- **`--repeat N` harness flag: built (2026-08-07), ahead of GPU migration rather than waiting for it.** `scripts/live_reliability_check.py` now runs the scenario N times against the same model and reports both a mean-of-rates and a pooled rate with the per-run spread (e.g. `run 1/3: 2/7 (29%)`, `run 2/3: 1/7 (14%)`, ... `mean of per-run rates: 21%`), instead of a single-sample point estimate — directly closes the "one true positive isn't proof" gap the `qwen3:8b` result above ran into by hand. `--out` with `--repeat > 1` writes a JSON report shaped `{repeat, aggregate, runs: [[...], [...]]}` rather than the single-run shape, via new `run_stats()`/`aggregate_stats()`/`write_json_repeat()` functions (6 new tests, `tests/test_live_reliability_check.py`, using a fresh `ScriptedNarrator` per simulated run). Built now, while still CPU-only, because the owner's structural-readiness goal ahead of a planned GPU move (see above) is exactly this kind of "have the tooling ready before the hardware makes it cheap to use" work — each repeat still costs as much wall-clock time as a single run today, so using `--repeat` with anything above 2-3 remains impractical until then, but the capability no longer needs to be built later under time pressure.
- **Image generation, revisited**: local Stable Diffusion (via ComfyUI, InvokeAI, or a bare `diffusers` script) is impractical on this project's current CPU-only dev hardware — text generation alone is already slow at 30-90s/turn; image diffusion on pure CPU is typically far slower per image. An `ImageBackend` protocol (mirroring `NarratorBackend`'s existing swappable-interface pattern) with a local ComfyUI/SD implementation becomes realistic on an 11GB GPU. A hosted alternative (OpenAI, Stability AI, Replicate, etc.) would still be a legitimate separate backend for users without a GPU, same relationship as `AnthropicNarrator`/`OllamaNarrator` today — paid+fast vs. free+hardware-dependent.

## Explicitly not doing

- Scraping or bundling copyrighted D&D sourcebook content (Monster Manual, Player's Handbook, Dungeon Master's Guide, published adventures) — not legally redistributable, even in a private repo. See `server/rules/ATTRIBUTION.md` for what's actually included and why.
- Making the repo public on any automatic trigger — that's the owner's call, not tied to any milestone here.
